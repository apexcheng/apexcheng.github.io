# Claude Code 项目入口

先读取项目根目录 `AGENTS.md`，再严格按其中的任务路由读取相关规则。

补充约定：

1. 只修改当前需求直接相关的文件。
2. 保留用户已有改动，不做无关重构、格式化或改名。
3. 在当前实际环境中运行可用检查；无法运行时明确说明。
4. 不确定的行为标注“需运行验证”。
5. commit、push 和部署仅在用户明确要求时执行。

本文件不是独立规则源；与项目规则冲突时以 `AGENTS.md` 和其路由到的 `docs/` 文件为准。
