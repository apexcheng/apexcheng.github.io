<section class="troubleshooting-map" aria-label="AI 商品图项目排查地图">
  <header class="trouble-header">
    <div>
      <span class="eyebrow">TROUBLESHOOTING</span>
      <h2>出了问题，不要先改 Prompt</h2>
      <p>先判断问题属于哪一层。整套方向错、某一张图错、产品结构错，回去检查的地方完全不同。</p>
    </div>
    <div class="rule-chip">先定位层级，再修改</div>
  </header>

  <div class="question-entry">
    <span>发现问题</span>
    <strong>这次到底错在哪里？</strong>
  </div>

  <div class="route-grid">
    <article class="route-card route-entry">
      <div class="route-head">
        <span>01</span>
        <strong>任务根本没有按商品图流程走</strong>
      </div>
      <p>例如没有先检查资料、没有做竞品分析，或者分析完直接出图。</p>
      <div class="route-back">
        <span>回到</span>
        <strong>ecommerce-product-image</strong>
      </div>
      <small>检查总入口和流程控制规则。</small>
    </article>

    <article class="route-card route-analysis">
      <div class="route-head">
        <span>02</span>
        <strong>竞品或评价分析本身不对</strong>
      </div>
      <p>例如竞品图没有分析到位，或者把用户问题错误映射成图片动作。</p>
      <div class="route-split">
        <div><span>竞品问题</span><strong>competitor-analysis</strong></div>
        <div><span>评价问题</span><strong>review-analysis</strong></div>
      </div>
      <small>专业分析错了，就回专业 Skill，不要先改图片任务。</small>
    </article>

    <article class="route-card route-material">
      <div class="route-head">
        <span>03</span>
        <strong>缺资料、缺角度、照片看不清</strong>
      </div>
      <p>例如想做接口图却没有接口照片，或者产品角度被遮挡。</p>
      <div class="route-back material-back">
        <span>回到</span>
        <strong>资料闸门</strong>
      </div>
      <small>暂停对应任务，明确告诉用户补什么、怎么拍。</small>
    </article>

    <article class="route-card route-plan">
      <div class="route-head">
        <span>04</span>
        <strong>整套图片方向就错了</strong>
      </div>
      <p>例如本来应该突出 HarmonyOS，却把整套主图都做成触控板；或者详情页顺序不合理。</p>
      <div class="route-back plan-back">
        <span>回到</span>
        <strong>商品图片生成计划.md</strong>
      </div>
      <small>这是方案问题，不是某一张图的 Prompt 问题。</small>
    </article>

    <article class="route-card route-task">
      <div class="route-head">
        <span>05</span>
        <strong>只有某一张图不对</strong>
      </div>
      <p>例如触控板太小、标题位置不好、产品占比不合适。</p>
      <div class="route-back task-back">
        <span>回到</span>
        <strong>商品图片单图任务.md</strong>
      </div>
      <small>修改这一张图的目标、构图、文案或审核标准。</small>
    </article>

    <article class="route-card route-structure">
      <div class="route-head">
        <span>06</span>
        <strong>产品结构被 AI 改坏了</strong>
      </div>
      <p>例如按键数量变化、触控板位置移动、接口凭空出现。</p>
      <div class="structure-stack">
        <span>先查真实参考图</span>
        <i>→</i>
        <span>再查结构校准图</span>
        <i>→</i>
        <strong>最后加强单图禁止项</strong>
      </div>
      <small>结构错误优先检查事实依据，而不是只改画面风格。</small>
    </article>
  </div>

  <footer class="trouble-verdict">
    <span>排查口诀</span>
    <strong>流程错看总入口，分析错看专业 Skill，资料不够先补拍，方向错回整套计划，单图错改单图任务，结构错查真实参考图。</strong>
  </footer>
</section>

<style>
  .troubleshooting-map {
    --blue: #2563eb;
    --cyan: #0891b2;
    --violet: #7c3aed;
    --green: #0f9f76;
    --amber: #d97706;
    --red: #dc2626;
    background:
      radial-gradient(circle at 90% 0%, rgba(129, 140, 248, 0.14), transparent 30%),
      linear-gradient(145deg, rgba(255, 255, 255, 0.98), rgba(248, 250, 252, 0.95));
    border: 1px solid rgba(148, 163, 184, 0.24);
    border-radius: 24px;
    box-shadow: 0 26px 70px rgba(15, 23, 42, 0.09);
    margin: 34px 0;
    overflow: hidden;
  }

  .trouble-header,
  .route-grid,
  .trouble-verdict {
    padding-left: clamp(22px, 4vw, 40px);
    padding-right: clamp(22px, 4vw, 40px);
  }

  .trouble-header {
    align-items: end;
    display: grid;
    gap: 24px;
    grid-template-columns: minmax(0, 1fr) auto;
    padding-bottom: 30px;
    padding-top: 36px;
  }

  .eyebrow {
    color: var(--blue);
    display: inline-flex;
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.15em;
  }

  .trouble-header h2 {
    color: var(--ink);
    font-size: clamp(30px, 5vw, 48px);
    letter-spacing: -0.045em;
    line-height: 1.12;
    margin: 9px 0 12px;
  }

  .trouble-header p {
    color: var(--muted);
    line-height: 1.75;
    margin: 0;
    max-width: 760px;
  }

  .rule-chip {
    background: rgba(37, 99, 235, 0.08);
    border: 1px solid rgba(37, 99, 235, 0.16);
    border-radius: 999px;
    color: var(--blue);
    font-size: 12px;
    font-weight: 850;
    padding: 9px 12px;
    white-space: nowrap;
  }

  .question-entry {
    background: rgba(15, 23, 42, 0.97);
    color: white;
    padding: 22px clamp(22px, 4vw, 40px);
    text-align: center;
  }

  .question-entry span {
    color: #67e8f9;
    display: block;
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.12em;
  }

  .question-entry strong {
    display: block;
    font-size: clamp(22px, 3.5vw, 32px);
    margin-top: 5px;
  }

  .route-grid {
    display: grid;
    gap: 0;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    padding-bottom: 36px;
    padding-top: 16px;
  }

  .route-card {
    border-bottom: 1px solid rgba(148, 163, 184, 0.22);
    min-width: 0;
    padding: 24px 22px;
  }

  .route-card:nth-child(odd) {
    border-right: 1px solid rgba(148, 163, 184, 0.22);
  }

  .route-card:nth-last-child(-n + 2) {
    border-bottom: 0;
  }

  .route-head {
    align-items: start;
    display: grid;
    gap: 12px;
    grid-template-columns: 34px minmax(0, 1fr);
  }

  .route-head span {
    align-items: center;
    background: rgba(37, 99, 235, 0.08);
    border-radius: 10px;
    color: var(--blue);
    display: inline-flex;
    font-size: 11px;
    font-weight: 900;
    height: 32px;
    justify-content: center;
    width: 32px;
  }

  .route-head strong {
    color: var(--ink);
    font-size: 18px;
    line-height: 1.4;
  }

  .route-card > p {
    color: var(--muted);
    line-height: 1.65;
    margin: 12px 0 0 46px;
  }

  .route-card > small {
    color: var(--soft);
    display: block;
    line-height: 1.55;
    margin: 11px 0 0 46px;
  }

  .route-back {
    align-items: baseline;
    background: rgba(37, 99, 235, 0.07);
    border: 1px solid rgba(37, 99, 235, 0.12);
    border-radius: 14px;
    display: flex;
    gap: 8px;
    margin: 16px 0 0 46px;
    padding: 12px 14px;
  }

  .route-back span {
    color: var(--soft);
    font-size: 11px;
  }

  .route-back strong {
    color: var(--blue);
    font-size: 14px;
  }

  .material-back {
    background: rgba(217, 119, 6, 0.08);
    border-color: rgba(217, 119, 6, 0.14);
  }

  .material-back strong {
    color: var(--amber);
  }

  .plan-back {
    background: rgba(124, 58, 237, 0.08);
    border-color: rgba(124, 58, 237, 0.14);
  }

  .plan-back strong {
    color: var(--violet);
  }

  .task-back {
    background: rgba(8, 145, 178, 0.08);
    border-color: rgba(8, 145, 178, 0.14);
  }

  .task-back strong {
    color: var(--cyan);
  }

  .route-split {
    display: grid;
    gap: 10px;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin: 16px 0 0 46px;
  }

  .route-split > div {
    background: rgba(255, 255, 255, 0.74);
    border: 1px solid rgba(148, 163, 184, 0.22);
    border-radius: 14px;
    padding: 12px;
  }

  .route-split span {
    color: var(--soft);
    display: block;
    font-size: 10px;
  }

  .route-split strong {
    display: block;
    font-size: 13px;
    margin-top: 4px;
    overflow-wrap: anywhere;
  }

  .structure-stack {
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin: 16px 0 0 46px;
  }

  .structure-stack span,
  .structure-stack strong {
    border-radius: 999px;
    font-size: 11px;
    padding: 8px 10px;
  }

  .structure-stack span {
    background: rgba(148, 163, 184, 0.11);
    color: var(--muted);
  }

  .structure-stack strong {
    background: rgba(220, 38, 38, 0.09);
    color: var(--red);
  }

  .structure-stack i {
    color: #94a3b8;
    font-style: normal;
  }

  .trouble-verdict {
    align-items: center;
    background: linear-gradient(110deg, rgba(37, 99, 235, 0.96), rgba(124, 58, 237, 0.94));
    display: grid;
    gap: 10px;
    grid-template-columns: auto minmax(0, 1fr);
    padding-bottom: 22px;
    padding-top: 22px;
  }

  .trouble-verdict span {
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 999px;
    color: white;
    font-size: 10px;
    font-weight: 900;
    padding: 8px 10px;
  }

  .trouble-verdict strong {
    color: white;
    font-size: clamp(15px, 2vw, 19px);
    line-height: 1.55;
  }

  @media (max-width: 760px) {
    .trouble-header {
      grid-template-columns: 1fr;
    }

    .rule-chip {
      justify-self: start;
    }

    .route-grid {
      grid-template-columns: 1fr;
    }

    .route-card,
    .route-card:nth-child(odd),
    .route-card:nth-last-child(-n + 2) {
      border-bottom: 1px solid rgba(148, 163, 184, 0.22);
      border-right: 0;
    }

    .route-card:last-child {
      border-bottom: 0;
    }

    .trouble-verdict {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 560px) {
    .troubleshooting-map {
      border-radius: 20px;
      margin-left: -8px;
      margin-right: -8px;
    }

    .trouble-header,
    .route-grid,
    .trouble-verdict {
      padding-left: 18px;
      padding-right: 18px;
    }

    .route-card {
      padding-left: 0;
      padding-right: 0;
    }

    .route-split {
      grid-template-columns: 1fr;
    }

    .structure-stack {
      align-items: flex-start;
      flex-direction: column;
    }

    .structure-stack i {
      transform: rotate(90deg);
    }
  }
</style>
