<section class="troubleshooting-map" aria-label="AI 商品图项目排查地图">
  <header class="trouble-header">
    <div>
      <span class="eyebrow">TROUBLESHOOTING</span>
      <h2>出了问题，不要先改 Prompt</h2>
      <p>先判断问题属于哪一层。分析、商品策略、视觉设计、产品依据和出图整改，回去检查的地方完全不同。</p>
    </div>
    <div class="rule-chip">先定位层级，再修改</div>
  </header>

  <div class="question-entry">
    <span>发现问题</span>
    <strong>这次到底错在哪里？</strong>
  </div>

  <div class="route-grid">
    <article class="route-card route-entry">
      <div class="route-head">
        <span>01</span>
        <strong>任务根本没有按商品图流程走</strong>
      </div>
      <p>例如没有先检查资料、没有做竞品分析，或者还没完成计划和确认就直接出图。</p>
      <div class="route-back">
        <span>回到</span>
        <strong>ecommerce-product-image</strong>
      </div>
      <small>检查总入口和流程控制规则。</small>
    </article>

    <article class="route-card route-analysis">
      <div class="route-head">
        <span>02</span>
        <strong>竞品或评价分析本身不对</strong>
      </div>
      <p>例如竞品页面没有读完整，或者把不适合用图片解决的用户问题硬塞进方案。</p>
      <div class="route-split">
        <div><span>竞品问题</span><strong>competitor-analysis</strong></div>
        <div><span>评价问题</span><strong>review-analysis</strong></div>
      </div>
      <small>专业分析错了，就回专业 Skill，不要先改图片任务。</small>
    </article>

    <article class="route-card route-material">
      <div class="route-head">
        <span>03</span>
        <strong>商品事实或真实依据不足</strong>
      </div>
      <p>例如关键结构无法从现有实拍确认，或者 AI 把真实结构、颜色、接口改坏了。</p>
      <div class="route-back material-back">
        <span>回到</span>
        <strong>输入与真实依据</strong>
      </div>
      <small>先查事实和参考图；只有关键结构无法确认时，才暂停对应图片并要求补拍。</small>
    </article>

    <article class="route-card route-plan">
      <div class="route-head">
        <span>04</span>
        <strong>卖点、图片任务或顺序不对</strong>
      </div>
      <p>例如最大卖点选错、某张图根本不该存在，或者详情页说服顺序不合理。</p>
      <div class="route-back plan-back">
        <span>回到</span>
        <strong>ecommerce-product-image + 生成计划</strong>
      </div>
      <small>这是 WHAT / WHY 的问题，不要交给视觉 Skill 或生成阶段修。</small>
    </article>

    <article class="route-card route-task">
      <div class="route-head">
        <span>05</span>
        <strong>画面、构图或 Prompt 不对</strong>
      </div>
      <p>例如画面太空、产品占比不合适、空间层次不足，或者最终 Prompt 和当前设计不一致。</p>
      <div class="route-back task-back">
        <span>回到</span>
        <strong>image-art-direction</strong>
      </div>
      <small>只调整受影响图片的画面设计、输入和最终 Prompt，不重新规划整套图片。</small>
    </article>

    <article class="route-card route-structure">
      <div class="route-head">
        <span>06</span>
        <strong>出图后到底是小修还是正式改设计</strong>
      </div>
      <p>先问：如果从零重新生成，这个改动是否仍然必须保留？</p>
      <div class="structure-stack">
        <span>不需要长期保留 → 直接小修</span>
        <i>→</i>
        <strong>需要长期保留 → 更新这张图的任务和 Prompt</strong>
      </div>
      <small>两种情况都只处理受影响图片，不因为一张图的问题重做整套方案。</small>
    </article>
  </div>

  <footer class="trouble-verdict">
    <span>排查口诀</span>
    <strong>流程错看总入口，分析错看专业 Skill，事实错查真实依据，策略错回生成计划，视觉错回 Art Direction，出图后先分清小修还是正式设计改变。</strong>
  </footer>
</section>

<style>
  .troubleshooting-map {
    --blue: #2563eb;
    --cyan: #0891b2;
    --violet: #7c3aed;
    --green: #0f9f76;
    --amber: #d97706;
    --red: #dc2626;
    background:
      radial-gradient(circle at 90% 0%, rgba(129, 140, 248, 0.14), transparent 30%),
      linear-gradient(145deg, rgba(255, 255, 255, 0.98), rgba(248, 250, 252, 0.95));
    border: 1px solid rgba(148, 163, 184, 0.24);
    border-radius: 24px;
    box-shadow: 0 26px 70px rgba(15, 23, 42, 0.09);
    container-name: troubleshooting-map;
    container-type: inline-size;
    margin: 34px 0;
    overflow: hidden;
  }

  .trouble-header,
  .route-grid,
  .trouble-verdict {
    padding-left: clamp(22px, 4vw, 40px);
    padding-right: clamp(22px, 4vw, 40px);
  }

  .trouble-header {
    align-items: end;
    display: grid;
    gap: 24px;
    grid-template-columns: minmax(0, 1fr) auto;
    padding-bottom: 30px;
    padding-top: 36px;
  }

  .eyebrow {
    color: var(--blue);
    display: inline-flex;
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.15em;
  }

  .trouble-header h2 {
    color: var(--ink);
    font-size: clamp(30px, 5vw, 48px);
    letter-spacing: -0.045em;
    line-height: 1.12;
    margin: 9px 0 12px;
  }

  .trouble-header p {
    color: var(--muted);
    line-height: 1.75;
    margin: 0;
    max-width: 760px;
  }

  .rule-chip {
    background: rgba(37, 99, 235, 0.08);
    border: 1px solid rgba(37, 99, 235, 0.16);
    border-radius: 999px;
    color: var(--blue);
    font-size: 12px;
    font-weight: 850;
    padding: 9px 12px;
    white-space: nowrap;
  }

  .question-entry {
    background: rgba(15, 23, 42, 0.97);
    color: white;
    padding: 22px clamp(22px, 4vw, 40px);
    text-align: center;
  }

  .question-entry span {
    color: #67e8f9;
    display: block;
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.12em;
  }

  .question-entry strong {
    display: block;
    font-size: clamp(22px, 3.5vw, 32px);
    margin-top: 5px;
  }

  .route-grid {
    display: grid;
    gap: 0;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    padding-bottom: 36px;
    padding-top: 16px;
  }

  .route-card {
    border-bottom: 1px solid rgba(148, 163, 184, 0.22);
    min-width: 0;
    padding: 24px 22px;
  }

  .route-card:nth-child(odd) {
    border-right: 1px solid rgba(148, 163, 184, 0.22);
  }

  .route-card:nth-last-child(-n + 2) {
    border-bottom: 0;
  }

  .route-head {
    align-items: start;
    display: grid;
    gap: 12px;
    grid-template-columns: 34px minmax(0, 1fr);
  }

  .route-head span {
    align-items: center;
    background: rgba(37, 99, 235, 0.08);
    border-radius: 10px;
    color: var(--blue);
    display: inline-flex;
    font-size: 11px;
    font-weight: 900;
    height: 32px;
    justify-content: center;
    width: 32px;
  }

  .route-head strong {
    color: var(--ink);
    font-size: 18px;
    line-height: 1.4;
  }

  .route-card > p {
    color: var(--muted);
    line-height: 1.65;
    margin: 12px 0 0 46px;
  }

  .route-card > small {
    color: var(--soft);
    display: block;
    line-height: 1.55;
    margin: 11px 0 0 46px;
  }

  .route-back {
    align-items: baseline;
    background: rgba(37, 99, 235, 0.07);
    border: 1px solid rgba(37, 99, 235, 0.12);
    border-radius: 14px;
    display: flex;
    gap: 8px;
    margin: 16px 0 0 46px;
    padding: 12px 14px;
  }

  .route-back span {
    color: var(--soft);
    font-size: 11px;
  }

  .route-back strong {
    color: var(--blue);
    font-size: 14px;
    min-width: 0;
    overflow-wrap: anywhere;
  }

  .material-back {
    background: rgba(217, 119, 6, 0.08);
    border-color: rgba(217, 119, 6, 0.14);
  }

  .material-back strong {
    color: var(--amber);
  }

  .plan-back {
    background: rgba(124, 58, 237, 0.08);
    border-color: rgba(124, 58, 237, 0.14);
  }

  .plan-back strong {
    color: var(--violet);
  }

  .task-back {
    background: rgba(8, 145, 178, 0.08);
    border-color: rgba(8, 145, 178, 0.14);
  }

  .task-back strong {
    color: var(--cyan);
  }

  .route-split {
    display: grid;
    gap: 10px;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin: 16px 0 0 46px;
  }

  .route-split > div {
    background: rgba(255, 255, 255, 0.74);
    border: 1px solid rgba(148, 163, 184, 0.22);
    border-radius: 14px;
    padding: 12px;
  }

  .route-split span {
    color: var(--soft);
    display: block;
    font-size: 10px;
  }

  .route-split strong {
    display: block;
    font-size: 13px;
    margin-top: 4px;
    overflow-wrap: anywhere;
  }

  .structure-stack {
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin: 16px 0 0 46px;
  }

  .structure-stack span,
  .structure-stack strong {
    border-radius: 999px;
    font-size: 11px;
    padding: 8px 10px;
  }

  .structure-stack span {
    background: rgba(148, 163, 184, 0.11);
    color: var(--muted);
  }

  .structure-stack strong {
    background: rgba(220, 38, 38, 0.09);
    color: var(--red);
  }

  .structure-stack i {
    color: #94a3b8;
    font-style: normal;
  }

  .trouble-verdict {
    align-items: center;
    background: linear-gradient(110deg, rgba(37, 99, 235, 0.96), rgba(124, 58, 237, 0.94));
    display: grid;
    gap: 10px;
    grid-template-columns: auto minmax(0, 1fr);
    padding-bottom: 22px;
    padding-top: 22px;
  }

  .trouble-verdict span {
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 999px;
    color: white;
    font-size: 10px;
    font-weight: 900;
    padding: 8px 10px;
  }

  .trouble-verdict strong {
    color: white;
    font-size: clamp(15px, 2vw, 19px);
    line-height: 1.55;
  }

  @container troubleshooting-map (max-width: 820px) {
    .trouble-header {
      grid-template-columns: 1fr;
    }

    .rule-chip {
      justify-self: start;
    }

    .route-grid {
      grid-template-columns: 1fr;
    }

    .route-card,
    .route-card:nth-child(odd),
    .route-card:nth-last-child(-n + 2) {
      border-bottom: 1px solid rgba(148, 163, 184, 0.22);
      border-right: 0;
    }

    .route-card:last-child {
      border-bottom: 0;
    }

    .trouble-verdict {
      grid-template-columns: 1fr;
    }
  }

  @container troubleshooting-map (max-width: 560px) {
    .troubleshooting-map {
      border-radius: 20px;
      margin-left: -8px;
      margin-right: -8px;
    }

    .trouble-header,
    .route-grid,
    .trouble-verdict {
      padding-left: 18px;
      padding-right: 18px;
    }

    .route-card {
      padding-left: 0;
      padding-right: 0;
    }

    .route-split {
      grid-template-columns: 1fr;
    }

    .structure-stack {
      align-items: flex-start;
      flex-direction: column;
    }

    .structure-stack i {
      transform: rotate(90deg);
    }
  }
</style>
