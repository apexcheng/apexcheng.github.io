<section class="x51c-overview" aria-label="X51C AI 商品图总流程">
  <header class="overview-hero">
    <span class="eyebrow">X51C PRODUCT IMAGE FLOW</span>
    <h2>先分析和确认，再进入图片生成</h2>
    <p>完整商品图任务从一个入口开始。竞品分析默认必经；评价分析只在有评价资料时使用；最终方案由商品图总入口统一汇总。</p>
  </header>

  <div class="pipeline" aria-label="商品图主流程">
    <article>
      <span>01</span>
      <strong>准备资料</strong>
      <small>商品事实、实拍图、竞品链接和可选评价数据</small>
    </article>
    <i>→</i>
    <article>
      <span>02</span>
      <strong>专业分析</strong>
      <small>竞品分析默认执行，评价分析按资料决定是否使用</small>
    </article>
    <i>→</i>
    <article>
      <span>03</span>
      <strong>汇总判断</strong>
      <small>商品事实 + 市场表达 + 用户反馈</small>
    </article>
    <i>→</i>
    <article>
      <span>04</span>
      <strong>生成计划</strong>
      <small>先落盘整套主图和详情图方案</small>
    </article>
    <i>→</i>
    <article>
      <span>05</span>
      <strong>用户确认</strong>
      <small>确认卖点、顺序、文案和禁止内容</small>
    </article>
    <i>→</i>
    <article>
      <span>06</span>
      <strong>逐张生成</strong>
      <small>拆单图任务、检查真实参考图、生成并审核</small>
    </article>
  </div>

  <section class="roles" aria-label="三个 Skill 的分工">
    <article class="role-card lead-card">
      <div class="role-topline">
        <span>MAIN SKILL</span>
        <b>唯一入口</b>
      </div>
      <strong>ecommerce-product-image</strong>
      <h3>商品图负责人</h3>
      <p>接收任务、检查资料、调用专业 Skill、汇总结论、生成整套计划，并控制什么时候继续、暂停或等待确认。</p>
      <div class="role-note">最终图片方案由它统一决定</div>
    </article>

    <article class="role-card market-card">
      <div class="role-topline">
        <span>SPECIALIST</span>
        <b>默认必经</b>
      </div>
      <strong>competitor-analysis</strong>
      <h3>竞品分析</h3>
      <p>逐个分析用户提供的竞品链接，读取页面、截图主图和详情图、OCR 文案，再整理市场表达和机会点。</p>
      <div class="role-note">结果返回商品图负责人</div>
    </article>

    <article class="role-card review-card">
      <div class="role-topline">
        <span>OPTIONAL</span>
        <b>有资料才用</b>
      </div>
      <strong>review-analysis</strong>
      <h3>评价分析</h3>
      <p>从评价和用户问题里找高频关注、误解和痛点，判断哪些内容值得被带回商品图方案。</p>
      <div class="role-note">没有评价资料就直接跳过</div>
    </article>
  </section>

  <footer class="flow-rule">
    <span>关键规则</span>
    <strong>竞品分析不会直接决定图片方案，所有结论都要回到 ecommerce-product-image，再和商品事实一起形成最终计划。</strong>
  </footer>
</section>

<style>
  .x51c-overview {
    --blue: #2563eb;
    --cyan: #0891b2;
    --violet: #7c3aed;
    background:
      radial-gradient(circle at 10% 0%, rgba(56, 189, 248, 0.14), transparent 30%),
      radial-gradient(circle at 90% 5%, rgba(124, 58, 237, 0.12), transparent 30%),
      linear-gradient(145deg, rgba(255, 255, 255, 0.98), rgba(241, 247, 255, 0.95));
    border: 1px solid rgba(96, 165, 250, 0.24);
    border-radius: 24px;
    box-shadow: 0 26px 68px rgba(15, 23, 42, 0.09);
    container-name: x51c-overview;
    container-type: inline-size;
    margin: 32px 0;
    overflow: hidden;
  }

  .overview-hero,
  .roles,
  .flow-rule {
    padding-left: clamp(20px, 4vw, 38px);
    padding-right: clamp(20px, 4vw, 38px);
  }

  .overview-hero {
    padding-bottom: 30px;
    padding-top: 36px;
  }

  .eyebrow,
  .role-topline span {
    color: var(--blue);
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.14em;
  }

  .overview-hero h2 {
    color: var(--ink);
    font-size: clamp(31px, 5vw, 50px);
    letter-spacing: -0.045em;
    line-height: 1.12;
    margin: 9px 0 13px;
  }

  .overview-hero p {
    color: var(--muted);
    line-height: 1.75;
    margin: 0;
    max-width: 780px;
  }

  .pipeline {
    align-items: stretch;
    background: rgba(15, 23, 42, 0.97);
    display: grid;
    gap: 10px;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr);
    padding: 22px clamp(20px, 4vw, 38px);
  }

  .pipeline article {
    min-width: 0;
  }

  .pipeline article span {
    color: #67e8f9;
    display: block;
    font-size: 10px;
    font-weight: 900;
  }

  .pipeline article strong {
    color: #f8fafc;
    display: block;
    font-size: 15px;
    margin-top: 5px;
  }

  .pipeline article small {
    color: #94a3b8;
    display: block;
    line-height: 1.5;
    margin-top: 5px;
  }

  .pipeline > i {
    align-self: center;
    color: #475569;
    font-style: normal;
  }

  .roles {
    display: grid;
    gap: 16px;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    padding-bottom: 34px;
    padding-top: 34px;
  }

  .role-card {
    background: rgba(255, 255, 255, 0.8);
    border: 1px solid rgba(148, 163, 184, 0.23);
    border-radius: 18px;
    min-width: 0;
    overflow-wrap: anywhere;
    padding: 20px;
  }

  .lead-card {
    border-top: 4px solid var(--blue);
  }

  .market-card {
    border-top: 4px solid var(--cyan);
  }

  .review-card {
    border-top: 4px solid var(--violet);
  }

  .role-topline {
    align-items: center;
    display: flex;
    gap: 10px;
    justify-content: space-between;
  }

  .role-topline b {
    background: rgba(37, 99, 235, 0.08);
    border-radius: 999px;
    color: var(--blue);
    font-size: 10px;
    padding: 6px 8px;
    white-space: nowrap;
  }

  .market-card .role-topline span,
  .market-card .role-topline b {
    color: var(--cyan);
  }

  .market-card .role-topline b {
    background: rgba(8, 145, 178, 0.08);
  }

  .review-card .role-topline span,
  .review-card .role-topline b {
    color: var(--violet);
  }

  .review-card .role-topline b {
    background: rgba(124, 58, 237, 0.08);
  }

  .role-card > strong {
    display: block;
    font-size: 14px;
    margin-top: 16px;
  }

  .role-card h3 {
    color: var(--ink);
    font-size: 21px;
    margin: 5px 0 9px;
  }

  .role-card p {
    color: var(--muted);
    line-height: 1.65;
    margin: 0;
  }

  .role-note {
    border-top: 1px dashed rgba(148, 163, 184, 0.3);
    color: var(--soft);
    font-size: 11px;
    margin-top: 15px;
    padding-top: 12px;
  }

  .flow-rule {
    align-items: center;
    background: linear-gradient(110deg, rgba(37, 99, 235, 0.96), rgba(14, 116, 144, 0.94));
    display: grid;
    gap: 12px;
    grid-template-columns: auto minmax(0, 1fr);
    padding-bottom: 20px;
    padding-top: 20px;
  }

  .flow-rule span {
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 999px;
    color: white;
    font-size: 10px;
    font-weight: 900;
    padding: 7px 9px;
    white-space: nowrap;
  }

  .flow-rule strong {
    color: white;
    font-size: 15px;
    line-height: 1.55;
  }

  @container x51c-overview (max-width: 900px) {
    .pipeline {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .pipeline > i {
      display: none;
    }

    .roles {
      grid-template-columns: 1fr;
    }
  }

  @container x51c-overview (max-width: 620px) {
    .x51c-overview {
      border-radius: 20px;
      margin-left: -8px;
      margin-right: -8px;
    }

    .overview-hero,
    .roles,
    .flow-rule {
      padding-left: 18px;
      padding-right: 18px;
    }

    .overview-hero h2 {
      font-size: 34px;
    }

    .pipeline {
      grid-template-columns: 1fr;
      gap: 18px;
      padding-left: 18px;
      padding-right: 18px;
    }

    .flow-rule {
      grid-template-columns: 1fr;
    }

    .flow-rule span {
      justify-self: start;
    }
  }
</style>
