<section class="delivery-gate" aria-label="评价报告发送与回写控制">
  <header>
    <span>DELIVERY CONTROL</span>
    <h2>发送不是最后一步，发送成功才打开回写闸门</h2>
    <p>这条规则防止“群里没有收到报告，但表格却已经被标记为已通知”。</p>
  </header>

  <div class="delivery-track" aria-label="报告交付链路">
    <article>
      <span>01</span>
      <strong>报告落盘</strong>
      <small>最终 Markdown 保存为可追踪文件</small>
    </article>
    <i aria-hidden="true">→</i>
    <article>
      <span>02</span>
      <strong>在线文档</strong>
      <small>创建并回读校验钉钉文档</small>
    </article>
    <i aria-hidden="true">→</i>
    <article>
      <span>03</span>
      <strong>Webhook 发送</strong>
      <small>把正文与文档入口发送到群</small>
    </article>
  </div>

  <div class="gate-layout">
    <div class="gate-question">
      <span>GATE</span>
      <strong>Webhook 是否成功？</strong>
      <small>只有明确成功结果，才允许继续改变业务状态。</small>
    </div>

    <div class="gate-outcomes">
      <article class="success-card">
        <div class="outcome-label">SUCCESS</div>
        <h3>按来源表分组回写</h3>
        <p>首次评价的 recordId 只回写首次评价表，追加评价的 recordId 只回写追加评价表。</p>
        <ul>
          <li>非正面评价 → 已通知</li>
          <li>正面评价 → 无需通知</li>
        </ul>
      </article>

      <article class="failure-card">
        <div class="outcome-label">FAILED</div>
        <h3>保留原业务状态</h3>
        <p>发送失败时不执行状态回写，让这些评价仍然留在下一次待处理范围内。</p>
        <ul>
          <li>不重新查询评价</li>
          <li>不伪装成本次任务已完成</li>
        </ul>
      </article>
    </div>
  </div>

  <footer>
    <span>异常兜底</span>
    <strong>如果报告已经发送成功，但状态回写失败，系统会追加发送风险提示，明确下一次可能重复处理。</strong>
  </footer>
</section>

<style>
  .delivery-gate {
    --accent: #ec4899;
    background: #18181b;
    border: 1px solid #3f3f46;
    border-radius: 24px;
    box-shadow: 0 28px 72px rgba(9, 9, 11, 0.22);
    color: #fafafa;
    container-name: delivery-gate;
    container-type: inline-size;
    margin: 34px 0;
    overflow: hidden;
  }

  .delivery-gate > header,
  .gate-layout,
  .delivery-gate > footer {
    padding-left: clamp(22px, 4vw, 38px);
    padding-right: clamp(22px, 4vw, 38px);
  }

  .delivery-gate > header {
    padding-bottom: 28px;
    padding-top: 34px;
  }

  .delivery-gate > header span,
  .delivery-track article span,
  .gate-question > span,
  .outcome-label {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 10px;
    font-weight: 800;
    letter-spacing: 0.13em;
  }

  .delivery-gate > header span {
    color: #f9a8d4;
  }

  .delivery-gate h2 {
    color: #fafafa;
    font-size: clamp(30px, 4.6vw, 48px);
    letter-spacing: -0.045em;
    line-height: 1.1;
    margin: 10px 0 12px;
    max-width: 860px;
  }

  .delivery-gate > header p {
    color: #a1a1aa;
    line-height: 1.7;
    margin: 0;
  }

  .delivery-track {
    background: #27272a;
    border-bottom: 1px solid #3f3f46;
    border-top: 1px solid #3f3f46;
    display: grid;
    gap: 14px;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr);
    padding: 22px clamp(22px, 4vw, 38px);
  }

  .delivery-track article {
    min-width: 0;
  }

  .delivery-track article span {
    color: #f9a8d4;
  }

  .delivery-track article strong {
    display: block;
    font-size: 17px;
    margin-top: 6px;
  }

  .delivery-track article small {
    color: #a1a1aa;
    display: block;
    line-height: 1.5;
    margin-top: 5px;
  }

  .delivery-track i {
    align-self: center;
    color: #71717a;
    font-style: normal;
  }

  .gate-layout {
    display: grid;
    gap: 22px;
    grid-template-columns: minmax(220px, 0.7fr) minmax(0, 1.5fr);
    padding-bottom: 34px;
    padding-top: 34px;
  }

  .gate-question {
    align-content: center;
    background: var(--accent);
    border-radius: 18px;
    display: grid;
    min-height: 220px;
    padding: 24px;
  }

  .gate-question > span {
    color: rgba(255, 255, 255, 0.72);
  }

  .gate-question strong {
    display: block;
    font-size: 27px;
    line-height: 1.18;
    margin-top: 12px;
  }

  .gate-question small {
    color: rgba(255, 255, 255, 0.82);
    display: block;
    line-height: 1.6;
    margin-top: 10px;
  }

  .gate-outcomes {
    display: grid;
    gap: 14px;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .gate-outcomes article {
    border: 1px solid #3f3f46;
    border-radius: 18px;
    min-width: 0;
    padding: 22px;
  }

  .success-card {
    background: #fafafa;
    color: #18181b;
  }

  .failure-card {
    background: #27272a;
  }

  .outcome-label {
    color: var(--accent);
  }

  .gate-outcomes h3 {
    font-size: 21px;
    margin: 11px 0 8px;
  }

  .gate-outcomes p,
  .gate-outcomes li {
    font-size: 13px;
    line-height: 1.65;
  }

  .success-card p,
  .success-card li {
    color: #52525b;
  }

  .failure-card p,
  .failure-card li {
    color: #d4d4d8;
  }

  .gate-outcomes ul {
    margin: 14px 0 0;
    padding-left: 18px;
  }

  .delivery-gate > footer {
    align-items: center;
    background: #09090b;
    border-top: 1px solid #3f3f46;
    display: grid;
    gap: 14px;
    grid-template-columns: auto minmax(0, 1fr);
    padding-bottom: 20px;
    padding-top: 20px;
  }

  .delivery-gate > footer span {
    background: rgba(236, 72, 153, 0.16);
    border: 1px solid rgba(236, 72, 153, 0.34);
    border-radius: 999px;
    color: #f9a8d4;
    font-size: 10px;
    font-weight: 800;
    padding: 7px 9px;
    white-space: nowrap;
  }

  .delivery-gate > footer strong {
    color: #e4e4e7;
    font-size: 14px;
    line-height: 1.55;
  }

  @container delivery-gate (max-width: 760px) {
    .gate-layout {
      grid-template-columns: 1fr;
    }

    .gate-question {
      min-height: 0;
    }
  }

  @container delivery-gate (max-width: 560px) {
    .delivery-gate {
      border-radius: 18px;
      margin-left: -8px;
      margin-right: -8px;
    }

    .delivery-gate > header,
    .gate-layout,
    .delivery-gate > footer,
    .delivery-track {
      padding-left: 18px;
      padding-right: 18px;
    }

    .delivery-gate h2 {
      font-size: 34px;
    }

    .delivery-track,
    .gate-outcomes {
      grid-template-columns: 1fr;
    }

    .delivery-track i {
      display: none;
    }

    .delivery-gate > footer {
      align-items: start;
      grid-template-columns: 1fr;
    }

    .delivery-gate > footer span {
      justify-self: start;
    }
  }
</style>
