<div class="qeo-hero">
  <div class="qeo-hero__copy">
    <span class="qeo-kicker">QIANNIU LEARNING MAP</span>
    <h2>先走一遍消费者的路，再翻到后台看答案</h2>
    <p class="qeo-hero__lead">
      <strong>消费者负责搜、看、选、付、收货和反馈</strong>
      <span>；商家则通过商品、营销、订单、ERP、售后和数据模块，让这段购物经历真正发生。</span>
    </p>
    <div class="qeo-hero__verdict">
      <span class="qeo-hero__verdict-icon" aria-hidden="true">
        <svg viewBox="0 0 24 24" role="img">
          <path d="M9 18h6M10 22h4M8.6 15.4A7 7 0 1 1 15.4 15.4C14.5 16.2 14 17 14 18h-4c0-1-.5-1.8-1.4-2.6Z" />
          <path d="M12 2V1M4.2 5.2l-.8-.8M19.8 5.2l.8-.8M2 12H1M23 12h-1" />
        </svg>
      </span>
      <span class="qeo-hero__verdict-copy">
        <span>这篇文章的读法</span>
        <strong>先看用户经历了什么 · 再看后台对应什么 · 最后理解运营为什么这样做</strong>
      </span>
    </div>
  </div>

  <div class="qeo-stage-map" aria-label="商品经营的三个阶段">
    <div class="qeo-stage-map__head">
      <span>一段完整购物经历</span>
      <strong>前台动作与后台工作的 3 次对应</strong>
    </div>

    <div class="qeo-stage-card is-blue">
      <span class="qeo-stage-card__number">01</span>
      <div class="qeo-stage-card__panel">
        <div class="qeo-stage-card__user">
          <span>用户动作</span>
          <strong>用户搜、看、选、付</strong>
        </div>
        <div class="qeo-stage-card__backend">
          <span class="qeo-stage-card__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" role="img">
              <rect x="3" y="4" width="18" height="12" rx="1.5" />
              <path d="M8 21h8M12 16v5" />
            </svg>
          </span>
          <span class="qeo-stage-card__label">后台对应</span>
          <div class="qeo-stage-card__copy">
            <strong>后台准备商品与成交条件</strong>
            <p>标题、主图、详情、SKU、价格、营销、推广和持续优化。</p>
          </div>
        </div>
      </div>
    </div>

    <i class="qeo-stage-map__arrow">↓</i>

    <div class="qeo-stage-card is-cyan">
      <span class="qeo-stage-card__number">02</span>
      <div class="qeo-stage-card__panel">
        <div class="qeo-stage-card__user">
          <span>用户动作</span>
          <strong>用户等待并收到包裹</strong>
        </div>
        <div class="qeo-stage-card__backend">
          <span class="qeo-stage-card__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" role="img">
              <path d="m12 2 8 4.5v10L12 21l-8-4.5v-10L12 2Z" />
              <path d="m4.4 6.7 7.6 4.4 7.6-4.4M12 11.1V21" />
            </svg>
          </span>
          <span class="qeo-stage-card__label">后台对应</span>
          <div class="qeo-stage-card__copy">
            <strong>后台完成订单与履约</strong>
            <p>订单、ERP、仓库、物流和售后。</p>
          </div>
        </div>
      </div>
    </div>

    <i class="qeo-stage-map__arrow">↓</i>

    <div class="qeo-stage-card is-violet">
      <span class="qeo-stage-card__number">03</span>
      <div class="qeo-stage-card__panel">
        <div class="qeo-stage-card__user">
          <span>用户动作</span>
          <strong>用户咨询、退款或评价</strong>
        </div>
        <div class="qeo-stage-card__backend">
          <span class="qeo-stage-card__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" role="img">
              <path d="M4 5h12a4 4 0 0 1 4 4v3a4 4 0 0 1-4 4H9l-5 4v-4a4 4 0 0 1-2-3.5V9a4 4 0 0 1 2-4Z" />
              <path d="M7 10h.01M11 10h.01M15 10h.01" />
            </svg>
          </span>
          <span class="qeo-stage-card__label">后台对应</span>
          <div class="qeo-stage-card__copy">
            <strong>后台接收反馈并优化</strong>
            <p>商品、销售、推广和服务进入下一轮改进。</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .qeo-hero {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(34rem, 0.95fr);
    gap: 1.5rem;
    align-items: stretch;
    margin: 1.4rem 0 2rem;
    min-height: 27.5rem;
    padding: 1.55rem 1.75rem;
    box-sizing: border-box;
    border-radius: 30px;
    color: #e2e8f0;
    background:
      radial-gradient(circle at 4% 0%, rgba(14, 165, 233, 0.22), transparent 29%),
      radial-gradient(circle at 83% 5%, rgba(79, 70, 229, 0.28), transparent 30%),
      linear-gradient(128deg, #071a3f 0%, #10255a 48%, #17174f 100%);
    box-shadow: 0 26px 72px rgba(15, 23, 42, 0.24);
  }

  .qeo-hero__copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-width: 0;
    padding: 0.65rem 0 0.65rem 0.2rem;
  }

  .qeo-kicker {
    color: #55d3ff;
    font-size: 0.76rem;
    font-weight: 900;
    letter-spacing: 0.13em;
  }

  .qeo-hero h2 {
    max-width: 42rem;
    margin: 1rem 0 1.25rem;
    color: #f8fafc;
    font-size: clamp(1.85rem, 3vw, 2.2rem);
    line-height: 1.18;
    letter-spacing: -0.035em;
  }

  .qeo-hero p {
    margin: 0;
    color: #cbd5e1;
  }

  .qeo-hero__lead {
    max-width: 43rem;
    font-size: 1rem;
    line-height: 1.85;
  }

  .qeo-hero__lead strong {
    color: #43c8f5;
    font-weight: 900;
  }

  .qeo-hero__lead span {
    color: #d8e1f1;
  }

  .qeo-hero__verdict {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 0.9rem;
    align-items: center;
    max-width: 43rem;
    margin-top: 2rem;
    padding: 1rem 1.1rem;
    border: 1px solid rgba(96, 165, 250, 0.26);
    border-radius: 20px;
    background: rgba(5, 19, 52, 0.56);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.03);
  }

  .qeo-hero__verdict-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 3rem;
    height: 3rem;
    border-radius: 50%;
    color: #7dd3fc;
    background: linear-gradient(145deg, rgba(59, 130, 246, 0.42), rgba(14, 116, 144, 0.3));
    box-shadow: 0 0 24px rgba(56, 189, 248, 0.22);
  }

  .qeo-hero__verdict-icon svg {
    width: 1.6rem;
    height: 1.6rem;
    fill: none;
    stroke: currentColor;
    stroke-width: 1.65;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .qeo-hero__verdict-copy,
  .qeo-hero__verdict-copy > span,
  .qeo-hero__verdict-copy > strong {
    display: block;
  }

  .qeo-hero__verdict-copy > span {
    color: #45c7f4;
    font-size: 0.82rem;
    font-weight: 900;
  }

  .qeo-hero__verdict-copy > strong {
    margin-top: 0.3rem;
    color: #f8fafc;
    font-size: 0.96rem;
    line-height: 1.55;
  }

  .qeo-stage-map {
    display: grid;
    align-content: center;
    gap: 0.35rem;
    min-width: 0;
    padding: 1rem 1rem 1rem 3.6rem;
    border: 1px solid rgba(148, 163, 184, 0.22);
    border-radius: 28px;
    background: linear-gradient(145deg, rgba(30, 41, 86, 0.66), rgba(45, 46, 104, 0.58));
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
  }

  .qeo-stage-map__head {
    display: flex;
    justify-content: space-between;
    gap: 0.75rem;
    margin: 0 0.2rem 0.45rem -2.5rem;
    color: #5cd4f8;
    font-size: 0.78rem;
    font-weight: 800;
  }

  .qeo-stage-map__head strong {
    color: #dfe7f5;
    font-size: 0.78rem;
  }

  .qeo-stage-map__arrow {
    margin-left: -2.5rem;
    color: #6cb9df;
    text-align: center;
    font-style: normal;
    font-size: 1.05rem;
    line-height: 1;
  }

  .qeo-stage-card {
    position: relative;
    display: grid;
    grid-template-columns: 1fr;
    align-items: center;
    min-width: 0;
  }

  .qeo-stage-card__number {
    position: absolute;
    top: 0.7rem;
    left: -3.25rem;
    z-index: 2;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.65rem;
    height: 2.65rem;
    border: 2px solid rgba(96, 165, 250, 0.65);
    border-radius: 50%;
    color: #f8fafc;
    background: linear-gradient(145deg, #2563eb, #1d4ed8);
    box-shadow: 0 8px 20px rgba(37, 99, 235, 0.32), inset 0 1px 4px rgba(255, 255, 255, 0.28);
    font-size: 0.95rem;
    font-weight: 900;
  }

  .qeo-stage-card.is-cyan .qeo-stage-card__number {
    border-color: rgba(34, 211, 238, 0.65);
    background: linear-gradient(145deg, #0891b2, #0e7490);
    box-shadow: 0 8px 20px rgba(8, 145, 178, 0.3), inset 0 1px 4px rgba(255, 255, 255, 0.28);
  }

  .qeo-stage-card.is-violet .qeo-stage-card__number {
    border-color: rgba(167, 139, 250, 0.72);
    background: linear-gradient(145deg, #7c3aed, #5b21b6);
    box-shadow: 0 8px 20px rgba(124, 58, 237, 0.3), inset 0 1px 4px rgba(255, 255, 255, 0.28);
  }

  .qeo-stage-card__panel {
    overflow: hidden;
    min-width: 0;
    border: 1px solid rgba(219, 234, 254, 0.76);
    border-radius: 20px;
    background: #f8fafc;
    box-shadow: 0 13px 28px rgba(2, 8, 23, 0.18);
  }

  .qeo-stage-card__user {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 1rem;
    align-items: center;
    min-width: 0;
    padding: 0.72rem 1rem;
    color: #fff;
    background: linear-gradient(110deg, #2563eb 0%, #1d4ed8 100%);
  }

  .qeo-stage-card.is-cyan .qeo-stage-card__user {
    background: linear-gradient(110deg, #0ea5b7 0%, #0891b2 100%);
  }

  .qeo-stage-card.is-violet .qeo-stage-card__user {
    background: linear-gradient(110deg, #7c3aed 0%, #5b21b6 100%);
  }

  .qeo-stage-card__user > span {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 5.4rem;
    padding: 0.32rem 0.8rem;
    border-radius: 999px;
    color: #2563eb;
    background: rgba(255, 255, 255, 0.96);
    font-size: 0.76rem;
    font-weight: 900;
    white-space: nowrap;
  }

  .qeo-stage-card.is-cyan .qeo-stage-card__user > span {
    color: #0e7490;
  }

  .qeo-stage-card.is-violet .qeo-stage-card__user > span {
    color: #6d28d9;
  }

  .qeo-stage-card__user > strong {
    min-width: 0;
    color: #fff;
    font-size: clamp(1.15rem, 1.85vw, 1.55rem);
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .qeo-stage-card__backend {
    display: grid;
    grid-template-columns: auto auto minmax(0, 1fr);
    gap: 0.7rem;
    align-items: center;
    min-width: 0;
    padding: 0.72rem 1rem 0.78rem;
    background: linear-gradient(180deg, #f8fafc, #eef2ff);
  }

  .qeo-stage-card__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.45rem;
    height: 2.45rem;
    border-radius: 12px;
    color: #2563eb;
    background: #dbeafe;
  }

  .qeo-stage-card.is-cyan .qeo-stage-card__icon {
    color: #0e7490;
    background: #cffafe;
  }

  .qeo-stage-card.is-violet .qeo-stage-card__icon {
    color: #7c3aed;
    background: #ede9fe;
  }

  .qeo-stage-card__icon svg {
    width: 1.55rem;
    height: 1.55rem;
    fill: none;
    stroke: currentColor;
    stroke-width: 1.7;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .qeo-stage-card__label {
    color: #2563eb;
    font-size: 0.88rem;
    font-weight: 900;
    white-space: nowrap;
  }

  .qeo-stage-card.is-cyan .qeo-stage-card__label {
    color: #0e7490;
  }

  .qeo-stage-card.is-violet .qeo-stage-card__label {
    color: #7c3aed;
  }

  .qeo-stage-card__copy,
  .qeo-stage-card__copy strong,
  .qeo-stage-card__copy p {
    display: block;
    min-width: 0;
    margin: 0;
  }

  .qeo-stage-card__copy strong {
    color: #0f172a;
    font-size: 0.98rem;
    line-height: 1.3;
  }

  .qeo-stage-card__copy p {
    margin-top: 0.18rem;
    color: #64748b;
    font-size: 0.78rem;
    line-height: 1.45;
  }

  @media (max-width: 980px) {
    .qeo-hero {
      grid-template-columns: 1fr;
      min-height: 0;
      gap: 1.4rem;
    }
  }

  @media (max-width: 700px) {
    .qeo-hero {
      padding: 0.9rem;
      border-radius: 21px;
    }

    .qeo-hero h2 {
      margin: 0.75rem 0 0.9rem;
      font-size: 1.8rem;
    }

    .qeo-hero__lead {
      font-size: 0.94rem;
      line-height: 1.75;
    }

    .qeo-hero__verdict {
      grid-template-columns: 1fr;
      margin-top: 1.2rem;
    }

    .qeo-hero__verdict-icon {
      width: 2.65rem;
      height: 2.65rem;
    }

    .qeo-stage-map {
      padding: 0.85rem 0.7rem 0.85rem 3rem;
      border-radius: 18px;
    }

    .qeo-stage-map__head {
      flex-direction: column;
      gap: 0.15rem;
      margin-left: -2rem;
    }

    .qeo-stage-map__arrow {
      margin-left: -2rem;
    }

    .qeo-stage-card__number {
      top: 0.75rem;
      left: -2.65rem;
      width: 2.2rem;
      height: 2.2rem;
      font-size: 0.78rem;
    }

    .qeo-stage-card__panel {
      border-radius: 16px;
    }

    .qeo-stage-card__user {
      grid-template-columns: 1fr;
      gap: 0.55rem;
      padding: 0.72rem 0.8rem;
    }

    .qeo-stage-card__user > span {
      justify-self: start;
      min-width: 0;
      font-size: 0.7rem;
    }

    .qeo-stage-card__user > strong {
      font-size: 1.15rem;
    }

    .qeo-stage-card__backend {
      grid-template-columns: auto 1fr;
      gap: 0.55rem 0.65rem;
      padding: 0.72rem 0.8rem;
    }

    .qeo-stage-card__copy {
      grid-column: 1 / -1;
    }

    .qeo-stage-card__copy strong {
      font-size: 0.92rem;
    }
  }
</style>
