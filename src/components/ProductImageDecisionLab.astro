<section class="product-image-overview" aria-label="AI 商品图总流程">
  <header class="overview-hero">
    <span class="eyebrow">PRODUCT IMAGE WORKFLOW</span>
    <h2>先把整套方案设计完，再进入图片生成</h2>
    <p>完整商品图任务从一个总入口开始。竞品分析默认执行，评价分析按资料决定是否使用，视觉规划在确认前完成，生成阶段只负责执行。</p>
  </header>

  <div class="pipeline" aria-label="商品图主流程">
    <article>
      <span>01</span>
      <strong>准备资料</strong>
      <small>商品事实、真实图片、竞品链接和可选用户反馈</small>
    </article>
    <i>→</i>
    <article>
      <span>02</span>
      <strong>专业分析</strong>
      <small>竞品分析默认执行，评价分析按资料决定是否使用</small>
    </article>
    <i>→</i>
    <article>
      <span>03</span>
      <strong>商品策略</strong>
      <small>卖点分级、图片任务和整套顺序</small>
    </article>
    <i>→</i>
    <article>
      <span>04</span>
      <strong>视觉规划</strong>
      <small>Art Direction、差异化、逐图设计和最终 Prompt</small>
    </article>
    <i>→</i>
    <article>
      <span>05</span>
      <strong>用户确认</strong>
      <small>确认策略、画面、输入、Prompt 和禁止内容</small>
    </article>
    <i>→</i>
    <article>
      <span>06</span>
      <strong>纯执行</strong>
      <small>拆单图任务、直接生成、分类保存、人工审查</small>
    </article>
  </div>

  <section class="roles" aria-label="四个 Skill 的分工">
    <article class="role-card lead-card">
      <div class="role-topline">
        <span>MAIN SKILL</span>
        <b>唯一入口</b>
      </div>
      <strong>ecommerce-product-image</strong>
      <h3>商品图负责人</h3>
      <p>负责 WHAT 和 WHY：检查资料、串联专业分析、决定卖点分级、图片任务、顺序和整套商品策略。</p>
      <div class="role-note">决定讲什么，以及为什么这样排</div>
    </article>

    <article class="role-card market-card">
      <div class="role-topline">
        <span>SPECIALIST</span>
        <b>默认执行</b>
      </div>
      <strong>competitor-analysis</strong>
      <h3>竞品分析</h3>
      <p>读取真实竞品页面，分析主图、详情图、OCR 文案、信息密度、空间表达和可借鉴机会。</p>
      <div class="role-note">提供市场证据，不直接拍板</div>
    </article>

    <article class="role-card review-card">
      <div class="role-topline">
        <span>OPTIONAL</span>
        <b>有资料才用</b>
      </div>
      <strong>review-analysis</strong>
      <h3>评价分析</h3>
      <p>从评价、差评和客服问题里找高频关注、误解和使用门槛，判断哪些值得带回图片方案。</p>
      <div class="role-note">没有评价资料就直接跳过</div>
    </article>

    <article class="role-card direction-card">
      <div class="role-topline">
        <span>VISUAL SKILL</span>
        <b>确认前完成</b>
      </div>
      <strong>image-art-direction</strong>
      <h3>视觉规划</h3>
      <p>负责 HOW：整套 Art Direction、同组差异化、逐图画面设计、输入组织和最终 Prompt。</p>
      <div class="role-note">设计在规划阶段完成，不留给生成时临场判断</div>
    </article>
  </section>

  <footer class="flow-rule">
    <span>关键边界</span>
    <strong>专业分析不直接决定最终图片，视觉规划不重做商品策略，生成阶段也不是第五个负责思考的 Skill。</strong>
  </footer>
</section>

<style>
  .product-image-overview {
    --blue: #2563eb;
    --cyan: #0891b2;
    --violet: #7c3aed;
    --green: #0f9f76;
    background:
      radial-gradient(circle at 10% 0%, rgba(56, 189, 248, 0.14), transparent 30%),
      radial-gradient(circle at 90% 5%, rgba(124, 58, 237, 0.12), transparent 30%),
      linear-gradient(145deg, rgba(255, 255, 255, 0.98), rgba(241, 247, 255, 0.95));
    border: 1px solid rgba(96, 165, 250, 0.24);
    border-radius: 24px;
    box-shadow: 0 26px 68px rgba(15, 23, 42, 0.09);
    container-name: product-image-overview;
    container-type: inline-size;
    margin: 32px 0;
    overflow: hidden;
  }

  .overview-hero,
  .roles,
  .flow-rule {
    padding-left: clamp(20px, 4vw, 38px);
    padding-right: clamp(20px, 4vw, 38px);
  }

  .overview-hero {
    padding-bottom: 30px;
    padding-top: 36px;
  }

  .eyebrow,
  .role-topline span {
    color: var(--blue);
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.14em;
  }

  .overview-hero h2 {
    color: var(--ink);
    font-size: clamp(31px, 5vw, 50px);
    letter-spacing: -0.045em;
    line-height: 1.12;
    margin: 9px 0 13px;
  }

  .overview-hero p {
    color: var(--muted);
    line-height: 1.75;
    margin: 0;
    max-width: 820px;
  }

  .pipeline {
    align-items: stretch;
    background: rgba(15, 23, 42, 0.97);
    display: grid;
    gap: 10px;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr);
    padding: 22px clamp(20px, 4vw, 38px);
  }

  .pipeline article {
    min-width: 0;
  }

  .pipeline article span {
    color: #67e8f9;
    display: block;
    font-size: 10px;
    font-weight: 900;
  }

  .pipeline article strong {
    color: #f8fafc;
    display: block;
    font-size: 15px;
    margin-top: 5px;
  }

  .pipeline article small {
    color: #94a3b8;
    display: block;
    line-height: 1.5;
    margin-top: 5px;
  }

  .pipeline > i {
    align-self: center;
    color: #475569;
    font-style: normal;
  }

  .roles {
    display: grid;
    gap: 16px;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    padding-bottom: 34px;
    padding-top: 34px;
  }

  .role-card {
    background: rgba(255, 255, 255, 0.8);
    border: 1px solid rgba(148, 163, 184, 0.23);
    border-radius: 18px;
    min-width: 0;
    overflow-wrap: anywhere;
    padding: 20px;
  }

  .lead-card {
    border-top: 4px solid var(--blue);
  }

  .market-card {
    border-top: 4px solid var(--cyan);
  }

  .review-card {
    border-top: 4px solid var(--violet);
  }

  .direction-card {
    border-top: 4px solid var(--green);
  }

  .role-topline {
    align-items: center;
    display: flex;
    gap: 10px;
    justify-content: space-between;
  }

  .role-topline b {
    background: rgba(37, 99, 235, 0.08);
    border-radius: 999px;
    color: var(--blue);
    font-size: 10px;
    padding: 6px 8px;
    white-space: nowrap;
  }

  .market-card .role-topline span,
  .market-card .role-topline b {
    color: var(--cyan);
  }

  .market-card .role-topline b {
    background: rgba(8, 145, 178, 0.08);
  }

  .review-card .role-topline span,
  .review-card .role-topline b {
    color: var(--violet);
  }

  .review-card .role-topline b {
    background: rgba(124, 58, 237, 0.08);
  }

  .direction-card .role-topline span,
  .direction-card .role-topline b {
    color: var(--green);
  }

  .direction-card .role-topline b {
    background: rgba(15, 159, 118, 0.08);
  }

  .role-card > strong {
    display: block;
    font-size: 14px;
    margin-top: 16px;
  }

  .role-card h3 {
    color: var(--ink);
    font-size: 21px;
    margin: 5px 0 9px;
  }

  .role-card p {
    color: var(--muted);
    line-height: 1.65;
    margin: 0;
  }

  .role-note {
    border-top: 1px dashed rgba(148, 163, 184, 0.3);
    color: var(--soft);
    font-size: 11px;
    margin-top: 15px;
    padding-top: 12px;
  }

  .flow-rule {
    align-items: center;
    background: linear-gradient(110deg, rgba(37, 99, 235, 0.96), rgba(14, 116, 144, 0.94));
    display: grid;
    gap: 12px;
    grid-template-columns: auto minmax(0, 1fr);
    padding-bottom: 20px;
    padding-top: 20px;
  }

  .flow-rule span {
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 999px;
    color: white;
    font-size: 10px;
    font-weight: 900;
    padding: 7px 9px;
    white-space: nowrap;
  }

  .flow-rule strong {
    color: white;
    font-size: 15px;
    line-height: 1.55;
  }

  @container product-image-overview (max-width: 900px) {
    .pipeline {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .pipeline > i {
      display: none;
    }
  }

  @container product-image-overview (max-width: 620px) {
    .product-image-overview {
      border-radius: 20px;
      margin-left: -8px;
      margin-right: -8px;
    }

    .overview-hero,
    .roles,
    .flow-rule {
      padding-left: 18px;
      padding-right: 18px;
    }

    .overview-hero h2 {
      font-size: 34px;
    }

    .pipeline,
    .roles {
      grid-template-columns: 1fr;
    }

    .pipeline {
      gap: 18px;
      padding-left: 18px;
      padding-right: 18px;
    }

    .flow-rule {
      grid-template-columns: 1fr;
    }

    .flow-rule span {
      justify-self: start;
    }
  }
</style>
