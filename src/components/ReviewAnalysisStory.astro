<section class="review-story" data-review-story>
  <nav class="story-rail" aria-label="文章章节">
    <a class="rail-brand" href="#review-overview" aria-label="返回文章开头">
      <span>REVIEW</span>
      <strong>AGENT</strong>
    </a>
    <div class="rail-links">
      <a href="#review-problem" data-story-link>01 问题</a>
      <a href="#review-data" data-story-link>02 数据</a>
      <a href="#review-fact" data-story-link>03 事实包</a>
      <a href="#review-run" data-story-link>04 执行</a>
      <a href="#review-gate" data-story-link>05 闸门</a>
      <a href="#review-modes" data-story-link>06 模式</a>
    </div>
    <div class="rail-progress" aria-hidden="true"><span data-story-progress></span></div>
  </nav>

  <header class="story-hero" id="review-overview" data-story-section>
    <div class="hero-grid-lines" aria-hidden="true"></div>
    <div class="hero-copy">
      <p class="story-kicker"><span>OPENCLAW PROJECT</span> / 电商评价分析实践</p>
      <h1>我把散落的评价，<br /><em>做成了一个会完成闭环的 Agent。</em></h1>
      <p class="hero-lead">
        它不是读取几条差评后写一段总结，而是把两张钉钉 AI 表格统一成事实包，生成日报或周期对比，发布文档、发送到群，并且只在消息真正送达后回写状态。
      </p>
      <div class="hero-actions">
        <a class="primary-action" href="#review-problem">从系统问题开始</a>
        <a class="text-action" href="https://github.com/apexcheng/review-analysis-reporter-skill" target="_blank" rel="noreferrer">查看 GitHub 项目 ↗</a>
      </div>
    </div>

    <div class="hero-dashboard" aria-label="系统概览">
      <div class="dashboard-head">
        <span>RUN / REVIEW-ANALYST</span>
        <b><i></i> ONLINE</b>
      </div>
      <div class="dashboard-main">
        <div class="hero-number">06</div>
        <div>
          <strong>个关键阶段</strong>
          <p>读取、统一、生成、表达、发送、回写。</p>
        </div>
      </div>
      <div class="metric-strip">
        <article><span>DATA SOURCE</span><strong>2 张表</strong></article>
        <article><span>SOURCE OF TRUTH</span><strong>1 个事实包</strong></article>
        <article><span>REPORT MODE</span><strong>2 种模式</strong></article>
        <article><span>WRITEBACK GATE</span><strong>1 道闸门</strong></article>
      </div>
      <div class="dashboard-log" aria-label="运行日志示意">
        <p><span>01</span> source.normal <b>ready</b></p>
        <p><span>02</span> source.append <b>ready</b></p>
        <p><span>03</span> fact.package <b>verified</b></p>
        <p><span>04</span> delivery.gate <b class="warning">waiting</b></p>
      </div>
    </div>

    <div class="hero-pipeline" aria-label="评价分析主链路">
      <span>评价进入</span><i>→</i><span>统一口径</span><i>→</i><span>事实包</span><i>→</i><span>报告</span><i>→</i><span>发送</span><i>→</i><span class="accent">成功后回写</span>
    </div>

    <p class="privacy-note">公开版本已去掉真实店铺、订单号、评价原文、手机号、Webhook、表格地址和内部配置。</p>
  </header>

  <main>
    <section class="story-chapter problem-chapter" id="review-problem" data-story-section>
      <div class="chapter-index" aria-hidden="true">01</div>
      <div class="chapter-intro">
        <p class="chapter-label">THE REAL PROBLEM</p>
        <h2>它不是一个<br />“评价总结 Prompt”</h2>
        <p>一句“读取今天的评价并发到群里”，背后其实藏着数据口径、任务模式、外部发送和状态副作用。</p>
      </div>

      <div class="problem-layout">
        <blockquote>
          <span>看起来的需求</span>
          “读取今天的评价，分析有哪些问题，然后发到群里。”
        </blockquote>
        <div class="problem-list">
          <article><b>01</b><strong>来源不同</strong><p>首次评价和追加评价在两张表里，字段和时间口径都不同。</p></article>
          <article><b>02</b><strong>结论不能重判</strong><p>情绪、问题类型和责任归因已经由上游字段给出，Agent 不能凭文案重新分类。</p></article>
          <article><b>03</b><strong>任务不是一种</strong><p>日报只处理尚未通知的记录，周期对比则必须忽略通知状态。</p></article>
          <article><b>04</b><strong>发送会失败</strong><p>群消息未送达时，绝不能提前把评价标记为“已通知”。</p></article>
          <article><b>05</b><strong>回写可能改错表</strong><p>两张表的 recordId 必须按来源隔离，不能混在一起更新。</p></article>
        </div>
      </div>

      <div class="three-layer">
        <article>
          <span>PYTHON / FACT</span>
          <h3>事实层</h3>
          <p>找表、读字段、筛选、去重、统计，生成可验证 JSON。</p>
          <b>确定什么是真的</b>
        </article>
        <article>
          <span>AGENT / LANGUAGE</span>
          <h3>表达层</h3>
          <p>只基于事实包组织 Markdown，让运营快速看懂重点。</p>
          <b>决定怎么说清楚</b>
        </article>
        <article>
          <span>DELIVERY / ACTION</span>
          <h3>交付层</h3>
          <p>报告落盘、在线文档、群消息，以及发送成功后的状态回写。</p>
          <b>控制副作用何时发生</b>
        </article>
      </div>
    </section>

    <section class="story-chapter data-chapter" id="review-data" data-story-section>
      <div class="chapter-index" aria-hidden="true">02</div>
      <div class="chapter-intro">
        <p class="chapter-label">NORMALIZE THE DATA</p>
        <h2>两张表，<br />先变成一种评价。</h2>
        <p>Agent 不应该理解两套混乱字段。脚本先把不同来源翻译成同一套内部语言。</p>
      </div>

      <div class="source-merge" aria-label="两张评价表标准化">
        <article class="source-card normal-source">
          <span>SOURCE A</span>
          <h3>评价收集明细</h3>
          <dl><div><dt>内容</dt><dd>评价内容</dd></div><div><dt>统计时间</dt><dd>评价时间</dd></div><div><dt>来源标记</dt><dd>首次评价</dd></div></dl>
        </article>
        <div class="merge-core" aria-hidden="true"><span>STANDARDIZE</span><b>→</b></div>
        <article class="source-card append-source">
          <span>SOURCE B</span>
          <h3>追加评价收集明细</h3>
          <dl><div><dt>内容</dt><dd>追加评价内容</dd></div><div><dt>统计时间</dt><dd>首次评价时间</dd></div><div><dt>参考时间</dt><dd>追加评价时间</dd></div></dl>
        </article>
      </div>

      <div class="schema-board">
        <div class="schema-title"><span>UNIFIED REVIEW SCHEMA</span><strong>统一后的内部字段</strong></div>
        <div class="schema-lines">
          <p><code>review_source</code><span>评价来自哪张表</span></p>
          <p><code>review_time</code><span>实际参与筛选和统计的时间</span></p>
          <p><code>first_review_time</code><span>首次评价时间</span></p>
          <p><code>append_review_time</code><span>追加评价时间，仅供参考</span></p>
          <p><code>评价内容</code><span>两张表统一后的文本字段</span></p>
        </div>
        <div class="dedupe-rule"><span>DEDUPE KEY</span><strong>订单号 + 商品ID + 评价内容</strong><p>只用于避免重复统计，不判断哪条记录更新，也不删除源表数据。</p></div>
      </div>
    </section>

    <section class="story-chapter fact-chapter" id="review-fact" data-story-section>
      <div class="chapter-index" aria-hidden="true">03</div>
      <div class="chapter-intro light-intro">
        <p class="chapter-label">SOURCE OF TRUTH</p>
        <h2>报告写得再好，<br />也不能越过事实包。</h2>
        <p>事实包是 Python 与 Agent 之间唯一允许通过的业务边界。</p>
      </div>

      <div class="fact-terminal">
        <div class="terminal-head"><span></span><span></span><span></span><b>review_context.json</b></div>
        <pre><code><i>REVIEW_REPORT_CONTEXT_JSON_START</i>
&#123;
  "mode": "daily",
  "stats": &#123; "total": 128, "followup": 11 &#125;,
  "source_stats": &#123; "normal": 103, "append": 25 &#125;,
  "records": [ "..." ],
  "writeback_record_ids": &#123; "sources": &#123; "..." &#125; &#125;
&#125;
<i>REVIEW_REPORT_CONTEXT_JSON_END</i></code></pre>
      </div>

      <div class="fact-guards">
        <article><span>GUARD 01</span><h3>日志不能混进事实</h3><p>Agent 只解析两个标记之间的 JSON，stderr、调试信息和工具记录全部被挡在外面。</p></article>
        <article><span>GUARD 02</span><h3>模型不能补原因</h3><p>不存在于事实包里的订单、批次、质检和供应链信息，不能为了“完整”而补写。</p></article>
        <article><span>GUARD 03</span><h3>统计与表达分开</h3><p>Python 完成数量、占比、排行和明细；Agent 只负责把这些事实组织清楚。</p></article>
      </div>

      <aside class="fact-rule"><span>不可越过的规则</span><strong>情绪倾向、问题类型和责任归因都服从表格已有字段。Agent 可以概括关键词，但不能重新分类。</strong></aside>
    </section>

    <section class="story-chapter run-chapter" id="review-run" data-story-section>
      <div class="chapter-index" aria-hidden="true">04</div>
      <div class="chapter-intro">
        <p class="chapter-label">DAILY RUN</p>
        <h2>一次日报，<br />到底怎么跑完？</h2>
        <p>每一步都必须写清输入、产出和失败边界，而不是让 Agent 在一个长 Prompt 里自由行动。</p>
      </div>

      <div class="run-track">
        <article><span>01</span><div><b>PYTHON</b><h3>定位两张表并读取字段</h3><p>分别解析首次评价和追加评价字段映射，确认当前模式所需字段。</p></div><em>输出：字段映射</em></article>
        <article><span>02</span><div><b>QUERY</b><h3>筛选本次待处理评价</h3><p>日报只读取通知状态不为“已通知 / 无需通知”的记录，最大回扫 30 天。</p></div><em>输出：候选记录</em></article>
        <article><span>03</span><div><b>FACT</b><h3>标准化、去重、生成事实包</h3><p>统一来源和时间字段，排除 unknown，生成统计、明细与分来源回写 ids。</p></div><em>输出：唯一事实源</em></article>
        <article><span>04</span><div><b>AGENT</b><h3>生成并保存最终报告</h3><p>只基于事实包生成 Markdown，再通过报告写入脚本保存到 report 目录。</p></div><em>输出：可追踪报告</em></article>
        <article class="critical-step"><span>05</span><div><b>DELIVERY</b><h3>发布文档、发送消息、成功后回写</h3><p>先确认报告已经送达，再按本次事实包里的来源分组 record ids 修改通知状态。</p></div><em>闸门：发送结果</em></article>
      </div>

      <div class="sentiment-rule">
        <article><span>非正面评价</span><strong>中性 + 负面</strong><p>进入日报关注范围与状态回写。</p></article>
        <article><span>重点差评</span><strong>仅负面</strong><p>中性评价不会混进重点差评内容。</p></article>
        <article><span>unknown</span><strong>暂不处理</strong><p>不统计、不回写，保留原状态等待上游分析。</p></article>
      </div>
    </section>

    <section class="story-chapter gate-chapter" id="review-gate" data-story-section>
      <div class="chapter-index" aria-hidden="true">05</div>
      <div class="chapter-intro light-intro">
        <p class="chapter-label">DELIVERY GATE</p>
        <h2>发送成功，<br />才允许回写。</h2>
        <p>这是整套系统最重要的可靠性设计，也是一次性 AI 总结与长期业务系统之间的分界线。</p>
      </div>

      <div class="gate-flow">
        <article class="gate-start"><span>REPORT READY</span><strong>报告已生成</strong><p>文档和群消息准备发送。</p></article>
        <div class="gate-question"><span>?</span><strong>Webhook 是否成功</strong></div>
        <div class="gate-branches">
          <article class="branch-success"><span>YES / DELIVERED</span><h3>按来源表回写</h3><p>只使用本次事实包中的 record ids：非正面写“已通知”，正面写“无需通知”。</p><b>任务完成</b></article>
          <article class="branch-fail"><span>NO / FAILED</span><h3>保持原状态</h3><p>不执行任何业务状态回写，让这些评价下一次仍能被查询和重新发送。</p><b>等待重试</b></article>
        </div>
      </div>

      <div class="writeback-only">
        <span>WRITEBACK-ONLY MODE</span>
        <h3>回写阶段故意变得很窄</h3>
        <ul>
          <li>只能使用本次事实包生成的 record ids</li>
          <li>禁止重新查询评价记录</li>
          <li>禁止重新生成一份 daily 事实包</li>
          <li>必须按来源表分组，不能混用两张表的 recordId</li>
        </ul>
        <p>如果报告已发送但回写失败，系统会追加风险提示：下次日报可能重复处理这批评价。</p>
      </div>
    </section>

    <section class="story-chapter modes-chapter" id="review-modes" data-story-section>
      <div class="chapter-index" aria-hidden="true">06</div>
      <div class="chapter-intro">
        <p class="chapter-label">TWO OPERATING MODES</p>
        <h2>日报和周期对比，<br />不是同一个任务。</h2>
        <p>它们共用数据标准化和事实包机制，但筛选口径、报告结构和副作用边界保持独立。</p>
      </div>

      <div class="mode-compare">
        <article class="daily-mode">
          <div class="mode-head"><span>MODE 01</span><strong>DAILY</strong></div>
          <h3>今天有哪些新评价需要关注？</h3>
          <ul><li>只处理尚未通知的记录</li><li>最大回扫 30 天</li><li>发送成功后回写状态</li><li>不展示商品或链接集中度</li></ul>
          <b>有业务副作用</b>
        </article>
        <article class="compare-mode">
          <div class="mode-head"><span>MODE 02</span><strong>PERIOD_COMPARE</strong></div>
          <h3>两个时间段之间发生了什么变化？</h3>
          <ul><li>必须提供两个完整日期区间</li><li>不受每日通知状态影响</li><li>分析商品、链接和问题趋势</li><li>不回写每日通知状态</li></ul>
          <b>纯分析，无状态回写</b>
        </article>
      </div>

      <div class="boundary-board">
        <div class="boundary-title"><span>AGENT BOUNDARIES</span><h3>我故意不让 Agent 做的事</h3></div>
        <div class="boundary-grid">
          <article><b>01</b><strong>不重判情绪</strong><p>避免覆盖上游口径，让同一条评价在不同报告里保持一致。</p></article>
          <article><b>02</b><strong>不混淆中性与重点差评</strong><p>关注口径与重点差评口径保持分离。</p></article>
          <article><b>03</b><strong>不使用事实包外解释</strong><p>推测不能伪装成订单、批次或质检事实。</p></article>
          <article><b>04</b><strong>发送失败不回写</strong><p>未送达的评价不能从后续日报中静默消失。</p></article>
          <article><b>05</b><strong>回写时不重新查询</strong><p>保证当前报告和更新对象仍是同一批数据。</p></article>
          <article><b>06</b><strong>不混用 recordId</strong><p>每个 id 只能回到自己的来源表中更新。</p></article>
        </div>
      </div>
    </section>

    <footer class="story-closing">
      <p class="chapter-label">THE ACTUAL VALUE</p>
      <h2>评价分析 Agent 的价值，<br /><em>不是会写报告。</em></h2>
      <p>真正困难的是数据口径、事实边界、发送结果、状态回写和失败兜底。只有这些环节连起来，评价分析才从一次性的 AI 总结，变成可以长期运行的业务系统。</p>
      <div class="closing-pipeline"><span>评价进入</span><i>→</i><span>稳定送达</span><i>→</i><span>状态可追踪</span><i>→</i><strong>下一次只处理新数据</strong></div>
      <a href="https://github.com/apexcheng/review-analysis-reporter-skill" target="_blank" rel="noreferrer">查看 review-analysis-reporter-skill 项目 <span>↗</span></a>
    </footer>
  </main>
</section>

<script is:inline>
  const story = document.querySelector('[data-review-story]');
  const progress = story?.querySelector('[data-story-progress]');
  const sections = story ? Array.from(story.querySelectorAll('[data-story-section]')) : [];
  const links = story ? Array.from(story.querySelectorAll('[data-story-link]')) : [];

  const updateProgress = () => {
    if (!story || !progress) return;
    const rect = story.getBoundingClientRect();
    const distance = Math.max(story.offsetHeight - window.innerHeight, 1);
    const value = Math.min(Math.max(-rect.top / distance, 0), 1);
    progress.style.transform = `scaleX(${value})`;
  };

  updateProgress();
  window.addEventListener('scroll', updateProgress, { passive: true });

  if ('IntersectionObserver' in window) {
    const observer = new IntersectionObserver((entries) => {
      const active = entries
        .filter((entry) => entry.isIntersecting)
        .sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];
      if (!active) return;
      links.forEach((link) => link.classList.toggle('is-active', link.getAttribute('href') === `#${active.target.id}`));
    }, { rootMargin: '-20% 0px -62% 0px', threshold: [0.05, 0.2, 0.5] });
    sections.forEach((section) => observer.observe(section));
  }
</script>

<style>
  :global(.page-content.article-shell:has(.review-story)) { display: block; padding: 0; }
  :global(.article-shell:has(.review-story) .article-sidebar),
  :global(.article-content:has(> .review-story) > .article-sidebar-restore),
  :global(.article-content:has(> .review-story) > .pill),
  :global(.article-content:has(> .review-story) > h1),
  :global(.article-content:has(> .review-story) > .article-description),
  :global(.article-content:has(> .review-story) > .meta-row) { display: none; }
  :global(.article-content:has(> .review-story)) { overflow: visible; padding: 0; }

  .review-story {
    --story-blue: #1e40af;
    --story-blue-bright: #3b82f6;
    --story-amber: #f59e0b;
    --story-ink: #07111f;
    --story-paper: #f8fafc;
    --story-line: #cbd5e1;
    background: var(--story-paper);
    color: var(--story-ink);
    font-family: "Fira Sans", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif;
    margin: 0 calc(50% - 50vw);
    overflow: clip;
    position: relative;
    width: 100vw;
  }

  .review-story * { box-sizing: border-box; }
  .review-story h1, .review-story h2, .review-story h3, .review-story p { margin-top: 0; }
  .review-story a { background: none; text-decoration: none; }

  .story-rail {
    align-items: center;
    backdrop-filter: blur(18px);
    background: rgba(248, 250, 252, 0.88);
    border-bottom: 1px solid rgba(148, 163, 184, 0.3);
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    min-height: 58px;
    padding: 0 clamp(20px, 5vw, 84px);
    position: sticky;
    top: 0;
    z-index: 20;
  }

  .rail-brand { color: var(--story-ink); display: flex; flex-direction: column; font-family: ui-monospace, monospace; line-height: 1; }
  .rail-brand span { color: var(--story-blue-bright); font-size: 9px; letter-spacing: .18em; }
  .rail-brand strong { font-size: 13px; letter-spacing: .08em; margin-top: 4px; }
  .rail-links { display: flex; gap: 4px; justify-content: flex-end; }
  .rail-links a { border-radius: 999px; color: #64748b; font-family: ui-monospace, monospace; font-size: 10px; padding: 8px 10px; transition: background .2s ease, color .2s ease; }
  .rail-links a:hover, .rail-links a:focus-visible, .rail-links a.is-active { background: #dbeafe; color: var(--story-blue); }
  .rail-progress { background: #dbe2ea; bottom: -1px; height: 2px; left: 0; overflow: hidden; position: absolute; right: 0; }
  .rail-progress span { background: linear-gradient(90deg, var(--story-blue-bright), var(--story-amber)); display: block; height: 100%; transform: scaleX(0); transform-origin: left; }

  .story-hero { background: #050b14; color: white; min-height: calc(100vh - 58px); overflow: hidden; padding: clamp(72px, 10vw, 140px) clamp(24px, 7vw, 112px) 46px; position: relative; }
  .hero-grid-lines { background-image: linear-gradient(rgba(59,130,246,.09) 1px, transparent 1px), linear-gradient(90deg, rgba(59,130,246,.09) 1px, transparent 1px); background-size: 64px 64px; inset: 0; mask-image: linear-gradient(to bottom, black, transparent 88%); position: absolute; }
  .story-hero::before { background: radial-gradient(circle, rgba(59,130,246,.35), transparent 68%); content: ""; height: 680px; position: absolute; right: -260px; top: -300px; width: 680px; }
  .story-hero::after { color: rgba(255,255,255,.025); content: "DATA"; font-family: ui-monospace, monospace; font-size: clamp(180px, 30vw, 520px); font-weight: 900; left: -40px; line-height: .8; position: absolute; top: 28%; }
  .hero-copy, .hero-dashboard, .hero-pipeline, .privacy-note { position: relative; z-index: 2; }
  .hero-copy { max-width: 980px; }
  .story-kicker, .chapter-label { color: #60a5fa; font-family: ui-monospace, monospace; font-size: 11px; font-weight: 700; letter-spacing: .14em; }
  .story-kicker span { background: #1d4ed8; color: white; margin-right: 8px; padding: 6px 8px; }
  .hero-copy h1 { color: white; font-size: clamp(48px, 8vw, 116px); font-weight: 820; letter-spacing: -.07em; line-height: .94; margin: 32px 0; max-width: 1200px; }
  .hero-copy h1 em { color: #93c5fd; font-style: normal; }
  .hero-lead { color: #b7c4d6; font-size: clamp(17px, 2vw, 23px); line-height: 1.7; max-width: 820px; }
  .hero-actions { align-items: center; display: flex; flex-wrap: wrap; gap: 18px; margin-top: 36px; }
  .primary-action { background: var(--story-amber) !important; border-radius: 4px; color: #271500 !important; font-weight: 800; padding: 14px 18px; }
  .text-action { color: #bfdbfe !important; font-weight: 700; }

  .hero-dashboard { background: rgba(15,23,42,.78); border: 1px solid rgba(96,165,250,.25); box-shadow: 0 30px 100px rgba(0,0,0,.38); margin: clamp(58px, 8vw, 104px) 0 0 auto; max-width: 920px; }
  .dashboard-head { align-items: center; border-bottom: 1px solid rgba(148,163,184,.18); display: flex; justify-content: space-between; padding: 14px 18px; }
  .dashboard-head span, .dashboard-head b { color: #94a3b8; font-family: ui-monospace, monospace; font-size: 10px; letter-spacing: .1em; }
  .dashboard-head b { color: #86efac; }
  .dashboard-head i { background: #22c55e; border-radius: 50%; display: inline-block; height: 7px; margin-right: 6px; width: 7px; }
  .dashboard-main { align-items: center; display: flex; gap: 24px; padding: 24px; }
  .hero-number { color: white; font-family: ui-monospace, monospace; font-size: clamp(70px, 10vw, 132px); font-weight: 800; letter-spacing: -.08em; line-height: .8; }
  .dashboard-main strong { color: white; font-size: 24px; }
  .dashboard-main p { color: #94a3b8; margin: 6px 0 0; }
  .metric-strip { border-bottom: 1px solid rgba(148,163,184,.18); border-top: 1px solid rgba(148,163,184,.18); display: grid; grid-template-columns: repeat(4, 1fr); }
  .metric-strip article { border-right: 1px solid rgba(148,163,184,.18); padding: 18px; }
  .metric-strip article:last-child { border-right: 0; }
  .metric-strip span { color: #64748b; display: block; font-family: ui-monospace, monospace; font-size: 9px; letter-spacing: .08em; }
  .metric-strip strong { color: #dbeafe; display: block; font-size: 18px; margin-top: 7px; }
  .dashboard-log { padding: 14px 18px; }
  .dashboard-log p { align-items: center; color: #64748b; display: grid; font-family: ui-monospace, monospace; font-size: 11px; grid-template-columns: 30px 1fr auto; margin: 0; padding: 6px 0; }
  .dashboard-log b { color: #86efac; font-weight: 500; }
  .dashboard-log b.warning { color: #fbbf24; }
  .hero-pipeline { align-items: center; display: flex; flex-wrap: wrap; gap: 10px; margin-top: 58px; }
  .hero-pipeline span { border: 1px solid rgba(148,163,184,.26); color: #cbd5e1; font-family: ui-monospace, monospace; font-size: 10px; padding: 8px 10px; }
  .hero-pipeline span.accent { border-color: rgba(245,158,11,.5); color: #fbbf24; }
  .hero-pipeline i { color: #475569; font-style: normal; }
  .privacy-note { color: #64748b; font-size: 11px; margin: 24px 0 0; }

  .story-chapter { padding: clamp(88px, 12vw, 170px) clamp(24px, 7vw, 112px); position: relative; }
  .chapter-index { color: rgba(30,64,175,.07); font-family: ui-monospace, monospace; font-size: clamp(170px, 28vw, 420px); font-weight: 900; line-height: .8; position: absolute; right: 2vw; top: 70px; }
  .chapter-intro { max-width: 900px; position: relative; z-index: 2; }
  .chapter-intro h2, .story-closing h2 { color: var(--story-ink); font-size: clamp(48px, 7vw, 104px); font-weight: 850; letter-spacing: -.065em; line-height: .96; margin: 18px 0 28px; }
  .chapter-intro > p:last-child { color: #475569; font-size: clamp(17px, 2vw, 22px); line-height: 1.7; max-width: 720px; }

  .problem-layout { display: grid; gap: 32px; grid-template-columns: minmax(260px, .75fr) minmax(0, 1.25fr); margin-top: 72px; }
  .problem-layout blockquote { align-self: start; background: var(--story-blue); color: white; font-size: clamp(24px, 3vw, 38px); font-weight: 760; line-height: 1.35; margin: 0; padding: 36px; position: sticky; top: 90px; }
  .problem-layout blockquote span { color: #bfdbfe; display: block; font-family: ui-monospace, monospace; font-size: 10px; letter-spacing: .12em; margin-bottom: 20px; }
  .problem-list article { align-items: start; border-top: 1px solid var(--story-line); display: grid; gap: 18px; grid-template-columns: 48px 180px 1fr; padding: 25px 0; }
  .problem-list b, .boundary-grid b { color: var(--story-blue-bright); font-family: ui-monospace, monospace; }
  .problem-list strong { font-size: 18px; }
  .problem-list p { color: #64748b; line-height: 1.7; margin: 0; }
  .three-layer { display: grid; gap: 1px; grid-template-columns: repeat(3, 1fr); margin-top: 92px; background: #94a3b8; border: 1px solid #94a3b8; }
  .three-layer article { background: white; min-height: 300px; padding: 34px; }
  .three-layer span, .source-card > span, .fact-guards span, .run-track b, .gate-flow span, .mode-head span, .boundary-title span { color: var(--story-blue); font-family: ui-monospace, monospace; font-size: 10px; letter-spacing: .12em; }
  .three-layer h3 { font-size: 32px; margin: 42px 0 14px; }
  .three-layer p { color: #64748b; line-height: 1.7; }
  .three-layer b { display: block; margin-top: 48px; }

  .data-chapter { background: #edf4ff; }
  .source-merge { align-items: stretch; display: grid; gap: 18px; grid-template-columns: 1fr 120px 1fr; margin-top: 72px; }
  .source-card { background: white; border-top: 5px solid var(--story-blue-bright); padding: 32px; }
  .append-source { border-color: var(--story-amber); }
  .source-card h3 { font-size: clamp(25px, 3vw, 38px); margin: 24px 0 32px; }
  .source-card dl { margin: 0; }
  .source-card dl div { border-top: 1px solid #dbe3ef; display: flex; justify-content: space-between; padding: 13px 0; }
  .source-card dt { color: #64748b; }
  .source-card dd { font-family: ui-monospace, monospace; font-weight: 700; margin: 0; }
  .merge-core { align-items: center; display: flex; flex-direction: column; justify-content: center; }
  .merge-core span { color: var(--story-blue); font-family: ui-monospace, monospace; font-size: 9px; writing-mode: vertical-rl; }
  .merge-core b { color: var(--story-blue-bright); font-size: 42px; margin-top: 18px; }
  .schema-board { background: #07111f; color: white; margin-top: 30px; padding: clamp(28px, 5vw, 58px); }
  .schema-title span { color: #60a5fa; font-family: ui-monospace, monospace; font-size: 10px; letter-spacing: .12em; }
  .schema-title strong { display: block; font-size: 30px; margin-top: 8px; }
  .schema-lines { margin-top: 32px; }
  .schema-lines p { border-top: 1px solid rgba(148,163,184,.18); display: grid; gap: 20px; grid-template-columns: minmax(180px, .7fr) 1.3fr; margin: 0; padding: 15px 0; }
  .schema-lines code { color: #93c5fd; font-size: 13px; }
  .schema-lines span { color: #94a3b8; }
  .dedupe-rule { border: 1px solid rgba(245,158,11,.36); margin-top: 30px; padding: 22px; }
  .dedupe-rule span { color: #fbbf24; font-family: ui-monospace, monospace; font-size: 10px; }
  .dedupe-rule strong { display: block; font-family: ui-monospace, monospace; font-size: clamp(18px, 3vw, 30px); margin-top: 12px; }
  .dedupe-rule p { color: #94a3b8; margin: 10px 0 0; }

  .fact-chapter, .gate-chapter { background: #050b14; color: white; }
  .fact-chapter .chapter-index, .gate-chapter .chapter-index { color: rgba(96,165,250,.08); }
  .light-intro h2 { color: white; }
  .light-intro > p:last-child { color: #94a3b8; }
  .fact-terminal { background: #0d1726; border: 1px solid rgba(96,165,250,.25); margin-top: 70px; max-width: 940px; }
  .terminal-head { align-items: center; border-bottom: 1px solid rgba(148,163,184,.18); display: flex; gap: 7px; padding: 12px 16px; }
  .terminal-head span { background: #334155; border-radius: 50%; height: 9px; width: 9px; }
  .terminal-head b { color: #64748b; font-family: ui-monospace, monospace; font-size: 10px; margin-left: 8px; }
  .fact-terminal pre { background: transparent; border: 0; margin: 0; overflow-x: auto; padding: clamp(22px, 4vw, 42px); }
  .fact-terminal code { color: #cbd5e1; font-family: ui-monospace, monospace; font-size: clamp(12px, 1.5vw, 15px); line-height: 1.8; }
  .fact-terminal i { color: #60a5fa; font-style: normal; }
  .fact-guards { display: grid; gap: 20px; grid-template-columns: repeat(3, 1fr); margin-top: 30px; }
  .fact-guards article { border-top: 1px solid #334155; padding: 28px 4px; }
  .fact-guards h3 { color: white; font-size: 23px; margin: 20px 0 12px; }
  .fact-guards p { color: #94a3b8; line-height: 1.7; }
  .fact-rule { align-items: start; background: var(--story-blue); display: grid; gap: 28px; grid-template-columns: 180px 1fr; margin-top: 72px; padding: 30px; }
  .fact-rule span { color: #bfdbfe; font-family: ui-monospace, monospace; font-size: 10px; letter-spacing: .12em; }
  .fact-rule strong { color: white; font-size: clamp(18px, 2.5vw, 28px); line-height: 1.5; }

  .run-track { margin-top: 76px; }
  .run-track article { align-items: center; border-top: 1px solid var(--story-line); display: grid; gap: 28px; grid-template-columns: 82px minmax(0, 1fr) 190px; min-height: 150px; padding: 24px 0; }
  .run-track > article > span { color: #cbd5e1; font-family: ui-monospace, monospace; font-size: 44px; font-weight: 800; }
  .run-track h3 { font-size: clamp(21px, 3vw, 33px); margin: 8px 0 10px; }
  .run-track p { color: #64748b; line-height: 1.65; margin: 0; }
  .run-track em { border: 1px solid #cbd5e1; color: #64748b; font-size: 12px; font-style: normal; padding: 10px; text-align: center; }
  .run-track .critical-step { background: #07111f; border: 0; color: white; margin: 20px -24px 0; padding: 34px 24px; }
  .critical-step h3 { color: white; }
  .critical-step p { color: #94a3b8; }
  .critical-step em { border-color: rgba(245,158,11,.48); color: #fbbf24; }
  .sentiment-rule { display: grid; gap: 1px; grid-template-columns: repeat(3, 1fr); margin-top: 54px; background: #cbd5e1; border: 1px solid #cbd5e1; }
  .sentiment-rule article { background: white; padding: 28px; }
  .sentiment-rule span { color: #64748b; font-size: 12px; }
  .sentiment-rule strong { display: block; font-size: 26px; margin-top: 10px; }
  .sentiment-rule p { color: #64748b; margin: 10px 0 0; }

  .gate-flow { margin-top: 70px; }
  .gate-start { border: 1px solid #334155; margin: 0 auto; max-width: 420px; padding: 24px; text-align: center; }
  .gate-start strong { color: white; display: block; font-size: 28px; margin-top: 10px; }
  .gate-start p { color: #94a3b8; margin: 8px 0 0; }
  .gate-question { align-items: center; display: flex; flex-direction: column; margin: 42px auto; }
  .gate-question::before, .gate-question::after { background: #334155; content: ""; height: 34px; width: 1px; }
  .gate-question span { align-items: center; background: var(--story-amber); border-radius: 50%; color: #291800; display: flex; font-size: 28px; height: 64px; justify-content: center; width: 64px; }
  .gate-question strong { color: white; font-size: 24px; margin: 16px 0; }
  .gate-branches { display: grid; gap: 22px; grid-template-columns: 1fr 1fr; }
  .gate-branches article { border: 1px solid #334155; min-height: 280px; padding: 34px; }
  .gate-branches h3 { color: white; font-size: 34px; margin: 28px 0 14px; }
  .gate-branches p { color: #94a3b8; line-height: 1.7; }
  .gate-branches b { display: inline-block; margin-top: 32px; padding: 9px 11px; }
  .branch-success { border-top: 5px solid #22c55e !important; }
  .branch-success b { background: rgba(34,197,94,.15); color: #86efac; }
  .branch-fail { border-top: 5px solid var(--story-amber) !important; }
  .branch-fail b { background: rgba(245,158,11,.14); color: #fbbf24; }
  .writeback-only { background: #111c2d; border-left: 5px solid var(--story-blue-bright); margin-top: 32px; padding: clamp(26px, 5vw, 48px); }
  .writeback-only > span { color: #60a5fa; font-family: ui-monospace, monospace; font-size: 10px; letter-spacing: .12em; }
  .writeback-only h3 { color: white; font-size: 30px; margin: 14px 0 22px; }
  .writeback-only ul { color: #cbd5e1; line-height: 1.9; padding-left: 20px; }
  .writeback-only p { border-top: 1px solid #334155; color: #fbbf24; margin: 26px 0 0; padding-top: 22px; }

  .modes-chapter { background: #f1f5f9; }
  .mode-compare { display: grid; gap: 22px; grid-template-columns: 1fr 1fr; margin-top: 72px; }
  .mode-compare article { background: white; border: 1px solid #cbd5e1; min-height: 430px; padding: 36px; }
  .daily-mode { border-top: 6px solid var(--story-blue-bright) !important; }
  .compare-mode { border-top: 6px solid var(--story-amber) !important; }
  .mode-head { display: flex; justify-content: space-between; }
  .mode-head strong { font-family: ui-monospace, monospace; font-size: 12px; }
  .mode-compare h3 { font-size: clamp(28px, 4vw, 46px); line-height: 1.12; margin: 55px 0 28px; }
  .mode-compare ul { color: #475569; line-height: 2; padding-left: 20px; }
  .mode-compare > article > b { background: #e2e8f0; display: inline-block; margin-top: 28px; padding: 10px; }
  .boundary-board { background: #07111f; color: white; margin-top: 96px; padding: clamp(28px, 6vw, 64px); }
  .boundary-title h3 { color: white; font-size: clamp(32px, 5vw, 60px); margin: 14px 0 48px; }
  .boundary-grid { display: grid; gap: 1px; grid-template-columns: repeat(3, 1fr); background: #334155; border: 1px solid #334155; }
  .boundary-grid article { background: #0b1626; min-height: 220px; padding: 26px; }
  .boundary-grid strong { color: white; display: block; font-size: 20px; margin: 28px 0 10px; }
  .boundary-grid p { color: #94a3b8; line-height: 1.65; }

  .story-closing { background: var(--story-blue); color: white; padding: clamp(100px, 14vw, 190px) clamp(24px, 7vw, 112px); }
  .story-closing .chapter-label { color: #bfdbfe; }
  .story-closing h2 { color: white; max-width: 1100px; }
  .story-closing h2 em { color: #fde68a; font-style: normal; }
  .story-closing > p:not(.chapter-label) { color: #dbeafe; font-size: clamp(18px, 2.5vw, 26px); line-height: 1.7; max-width: 920px; }
  .closing-pipeline { align-items: center; border-bottom: 1px solid rgba(255,255,255,.25); border-top: 1px solid rgba(255,255,255,.25); display: flex; flex-wrap: wrap; gap: 12px; margin: 70px 0 42px; padding: 26px 0; }
  .closing-pipeline span, .closing-pipeline strong { font-family: ui-monospace, monospace; font-size: 12px; }
  .closing-pipeline i { color: #93c5fd; font-style: normal; }
  .closing-pipeline strong { color: #fde68a; }
  .story-closing > a { background: white !important; color: var(--story-blue) !important; display: inline-flex; font-weight: 800; gap: 20px; padding: 16px 18px; }

  @media (max-width: 920px) {
    .rail-links { display: none; }
    .story-rail { grid-template-columns: 1fr; }
    .metric-strip, .three-layer, .fact-guards, .sentiment-rule, .boundary-grid { grid-template-columns: 1fr 1fr; }
    .problem-layout { grid-template-columns: 1fr; }
    .problem-layout blockquote { position: static; }
    .source-merge { grid-template-columns: 1fr; }
    .merge-core { flex-direction: row; gap: 12px; padding: 8px; }
    .merge-core span { writing-mode: initial; }
    .merge-core b { margin: 0; transform: rotate(90deg); }
    .run-track article { grid-template-columns: 70px 1fr; }
    .run-track em { grid-column: 2; justify-self: start; }
  }

  @media (max-width: 640px) {
    .story-hero { padding-top: 74px; }
    .story-kicker span { display: inline-block; margin-bottom: 10px; }
    .hero-copy h1 { font-size: clamp(43px, 14vw, 70px); }
    .hero-actions { align-items: flex-start; flex-direction: column; }
    .dashboard-main { align-items: flex-start; flex-direction: column; }
    .metric-strip, .three-layer, .fact-guards, .sentiment-rule, .gate-branches, .mode-compare, .boundary-grid { grid-template-columns: 1fr; }
    .metric-strip article { border-bottom: 1px solid rgba(148,163,184,.18); border-right: 0; }
    .problem-list article { grid-template-columns: 40px 1fr; }
    .problem-list p { grid-column: 2; }
    .three-layer article { min-height: auto; }
    .schema-lines p { grid-template-columns: 1fr; }
    .fact-rule { grid-template-columns: 1fr; }
    .run-track article { gap: 14px; grid-template-columns: 54px 1fr; }
    .run-track > article > span { font-size: 32px; }
    .run-track .critical-step { margin-left: -12px; margin-right: -12px; }
    .gate-question strong { font-size: 20px; }
    .mode-compare article { min-height: auto; }
    .chapter-index { font-size: 150px; top: 50px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .rail-progress span, .rail-links a { transition: none; }
  }
</style>
