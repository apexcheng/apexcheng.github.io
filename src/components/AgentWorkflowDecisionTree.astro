<section class="routing-console" aria-label="Agent 任务路由控制台">
  <header class="console-header">
    <div>
      <span class="console-kicker">01 · TASK ROUTING</span>
      <h3>先决定流程重量，再开始执行</h3>
      <p>工作流不是越完整越好。用任务规模、状态风险和副作用三个信号，选择刚好够用的执行方式。</p>
    </div>
    <div class="entry-chip"><span aria-hidden="true">↓</span> 新任务进入</div>
  </header>

  <div class="route-map">
    <article class="question-panel q-one">
      <span>Q1 · 规模</span>
      <strong>能否在一个清晰动作里完成并验证？</strong>
      <small>例如补一句文案、修一个样式、改一个局部判断。</small>
    </article>

    <div class="route-bridge" aria-hidden="true"><i>是</i><b></b><i>否</i></div>

    <div class="route-results">
      <article class="route-card direct">
        <span class="decision-path">Q1 = 是</span>
        <span class="route-status">ROUTE A</span>
        <h4>直接执行</h4>
        <p>不为了“小事”制造额外状态。</p>
        <ul><li>最小改动</li><li>局部验证</li></ul>
      </article>

      <article class="question-panel q-two">
        <span>Q2 · 状态</span>
        <strong>是否必须分阶段推进，且中断后难以只靠当前上下文续接？</strong>
        <small>跨文件本身不等于高状态风险；关键是任务能否分段验收，以及是否需要稳定记录下一步。</small>
      </article>

      <article class="route-card task">
        <span class="decision-path">Q1 = 否 · Q2 = 是</span>
        <span class="route-status">ROUTE C</span>
        <h4>建立 TASK.md</h4>
        <p>把每次继续执行所需的事实外置。</p>
        <ul><li>阶段与进度</li><li>风险与验收</li></ul>
      </article>
    </div>

    <div class="route-bridge second-bridge" aria-hidden="true"><i>否</i><b></b><i>是</i></div>

    <article class="route-card brief">
      <span class="decision-path">Q1 = 否 · Q2 = 否</span>
      <span class="route-status">ROUTE B</span>
      <h4>轻量 brief</h4>
      <p>用一页目标、边界和验证点，保持执行可读。</p>
      <ul><li>目标明确</li><li>不留长期负担</li></ul>
    </article>
  </div>

  <aside class="route-rule">
    <span>副作用闸门</span>
    <strong>所有副作用：记录动作并验证结果</strong>
    <p>只有超出既有授权、对外发送、批量高风险或不可逆操作，才额外停下来请求人工确认。</p>
  </aside>
</section>

<style>
  .routing-console {
    --route-ink: #18243a;
    --route-muted: #64748b;
    --route-line: #d8e1ef;
    --route-blue: #2f6feb;
    --route-purple: #7653d6;
    --route-green: #178b68;
    --route-orange: #c96a1d;
    color: var(--route-ink);
    width: min(1080px, calc(100vw - 2rem));
    margin: 1.5rem 0 2.25rem 50%;
    overflow: hidden;
    border: 1px solid #cdd9ea;
    border-radius: 22px;
    background: linear-gradient(145deg, #f7faff, #fff 42%, #fbfaff);
    box-shadow: 0 24px 56px rgba(42, 67, 108, .12);
    transform: translateX(-50%);
  }

  .console-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1.4rem;
    padding: 1.55rem 1.65rem 1.35rem;
    border-bottom: 1px solid var(--route-line);
    background: linear-gradient(100deg, rgba(47, 111, 235, .11), transparent 58%), #fff;
  }

  .console-kicker,
  .route-status,
  .decision-path,
  .question-panel > span,
  .route-rule > span {
    color: var(--route-blue);
    font-size: .7rem;
    font-weight: 900;
    letter-spacing: .08em;
  }

  .console-header h3 { margin: .34rem 0 .45rem; color: #13213a; font-size: clamp(1.35rem, 2.8vw, 2.15rem); line-height: 1.2; }
  .console-header p { max-width: 650px; margin: 0; color: var(--route-muted); font-size: .9rem; line-height: 1.72; }
  .entry-chip { flex: 0 0 auto; padding: .55rem .7rem; border: 1px solid #a7c5f3; border-radius: 9px; color: var(--route-blue); background: #eef5ff; font-size: .75rem; font-weight: 800; }
  .entry-chip span { margin-right: .28rem; font-size: 1.05rem; }

  .route-map { position: relative; padding: 1.5rem 1.65rem 1.35rem; background-image: radial-gradient(#dce6f6 1px, transparent 1px); background-size: 15px 15px; }
  .question-panel, .route-card { position: relative; border: 1px solid var(--route-line); border-radius: 13px; background: rgba(255, 255, 255, .94); box-shadow: 0 10px 24px rgba(56, 80, 117, .06); }
  .question-panel { max-width: 620px; margin: 0 auto; padding: .95rem 1.1rem; text-align: center; }
  .question-panel > span { display: block; margin-bottom: .35rem; color: var(--route-purple); }
  .question-panel strong { display: block; color: #263554; font-size: 1rem; line-height: 1.45; }
  .question-panel small { display: block; margin-top: .3rem; color: var(--route-muted); font-size: .8125rem; line-height: 1.5; }
  .route-bridge { display: grid; grid-template-columns: 1fr 2px 1fr; align-items: center; max-width: 760px; height: 38px; margin: 0 auto; color: var(--route-muted); font-size: .72rem; font-style: normal; font-weight: 800; }
  .route-bridge b { align-self: stretch; background: #a9b9d1; }
  .route-bridge i:first-child { justify-self: start; color: var(--route-green); }
  .route-bridge i:last-child { justify-self: end; color: var(--route-orange); }

  .route-results { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1.18fr) minmax(0, 1fr); align-items: center; gap: .85rem; }
  .route-card { min-height: 154px; padding: 1rem; }
  .route-card h4 { margin: .35rem 0; color: #1e2e49; font-size: 1.08rem; }
  .route-card p { min-height: 2.9em; margin: 0; color: var(--route-muted); font-size: .8125rem; line-height: 1.58; }
  .route-card ul { display: flex; flex-wrap: wrap; gap: .35rem; margin: .8rem 0 0; padding: 0; list-style: none; }
  .route-card li { padding: .22rem .38rem; border-radius: 4px; background: #f1f5f9; color: #53647b; font-size: .6875rem; font-weight: 800; }
  .decision-path { display: block; margin-bottom: .28rem; color: #526987; font-size: .6875rem; }
  .direct { border-color: #b5dfcf; background: linear-gradient(145deg, #f2fbf7, #fff); }
  .direct .route-status { color: var(--route-green); }
  .task { border-color: #bec3f2; background: linear-gradient(145deg, #f5f3ff, #fff); }
  .task .route-status { color: var(--route-purple); }
  .brief { width: min(370px, 100%); margin: 0 auto; border-color: #f0cda9; background: linear-gradient(145deg, #fff8ed, #fff); }
  .brief .route-status { color: var(--route-orange); }
  .second-bridge { max-width: 430px; transform: translateX(17%); }

  .route-rule { display: grid; grid-template-columns: auto minmax(180px, 1fr) minmax(220px, 1.3fr); gap: 1rem; align-items: center; margin: 0 1.65rem 1.45rem; padding: .9rem 1rem; border: 1px solid #efc3a0; border-radius: 11px; background: #fff8ef; }
  .route-rule > span { color: #b85d19; }
  .route-rule strong { color: #7c3e14; font-size: .86rem; line-height: 1.5; }
  .route-rule p { margin: 0; color: #8c6246; font-size: .8125rem; line-height: 1.55; }

  @media (max-width: 1400px) and (min-width: 761px) {
    .routing-console {
      width: 100%;
      margin-left: 0;
      transform: none;
    }
  }

  @media (max-width: 760px) {
    .routing-console { width: auto; margin: 1.25rem -.35rem 1.8rem; border-radius: 16px; transform: none; }
    .console-header { flex-direction: column; padding: 1.1rem; }
    .entry-chip { align-self: flex-start; }
    .route-map { padding: 1rem; background-size: 12px 12px; }
    .route-results { grid-template-columns: 1fr; }
    .route-bridge, .second-bridge { display: none; }
    .question-panel { text-align: left; }
    .route-results { margin-top: .8rem; }
    .route-card { min-height: 0; }
    .route-card p { min-height: 0; }
    .q-two { order: 2; }
    .task { order: 3; }
    .route-rule { grid-template-columns: 1fr; gap: .4rem; margin: 0 1rem 1rem; }
  }
</style>
