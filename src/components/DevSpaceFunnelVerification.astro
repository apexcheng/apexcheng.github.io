<section class="funnel-verification" aria-label="DevSpace 通过 Tailscale Funnel 接入 ChatGPT 的验收路径">
  <header class="verification-header">
    <div>
      <span class="eyebrow">VALIDATION PATH</span>
      <h3>先本地，再公网，最后做真实调用</h3>
      <p>哪一层没有达到预期，就停在这一层排查。不要在本地还没通时先查 ChatGPT。</p>
    </div>
    <code>https://...ts.net/mcp</code>
  </header>

  <div class="verification-track" aria-label="四层验收顺序">
    <article class="verification-node local-node">
      <span class="node-step">01</span>
      <small>WSL 本地</small>
      <strong>DevSpace</strong>
      <code>127.0.0.1:7676/mcp</code>
      <span class="node-signal green">401</span>
      <p>端点存在，只是当前请求还没有授权。</p>
    </article>

    <div class="verification-connector">
      <span>通过后</span>
      <i aria-hidden="true">→</i>
    </div>

    <article class="verification-node oauth-node">
      <span class="node-step">02</span>
      <small>OAuth 发现</small>
      <strong>Metadata</strong>
      <code>/.well-known/...</code>
      <span class="node-signal blue">200 × 2</span>
      <p>受保护资源和授权服务器信息都能访问。</p>
    </article>

    <div class="verification-connector">
      <span>通过后</span>
      <i aria-hidden="true">→</i>
    </div>

    <article class="verification-node funnel-node">
      <span class="node-step">03</span>
      <small>公网隧道</small>
      <strong>Tailscale Funnel</strong>
      <code>funnel status</code>
      <span class="node-signal cyan">Funnel on</span>
      <p>公网域名已经代理到本地的 7676。</p>
    </article>

    <div class="verification-connector">
      <span>通过后</span>
      <i aria-hidden="true">→</i>
    </div>

    <article class="verification-node chatgpt-node">
      <span class="node-step">04</span>
      <small>最终验收</small>
      <strong>ChatGPT</strong>
      <code>读取测试文件</code>
      <span class="node-signal violet">真实调用</span>
      <p>能读取 allowedRoots 中的文件，整条链路才算完成。</p>
    </article>
  </div>

  <div class="verification-rule">
    <strong>排查规则</strong>
    <p>看到 `401` 不代表失败；这里真正的失败，是某一层没有得到它应该出现的信号。</p>
    <div class="signal-list" aria-label="验收信号说明">
      <span><b>401</b> MCP 端点可达</span>
      <span><b>200 × 2</b> OAuth 发现完整</span>
      <span><b>Funnel on</b> 公网代理已开</span>
      <span><b>真实调用</b> 权限和项目目录闭环</span>
    </div>
  </div>
</section>

<style>
  .funnel-verification {
    background:
      radial-gradient(circle at 8% 0%, rgba(14, 165, 233, 0.18), transparent 30%),
      radial-gradient(circle at 94% 8%, rgba(99, 102, 241, 0.14), transparent 28%),
      var(--paper-soft);
    border: 1px solid var(--line-soft);
    border-radius: 24px;
    box-shadow: 0 24px 64px rgba(15, 23, 42, 0.08);
    margin: 30px 0;
    overflow: hidden;
    padding: clamp(22px, 4vw, 36px);
  }

  .verification-header {
    align-items: end;
    display: flex;
    gap: 24px;
    justify-content: space-between;
  }

  .verification-header > div {
    max-width: 700px;
  }

  .eyebrow {
    color: #2563eb;
    display: inline-flex;
    font-size: 12px;
    font-weight: 850;
    letter-spacing: 0.14em;
  }

  .verification-header h3 {
    color: var(--ink);
    font-size: clamp(24px, 4vw, 36px);
    letter-spacing: -0.045em;
    line-height: 1.14;
    margin: 10px 0 12px;
  }

  .verification-header p {
    color: var(--muted);
    line-height: 1.72;
    margin: 0;
  }

  .verification-header > code {
    background: rgba(37, 99, 235, 0.09);
    border: 1px solid rgba(37, 99, 235, 0.16);
    border-radius: 999px;
    color: #2563eb;
    flex: 0 0 auto;
    font-size: 12px;
    padding: 9px 12px;
  }

  .verification-track {
    align-items: stretch;
    display: grid;
    gap: 10px;
    grid-template-columns: minmax(150px, 1fr) 44px minmax(150px, 1fr) 44px minmax(150px, 1fr) 44px minmax(150px, 1fr);
    margin-top: 28px;
  }

  .verification-node {
    background: var(--paper);
    border: 1px solid var(--line-soft);
    border-radius: 18px;
    display: flex;
    flex-direction: column;
    min-height: 230px;
    padding: 18px;
    position: relative;
  }

  .verification-node::before {
    background: var(--node-accent);
    border-radius: 999px;
    content: '';
    height: 3px;
    margin-bottom: 18px;
    width: 42px;
  }

  .local-node { --node-accent: #10b981; }
  .funnel-node { --node-accent: #06b6d4; }
  .oauth-node { --node-accent: #2563eb; }
  .chatgpt-node { --node-accent: #7c3aed; }

  .node-step {
    color: var(--soft);
    font-size: 11px;
    font-weight: 850;
    position: absolute;
    right: 16px;
    top: 16px;
  }

  .verification-node small {
    color: var(--soft);
    font-size: 12px;
    font-weight: 760;
  }

  .verification-node strong {
    color: var(--ink);
    font-size: 19px;
    letter-spacing: -0.025em;
    margin-top: 6px;
  }

  .verification-node code {
    background: var(--paper-soft);
    border: 1px solid var(--line-soft);
    border-radius: 10px;
    color: var(--muted);
    font-size: 11px;
    line-height: 1.5;
    margin-top: 14px;
    padding: 8px 9px;
    word-break: break-word;
  }

  .node-signal {
    align-self: flex-start;
    border-radius: 999px;
    font-size: 12px;
    font-weight: 850;
    margin-top: 14px;
    padding: 6px 10px;
  }

  .node-signal.green {
    background: rgba(16, 185, 129, 0.11);
    color: #059669;
  }

  .node-signal.cyan {
    background: rgba(6, 182, 212, 0.11);
    color: #0891b2;
  }

  .node-signal.blue {
    background: rgba(37, 99, 235, 0.1);
    color: #2563eb;
  }

  .node-signal.violet {
    background: rgba(124, 58, 237, 0.1);
    color: #7c3aed;
  }

  .verification-node p {
    color: var(--muted);
    font-size: 13px;
    line-height: 1.62;
    margin: auto 0 0;
    padding-top: 14px;
  }

  .verification-connector {
    align-items: center;
    color: var(--soft);
    display: flex;
    flex-direction: column;
    font-size: 10px;
    font-weight: 800;
    gap: 8px;
    justify-content: center;
    text-align: center;
  }

  .verification-connector i {
    color: #2563eb;
    font-size: 24px;
    font-style: normal;
    line-height: 1;
  }

  .verification-rule {
    background: var(--paper);
    border: 1px solid var(--line-soft);
    border-radius: 18px;
    margin-top: 18px;
    padding: 20px 22px;
  }

  .verification-rule > strong {
    color: var(--ink);
    font-size: 16px;
  }

  .verification-rule > p {
    color: var(--muted);
    line-height: 1.65;
    margin: 8px 0 0;
  }

  .signal-list {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 16px;
  }

  .signal-list span {
    background: var(--paper-soft);
    border: 1px solid var(--line-soft);
    border-radius: 999px;
    color: var(--muted);
    font-size: 12px;
    padding: 7px 10px;
  }

  .signal-list b {
    color: var(--ink);
  }

  @media (max-width: 920px) {
    .verification-header {
      align-items: start;
      flex-direction: column;
    }

    .verification-track {
      grid-template-columns: 1fr;
    }

    .verification-node {
      min-height: 0;
    }

    .verification-node p {
      margin-top: 0;
    }

    .verification-connector {
      gap: 4px;
      min-height: 40px;
    }

    .verification-connector i {
      transform: rotate(90deg);
    }
  }
</style>
