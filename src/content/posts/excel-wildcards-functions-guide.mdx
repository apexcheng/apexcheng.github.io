---
title: "Excel 通配符使用指南：*、?、~ 和支持函数清单"
description: "讲清 Excel 中 *、?、~ 三种通配符的含义，整理 COUNTIF、SUMIFS、XLOOKUP、MATCH、SEARCH 等支持通配符的常用函数，并给出前缀、包含、结尾和转义案例。"
date: 2026-07-16T09:46:18+08:00
updated: 2026-07-17T10:10:02+08:00
category: "教程"
tags:
  - "Excel"
  - "数据处理"
minutes: 12
featured: false
series: "excel-functions"
seriesOrder: 4
draft: false
private: false
---

import HighlightBox from '../../components/HighlightBox.astro';
import DecisionFlow from '../../components/DecisionFlow.astro';

在 Excel 里，通配符就是用一个符号代替不确定的文字。比如商品编码可能是 `K161-BK`、`K161-WH`、`K161-蓝牙版`，共同点都是以 `K161` 开头，就不必先用 `LEFT` 截取，直接写 `"K161*"` 即可。

先看一个完整案例。下面的公式会检查 SKU 列，找到所有以 `K161` 开头的记录，再把对应订单金额汇总起来。

<div class="wildcard-sheet-hero" role="group" aria-label="SUMIF 使用 K161 星号匹配 SKU 并汇总订单金额">
  <header class="wildcard-sheet-hero__header">
    <div class="wildcard-excel-mark" aria-hidden="true">X</div>
    <div>
      <span>EXCEL WILDCARD MATCHING</span>
      <strong><code>K161&#42;</code>：匹配所有以 K161 开头的 SKU</strong>
      <p>不用 LEFT 截取，直接用通配符完成匹配与汇总。</p>
    </div>
  </header>

  <div class="wildcard-sheet-workspace">
    <div class="wildcard-sheet-table-wrap">
      <table class="wildcard-sheet-table">
        <thead>
          <tr class="sheet-column-letters">
            <th class="sheet-corner" aria-hidden="true"></th>
            <th>A</th>
            <th>B</th>
            <th>C</th>
            <th>D</th>
            <th>E</th>
          </tr>
          <tr class="sheet-field-row">
            <th class="sheet-row-number">7</th>
            <th>订单号</th>
            <th>平台</th>
            <th class="is-condition-column">SKU</th>
            <th class="is-sum-column">订单金额</th>
            <th>客服响应分钟</th>
          </tr>
        </thead>
        <tbody>
          <tr class="is-hit"><th class="sheet-row-number">8</th><td>TM001</td><td>天猫</td><td class="is-condition-column is-match">K161-BK</td><td class="is-sum-column is-sum">199.00</td><td>4</td></tr>
          <tr><th class="sheet-row-number">9</th><td>PDD002</td><td>拼多多</td><td class="is-condition-column">X51C-BK</td><td class="is-sum-column">159.00</td><td>8</td></tr>
          <tr><th class="sheet-row-number">10</th><td>JD003</td><td>京东</td><td class="is-condition-column">M30-BK</td><td class="is-sum-column">99.00</td><td>3</td></tr>
          <tr><th class="sheet-row-number">11</th><td>TM004</td><td>天猫</td><td class="is-condition-column">P12-BK</td><td class="is-sum-column">149.00</td><td>5</td></tr>
          <tr class="is-hit"><th class="sheet-row-number">12</th><td>PDD005</td><td>拼多多</td><td class="is-condition-column is-match">K161-WH</td><td class="is-sum-column is-sum">219.00</td><td>12</td></tr>
          <tr><th class="sheet-row-number">13</th><td>JD006</td><td>京东</td><td class="is-condition-column">X51C-BK</td><td class="is-sum-column">169.00</td><td>6</td></tr>
          <tr><th class="sheet-row-number">14</th><td>TM007</td><td>天猫</td><td class="is-condition-column">M30-WH</td><td class="is-sum-column">109.00</td><td>2</td></tr>
          <tr><th class="sheet-row-number">15</th><td>PDD008</td><td>拼多多</td><td class="is-condition-column">P12-BL</td><td class="is-sum-column">139.00</td><td>10</td></tr>
          <tr class="is-hit"><th class="sheet-row-number">16</th><td>TM009</td><td>天猫</td><td class="is-condition-column is-match">K161-BK</td><td class="is-sum-column is-sum">209.00</td><td>7</td></tr>
          <tr><th class="sheet-row-number">17</th><td>PDD010</td><td>拼多多</td><td class="is-condition-column">M30-BK</td><td class="is-sum-column">89.00</td><td>9</td></tr>
        </tbody>
      </table>

      <div class="wildcard-sheet-legend">
        <span class="legend-blue"><b>1</b>条件范围 C8:C17</span>
        <span class="legend-orange"><b>2</b>条件：以 K161 开头</span>
        <span class="legend-violet"><b>3</b>求和范围 D8:D17</span>
      </div>
    </div>

    <div class="wildcard-formula-panel">
      <div class="wildcard-formula-heading"><span>▦</span><strong>SUMIF 公式</strong></div>
      <div class="wildcard-formula-line" aria-label="SUMIF 公式参数拆解">
        <span>=SUMIF(</span><b class="tone-blue"><em>1</em>C8:C17</b><span>,</span><b class="tone-orange"><em>2</em>{'"K161*"'}</b><span>,</span><b class="tone-violet"><em>3</em>D8:D17</b><span>)</span>
      </div>

      <div class="wildcard-formula-legend">
        <div class="tone-blue"><b>1</b><section><small>检查哪里</small><strong>SKU 列</strong><span>C8:C17</span></section></div>
        <div class="tone-orange"><b>2</b><section><small>匹配什么</small><strong>以 K161 开头</strong><span>{'"K161*"'}</span></section></div>
        <div class="tone-violet"><b>3</b><section><small>汇总哪里</small><strong>订单金额列</strong><span>D8:D17</span></section></div>
      </div>

      <div class="wildcard-result-card">
        <div><small>命中</small><strong>K161-BK、K161-WH、K161-BK</strong></div>
        <div><small>金额</small><strong>199 + 219 + 209</strong></div>
        <div class="is-total"><small>结果</small><strong>627</strong></div>
      </div>

      <div class="wildcard-result-path"><span>C 列匹配 3 行</span><i>→</i><span>保持相同行</span><i>→</i><strong>D 列汇总：627</strong></div>
    </div>
  </div>
</div>

这个案例里，`C8:C17`、`"K161*"`、`D8:D17` 分别回答三个问题：**检查哪里、匹配什么、汇总哪里**。理解这三个问题，比单独背公式参数更容易。

表格上方的 `A～E` 和左侧的 `7～17` 是 Excel 的列标与行号，不属于数据本身。字段标题放在第 7 行，公式从第 8 行开始计算，因此 SKU 对应 `C8:C17`，订单金额对应 `D8:D17`。

## Excel 只有三种通配符

严格来说，真正负责模糊匹配的是 `*` 和 `?`；`~` 是转义符，用于告诉 Excel：后面的星号或问号是普通字符，不要把它当作通配符。

<div class="wildcard-symbol-lab" role="group" aria-label="Excel 三种通配符的匹配实验">
  <article class="wildcard-symbol-card is-star">
    <header><code>&#42;</code><div><strong>任意数量字符</strong><span>可以很多，也可以没有</span></div></header>
    <div class="wildcard-token-demo">
      <span class="token-fixed">K161</span><span class="token-stretch">任意内容</span>
    </div>
    <div class="wildcard-test-list">
      <span class="is-pass"><code>K161</code><b>✓ 匹配</b></span>
      <span class="is-pass"><code>K161-BK</code><b>✓ 匹配</b></span>
      <span class="is-pass"><code>K161-黑色-套装</code><b>✓ 匹配</b></span>
    </div>
    <p><code>K161&#42;</code> 表示固定内容是 K161，后面允许出现任意数量字符。</p>
  </article>

  <article class="wildcard-symbol-card is-question">
    <header><code>?</code><div><strong>正好一个字符</strong><span>不能多，也不能少</span></div></header>
    <div class="wildcard-token-demo">
      <span class="token-fixed">K</span><span class="token-slot">?</span><span class="token-fixed">61</span>
    </div>
    <div class="wildcard-test-list">
      <span class="is-pass"><code>K161</code><b>✓ 匹配</b></span>
      <span class="is-pass"><code>KA61</code><b>✓ 匹配</b></span>
      <span class="is-fail"><code>K1261</code><b>× 两个字符</b></span>
    </div>
    <p><code>K?61</code> 中间只有一个字符槽位，所以 K1261 不符合。</p>
  </article>

  <article class="wildcard-symbol-card is-escape">
    <header><code>&#126;</code><div><strong>取消通配能力</strong><span>查找真正的 &#42; 或 ?</span></div></header>
    <div class="wildcard-escape-demo">
      <span><code>&#42;</code><small>任意字符</small></span>
      <b>→</b>
      <span><code>&#126;&#42;</code><small>真实星号</small></span>
    </div>
    <div class="wildcard-test-list">
      <span class="is-pass"><code>赠品&#42;</code><b>✓ 包含星号</b></span>
      <span class="is-pass"><code>赠品&#42;请勿单拍</code><b>✓ 包含星号</b></span>
      <span class="is-fail"><code>赠品说明</code><b>× 没有星号</b></span>
    </div>
    <p><code>{'"*~**"'}</code> 中，前后两个星号是通配符，中间的 <code>{'~*'}</code> 才是真正的星号字符。</p>
  </article>
</div>

## 六种高频写法

通配符最常见的差别，不是函数，而是固定文字出现在什么位置。把字符串看成几段，就能快速判断星号应该放在哪里。

<div class="wildcard-position-map" role="group" aria-label="Excel 六种常见通配符位置关系">
  <article>
    <span>开头匹配</span>
    <div class="wildcard-position-track"><b>K161</b><i>任意内容</i></div>
    <code>{'"K161*"'}</code>
    <p>型号固定，颜色、规格或备注在后面变化。</p>
  </article>
  <article>
    <span>结尾匹配</span>
    <div class="wildcard-position-track"><i>任意内容</i><b>已退款</b></div>
    <code>{'"*已退款"'}</code>
    <p>状态文字固定在单元格末尾。</p>
  </article>
  <article>
    <span>包含匹配</span>
    <div class="wildcard-position-track"><i>任意</i><b>蓝牙</b><i>任意</i></div>
    <code>{'"*蓝牙*"'}</code>
    <p>标题任意位置出现“蓝牙”。</p>
  </article>
  <article>
    <span>固定一位</span>
    <div class="wildcard-position-track"><b>K</b><em>?</em><b>61</b></div>
    <code>{'"K?61"'}</code>
    <p>K 和 61 中间必须正好一个字符。</p>
  </article>
  <article>
    <span>引用条件</span>
    <div class="wildcard-position-track"><b>F2 的内容</b><i>任意内容</i></div>
    <code>{'F2&"*"'}</code>
    <p>把单元格中的型号作为开头条件。</p>
  </article>
  <article>
    <span>查真实符号</span>
    <div class="wildcard-position-track"><i>任意</i><b>&#42;</b><i>任意</i></div>
    <code>{'"*~**"'}</code>
    <p>查找文本中真正出现的星号。</p>
  </article>
</div>

下面这张表保留了更完整的边界，适合需要核对时快速查询。

| 写法 | 匹配规则 | 可以匹配 | 不会匹配 |
| --- | --- | --- | --- |
| `K161*` | 以 K161 开头 | `K161`、`K161-黑色` | `黑色-K161` |
| `*K161` | 以 K161 结尾 | `黑色-K161` | `K161-黑色` |
| `*K161*` | 任意位置包含 K161 | `黑色-K161-套装` | `K162-黑色` |
| `K161?` | K161 后正好一个字符 | `K161A`、`K161黑` | `K161`、`K161黑色` |
| `K?61` | K 和 61 中间正好一个字符 | `K161`、`KA61` | `K1261` |
| `~*` | 查找真正的星号 | `赠品*` 中的 `*` | 普通文字 |

## 哪些函数支持通配符

并不是所有函数都能直接识别通配符。即使支持，不同函数的开启方式也不一样：条件统计函数直接写，`XLOOKUP` 和 `XMATCH` 要把匹配模式设为 `2`，`VLOOKUP`、`HLOOKUP` 和 `MATCH` 则要使用精确匹配参数。

<div class="wildcard-function-route" role="group" aria-label="根据需求选择支持通配符的 Excel 函数">
  <header>
    <span>先回答一个问题</span>
    <strong>你希望 Excel 返回什么？</strong>
  </header>
  <div class="wildcard-function-route__grid">
    <article>
      <span>统计或汇总</span>
      <strong>直接把通配符写进条件</strong>
      <div><code>COUNTIF(S)</code><code>SUMIF(S)</code><code>AVERAGEIF(S)</code><code>MAXIFS</code><code>MINIFS</code></div>
      <small>例如：<code>{'"K161*"'}</code></small>
    </article>
    <article>
      <span>返回一条结果</span>
      <strong>查找函数需要开启匹配方式</strong>
      <div><code>XLOOKUP</code><code>VLOOKUP</code><code>HLOOKUP</code></div>
      <small>XLOOKUP 写 <code>match_mode=2</code>；传统函数写 <code>FALSE</code></small>
    </article>
    <article>
      <span>返回所在位置</span>
      <strong>位置函数同样区分参数</strong>
      <div><code>XMATCH</code><code>MATCH</code></div>
      <small>XMATCH 写 <code>2</code>；MATCH 写 <code>0</code></small>
    </article>
    <article>
      <span>判断文字内部</span>
      <strong>在单元格文本里搜索模式</strong>
      <div><code>SEARCH</code><code>ISNUMBER</code><code>FILTER</code></div>
      <small>SEARCH 负责匹配，其他函数负责转换或筛选</small>
    </article>
  </div>
</div>

### 支持方式速查表

| 函数 | 通配符写在哪里 | 必须注意的参数 | 常见用途 |
| --- | --- | --- | --- |
| `COUNTIF`、`COUNTIFS` | 条件参数 | 无 | 统计某类编码、标题或状态数量 |
| `SUMIF`、`SUMIFS` | 条件参数 | 无 | 汇总某类商品的销量或金额 |
| `AVERAGEIF`、`AVERAGEIFS` | 条件参数 | 无 | 计算某类商品的平均值 |
| `MAXIFS`、`MINIFS` | 条件参数 | 无 | 找出某类数据的最大值或最小值 |
| `VLOOKUP`、`HLOOKUP` | 查找值 | 最后一个参数写 `FALSE` | 返回第一条符合模糊条件的数据 |
| `MATCH` | 查找值 | 第三个参数写 `0` | 返回第一条匹配记录的位置 |
| `XLOOKUP` | 查找值 | 第五个参数 `match_mode` 写 `2` | 返回第一条符合通配符的数据 |
| `XMATCH` | 查找值 | 第三个参数 `match_mode` 写 `2` | 返回第一条符合通配符的位置 |
| `SEARCH` | `find_text` 参数 | 无 | 在单元格文字内部查找模式 |

## 三类函数的写法不一样

### 1. 条件统计函数：直接写通配符

这类函数最简单，只需要把通配符写进条件中。

<div class="wildcard-condition-family" role="group" aria-label="条件统计函数使用通配符的四种写法">
  <article><span>统计数量</span><code>{'=COUNTIF(C8:C17,"K161*")'}</code><strong>命中 3 条</strong></article>
  <article><span>汇总金额</span><code>{'=SUMIF(C8:C17,"K161*",D8:D17)'}</code><strong>结果 627</strong></article>
  <article><span>计算平均</span><code>{'=AVERAGEIF(C8:C17,"K161*",D8:D17)'}</code><strong>结果 209</strong></article>
  <article><span>条件来自单元格</span><code>{'=AVERAGEIF(C8:C17,F2&"*",D8:D17)'}</code><strong>F2 填写 K161</strong></article>
</div>

条件来自单元格时，不要把单元格引用放进引号：

```text
=AVERAGEIF(C8:C17,F2&"*",D8:D17)
```

假设 F2 是 `K161`，`F2&"*"` 会拼成 `K161*`。这里的 F2 是额外的条件输入单元格，不属于上方 `A:E` 数据区域。

### 2. 查找函数：先开启正确的匹配模式

`XLOOKUP` 默认是精确匹配，但默认不会把 `*` 和 `?` 当成通配符。要使用通配符，必须把第五个参数设为 `2`：

<div class="wildcard-lookup-link">
  <div><span>需要先理解函数本身？</span><strong>VLOOKUP、XLOOKUP、INDEX、MATCH、XMATCH 已有单独的可视化专题。</strong></div>
  <a href="/articles/excel-lookup-functions-visual-guide/">查看查找函数专题 →</a>
</div>

<div class="wildcard-mode-compare" role="group" aria-label="XLOOKUP 是否开启通配符模式的结果对比">
  <section>
    <span>默认写法</span>
    <code>{'=XLOOKUP("K161*",C8:C17,D8:D17)'}</code>
    <strong>结果：未找到</strong>
    <p>默认把星号当成公式中的普通匹配内容，不会进入通配符模式。</p>
  </section>
  <div aria-hidden="true">→</div>
  <section class="is-correct">
    <span>开启通配符模式</span>
    <code>{'=XLOOKUP("K161*",C8:C17,D8:D17,"未找到",2)'}</code>
    <strong>结果：199</strong>
    <p>第五个参数写 2 后，返回第一条以 K161 开头的记录。</p>
  </section>
</div>

`XMATCH` 同样需要把第三个参数设为 `2`：

```text
=XMATCH("K161*",C8:C17,2)
```

传统查找函数使用精确匹配参数时，也能识别通配符：

```text
=VLOOKUP("K161*",C8:D17,2,FALSE)
```

```text
=MATCH("K161*",C8:C17,0)
```

<HighlightBox eyebrow="关键区别" title="精确匹配，不等于不能使用通配符">
  对 VLOOKUP、HLOOKUP 和 MATCH 来说，FALSE 或 0 的意思是不要做近似区间查找。查找值中如果包含 * 或 ?，Excel 仍然会按通配符规则匹配。XLOOKUP 和 XMATCH 的设计不同，需要专门选择 match_mode = 2。
</HighlightBox>

### 3. SEARCH：在文本内部查找模式

`SEARCH` 返回的是匹配内容在文本中的起始位置。比如编码中间有一个字符不确定：

```text
=SEARCH("K?61",C8)
```

上方案例中的 C8 是 `K161-BK`，结果为 `1`。如果找不到，会返回 `#VALUE!`。

实际判断时，通常会和 `ISNUMBER` 组合：

```text
=ISNUMBER(SEARCH("K?61",C8))
```

找到时返回 `TRUE`，找不到时返回 `FALSE`。

## 回到最开始的 AVERAGEIF 公式

原来的公式是：

```text
=AVERAGEIF(MAP(LEFT(C8:C17,4),LAMBDA(r,LEFT(r,4))),"K161",D8:D17)
```

它的真实目标只是：**找出 C 列中所有以 K161 开头的记录，再计算 D 列平均值。**

因此可以直接改成：

```text
=AVERAGEIF(C8:C17,"K161*",D8:D17)
```

<div class="wildcard-before-after" role="group" aria-label="复杂数组公式与通配符公式对比">
  <section>
    <span>原写法</span>
    <strong>先截取，再逐项处理</strong>
    <div class="wildcard-step-chain"><i>LEFT</i><b>→</b><i>MAP</i><b>→</b><i>LAMBDA</i><b>→</b><i>AVERAGEIF</i></div>
    <code>{'=AVERAGEIF(MAP(LEFT(...)),"K161",D8:D17)'}</code>
    <p>需要先生成中间数组，公式长，读者还要反推每一步在做什么。</p>
  </section>
  <div aria-hidden="true">→</div>
  <section class="is-recommended">
    <span>推荐写法</span>
    <strong>直接表达“以 K161 开头”</strong>
    <div class="wildcard-step-chain"><i>AVERAGEIF</i><b>+</b><i>{'"K161*"'}</i></div>
    <code>{'=AVERAGEIF(C8:C17,"K161*",D8:D17)'}</code>
    <p>不需要 LEFT、MAP 和 LAMBDA，条件本身就与实际需求完全一致。</p>
  </section>
</div>

## 容易踩的五个坑

<div class="wildcard-trouble-stack">
  <article>
    <span>01</span>
    <div>
      <h3>XLOOKUP 忘了写匹配模式 2</h3>
      <p><code>{'=XLOOKUP("K161*",C8:C17,D8:D17)'}</code> 不会启用通配符。应改为 <code>{'=XLOOKUP("K161*",C8:C17,D8:D17,"未找到",2)'}</code>。</p>
    </div>
    <b>参数问题</b>
  </article>
  <article>
    <span>02</span>
    <div>
      <h3><code>&#42;</code> 不等于“所有非空单元格”</h3>
      <p><code>{'=COUNTIF(A2:A100,"*")'}</code> 主要统计文本。要统计数字、文字和日期等全部非空内容，优先使用 <code>COUNTA(A2:A100)</code>。</p>
    </div>
    <b>统计口径</b>
  </article>
  <article>
    <span>03</span>
    <div>
      <h3>通配符通常不区分英文大小写</h3>
      <p><code>K161&#42;</code> 和 <code>k161&#42;</code> 通常会匹配相同内容。需要严格区分大小写时，不能只依赖通配符条件。</p>
    </div>
    <b>匹配边界</b>
  </article>
  <article>
    <span>04</span>
    <div>
      <h3>FIND 和 FILTER 不会直接解释通配符</h3>
      <p>FILTER 需要一组 TRUE/FALSE。按包含关系筛选时，可以组合：<code>{'=FILTER(D8:D17,ISNUMBER(SEARCH("K161",C8:C17)))'}</code>。</p>
    </div>
    <b>函数能力</b>
  </article>
  <article>
    <span>05</span>
    <div>
      <h3>前后空格会改变“开头”和“结尾”</h3>
      <p>如果单元格实际是 <code> K161-BK</code>，前面多了空格，就不满足“以 K161 开头”。数据来源不干净时，先用 TRIM 或清洗源数据。</p>
    </div>
    <b>数据质量</b>
  </article>
</div>

## 应该选哪个函数

<DecisionFlow
  title="通配符函数选择顺序"
  items={[
    {
      label: '统计或汇总',
      title: '使用 COUNTIF、SUMIF 等条件统计函数',
      description: '计数用 COUNTIF(S)，求和用 SUMIF(S)，平均值用 AVERAGEIF(S)，极值用 MAXIFS 或 MINIFS。',
    },
    {
      label: '返回一条结果',
      title: '优先使用 XLOOKUP',
      description: '把 match_mode 设为 2；旧表格可以使用 VLOOKUP，并把最后一个参数设为 FALSE。',
    },
    {
      label: '返回所在位置',
      title: '使用 XMATCH 或 MATCH',
      description: 'XMATCH 的 match_mode 写 2；MATCH 的 match_type 写 0。',
    },
    {
      label: '判断文字内部是否符合模式',
      title: '使用 SEARCH',
      description: '需要 TRUE/FALSE 时，再组合 ISNUMBER；需要筛选多行时，可继续组合 FILTER。',
    },
  ]}
/>

<div class="wildcard-final-check" role="group" aria-label="Excel 通配符使用判断标准">
  <span>开头</span><span>结尾</span><span>包含</span><span>固定一位</span>
  <strong>这些文字位置关系明确时，优先考虑通配符。</strong>
</div>

<HighlightBox eyebrow="最终判断" title="先看需求关系，再决定要不要使用通配符">
  条件本身是“以什么开头、以什么结尾、包含什么、某一位可以变化”时，通配符通常是最直接的写法。条件涉及数字区间、复杂逻辑或严格大小写时，就不应继续硬套通配符。
</HighlightBox>

## 参考资料

- [Microsoft Support：COUNTIF function](https://support.microsoft.com/en-us/office/countif-function-e0de10c6-f885-4e71-abb4-1f464816df34)
- [Microsoft Support：SUMIF function](https://support.microsoft.com/en-us/office/sumif-function-169b8c99-c05c-4483-a712-1697a653039b)
- [Microsoft Support：XLOOKUP function](https://support.microsoft.com/en-us/office/xlookup-function-b7fd680e-6d10-43e6-84f9-88eae8bf5929)
- [Microsoft Support：XMATCH function](https://support.microsoft.com/en-us/excel/functions/xmatch-function)
- [Microsoft Support：VLOOKUP function](https://support.microsoft.com/en-us/excel/functions/vlookup-function)
- [Microsoft Support：MATCH function](https://support.microsoft.com/en-us/excel/functions/match-function)
- [Microsoft Support：SEARCH function](https://support.microsoft.com/en-us/excel/functions/search-function)

<style>{`
  .wildcard-sheet-hero,
  .wildcard-symbol-lab,
  .wildcard-position-map,
  .wildcard-function-route,
  .wildcard-condition-family,
  .wildcard-lookup-link,
  .wildcard-mode-compare,
  .wildcard-before-after,
  .wildcard-trouble-stack,
  .wildcard-final-check {
    --wc-blue: #1569d8;
    --wc-blue-soft: #eef6ff;
    --wc-orange: #ed6d1f;
    --wc-orange-soft: #fff4ec;
    --wc-violet: #7741bc;
    --wc-violet-soft: #f6f0ff;
    --wc-green: #17884c;
    --wc-green-soft: #edf9f1;
    --wc-navy: #0c2852;
    --wc-text: #283c52;
    --wc-muted: #687b90;
    --wc-line: #d8e2ed;
    color: var(--wc-text);
  }

  .wildcard-sheet-hero {
    margin: 1.55rem 0 2.4rem;
    overflow: hidden;
    border: 1px solid #c9d8e8;
    border-radius: 26px;
    background:
      radial-gradient(circle at 92% 0%, rgba(21,105,216,.12), transparent 28%),
      linear-gradient(145deg,#fff,#f7fbff);
    box-shadow: 0 22px 58px rgba(33,70,113,.12);
  }
  .wildcard-sheet-hero__header {
    display: flex;
    gap: 1rem;
    align-items: center;
    padding: 1.35rem 1.45rem 1.1rem;
  }
  .wildcard-excel-mark {
    display: grid;
    flex: 0 0 54px;
    place-items: center;
    width: 54px;
    height: 54px;
    border-radius: 13px;
    color: #fff;
    background: linear-gradient(145deg,#24a45c,#0d753c);
    box-shadow: 0 10px 22px rgba(15,117,60,.22);
    font-size: 1.65rem;
    font-weight: 900;
  }
  .wildcard-sheet-hero__header span {
    display: block;
    color: var(--wc-blue);
    font-size: .75rem;
    font-weight: 900;
    letter-spacing: .14em;
  }
  .wildcard-sheet-hero__header strong {
    display: block;
    margin: .28rem 0 .2rem;
    color: var(--wc-navy);
    font-size: clamp(1.1rem,2.7vw,1.65rem);
    line-height: 1.35;
  }
  .wildcard-sheet-hero__header strong code { color: var(--wc-orange); background: transparent; }
  .wildcard-sheet-hero__header p { margin: 0; color: var(--wc-muted); font-size: .9rem; }
  .wildcard-sheet-workspace {
    display: grid;
    grid-template-columns: minmax(0,1.08fr) minmax(0,.92fr);
    gap: 1rem;
    padding: 0 1.1rem 1.2rem;
  }
  .wildcard-sheet-table-wrap {
    display: grid;
    align-content: start;
    gap: .7rem;
    min-width: 0;
    padding: .75rem;
    border: 1px solid #cbd9e8;
    border-radius: 17px;
    background: #fff;
  }
  .wildcard-sheet-table {
    width: 100%;
    margin: 0;
    overflow: hidden;
    border-collapse: separate;
    border-spacing: 0;
    border: 1px solid #c8d8e7;
    border-radius: 11px;
    table-layout: fixed;
    font-size: .88rem;
  }
  .wildcard-sheet-table th,
  .wildcard-sheet-table td {
    position: relative;
    padding: .56rem .45rem;
    border-right: 1px solid #e0e7ee;
    border-bottom: 1px solid #e0e7ee;
    text-align: left;
    white-space: nowrap;
  }
  .wildcard-sheet-table th:first-child,
  .wildcard-sheet-table td:first-child { width: 7%; text-align: center; }
  .wildcard-sheet-table th:nth-child(2),
  .wildcard-sheet-table td:nth-child(2) { width: 18%; }
  .wildcard-sheet-table th:nth-child(3),
  .wildcard-sheet-table td:nth-child(3) { width: 14%; }
  .wildcard-sheet-table th:nth-child(4),
  .wildcard-sheet-table td:nth-child(4) { width: 22%; }
  .wildcard-sheet-table th:nth-child(5),
  .wildcard-sheet-table td:nth-child(5) { width: 20%; }
  .wildcard-sheet-table th:nth-child(6),
  .wildcard-sheet-table td:nth-child(6) { width: 19%; }
  .wildcard-sheet-table .sheet-column-letters th {
    height: 30px;
    padding: .25rem .4rem;
    color: #26384b;
    background: #f2f4f6;
    font-size: .78rem;
    font-weight: 750;
    text-align: center;
  }
  .wildcard-sheet-table .sheet-column-letters .sheet-corner {
    background: linear-gradient(135deg, transparent 49%, #b7bec6 50%, #b7bec6 100%), #eef1f3;
  }
  .wildcard-sheet-table .sheet-field-row th {
    height: 48px;
    padding: .5rem .4rem;
    color: #fff;
    background: linear-gradient(180deg,#1971d8,#0e5ebd);
    font-size: .86rem;
    font-weight: 850;
    text-align: center;
    vertical-align: middle;
  }
  .wildcard-sheet-table .sheet-row-number {
    color: #33475a;
    background: #f2f4f6;
    font-size: .78rem;
    font-weight: 750;
    text-align: center;
  }
  .wildcard-sheet-table .sheet-field-row .sheet-row-number {
    color: #33475a;
    background: #f2f4f6;
  }
  .wildcard-sheet-table .is-condition-column {
    background: var(--wc-blue-soft);
    box-shadow: inset 2px 0 var(--wc-blue), inset -2px 0 var(--wc-blue);
  }
  .wildcard-sheet-table .is-sum-column {
    background: var(--wc-violet-soft);
    box-shadow: inset 2px 0 var(--wc-violet), inset -2px 0 var(--wc-violet);
  }
  .wildcard-sheet-table .sheet-field-row .is-condition-column {
    background: #145fbf;
  }
  .wildcard-sheet-table .sheet-field-row .is-sum-column {
    background: #6947ad;
  }
  .wildcard-sheet-table tbody tr:first-child .is-condition-column {
    border-top: 2px solid var(--wc-blue);
  }
  .wildcard-sheet-table tbody tr:first-child .is-sum-column {
    border-top: 2px solid var(--wc-violet);
  }
  .wildcard-sheet-table tbody tr:last-child .is-condition-column {
    border-bottom: 2px solid var(--wc-blue);
  }
  .wildcard-sheet-table tbody tr:last-child .is-sum-column {
    border-bottom: 2px solid var(--wc-violet);
  }
  .wildcard-sheet-table tr.is-hit td {
    color: #126c45;
    background: #eaf8f1;
    font-weight: 850;
  }
  .wildcard-sheet-table tr.is-hit .sheet-row-number {
    color: #126c45;
    background: #e4f4eb;
    font-weight: 900;
  }
  .wildcard-sheet-table tr.is-hit .is-condition-column,
  .wildcard-sheet-table tr.is-hit .is-sum-column {
    color: #126c45;
    background: #dff5e9;
  }
  .wildcard-sheet-table td.is-match,
  .wildcard-sheet-table td.is-sum {
    color: var(--wc-green);
    box-shadow: inset 0 0 0 2px var(--wc-green);
    font-weight: 900;
  }
  .wildcard-sheet-table tbody tr:last-child th,
  .wildcard-sheet-table tbody tr:last-child td { border-bottom: 0; }
  .wildcard-sheet-table tr > *:last-child { border-right: 0; }
  .wildcard-sheet-legend { display: flex; flex-wrap: wrap; gap: .42rem; }
  .wildcard-sheet-legend span {
    display: inline-flex;
    gap: .36rem;
    align-items: center;
    padding: .44rem .52rem;
    border: 1px solid #d2deea;
    border-radius: 9px;
    background: #f8fbff;
    font-size: .76rem;
    font-weight: 800;
    line-height: 1.35;
  }
  .wildcard-sheet-legend b {
    display: grid;
    place-items: center;
    width: 21px;
    height: 21px;
    border-radius: 999px;
    color: #fff;
    background: #6e8ba8;
    font-size: .68rem;
  }
  .wildcard-sheet-legend .legend-blue { color: #245f9d; border-color: #bcd3ea; background: #edf6ff; }
  .wildcard-sheet-legend .legend-blue b { background: var(--wc-blue); }
  .wildcard-sheet-legend .legend-orange { color: #9b482f; border-color: #efd0c5; background: #fff5f1; }
  .wildcard-sheet-legend .legend-orange b { background: var(--wc-orange); }
  .wildcard-sheet-legend .legend-violet { color: #6243a0; border-color: #d7caef; background: #f7f2ff; }
  .wildcard-sheet-legend .legend-violet b { background: var(--wc-violet); }

  .wildcard-formula-panel {
    display: grid;
    align-content: center;
    gap: .72rem;
    min-width: 0;
    padding: .82rem;
    border: 1px solid #cbd9e8;
    border-radius: 17px;
    background: rgba(255,255,255,.86);
  }
  .wildcard-formula-heading { display: flex; gap: .5rem; align-items: center; color: var(--wc-navy); }
  .wildcard-formula-heading span { display: grid; place-items: center; width: 25px; height: 25px; border-radius: 6px; color: #fff; background: var(--wc-green); font-size: .82rem; }
  .wildcard-formula-heading strong { font-size: .96rem; }
  .wildcard-formula-line,
  .wildcard-result-card,
  .wildcard-formula-legend > div {
    border: 1px solid #d5e0eb;
    background: rgba(255,255,255,.92);
    box-shadow: 0 10px 26px rgba(31,65,105,.07);
  }
  .wildcard-formula-line {
    overflow-x: auto;
    padding: .95rem .82rem;
    border-radius: 11px;
    color: #101820;
    font-family: ui-monospace,SFMono-Regular,Menlo,monospace;
    font-size: clamp(.9rem,1.8vw,1.05rem);
    font-weight: 850;
    line-height: 2.05;
    white-space: nowrap;
  }
  .wildcard-formula-line b { position: relative; display: inline-block; margin: .34rem .24rem; padding: .05rem .18rem; border-radius: 6px; }
  .wildcard-formula-line em {
    position: absolute;
    z-index: 1;
    display: grid;
    place-items: center;
    width: 22px;
    height: 22px;
    border: 2px solid #fff;
    border-radius: 999px;
    color: #fff;
    background: #6e8ba8;
    box-shadow: 0 2px 5px rgba(0,0,0,.18);
    font-size: .68rem;
    font-style: normal;
    line-height: 1;
  }
  .wildcard-formula-line b:nth-of-type(1) em { top: -.55rem; left: -.5rem; }
  .wildcard-formula-line b:nth-of-type(2) em { top: -.55rem; right: -.5rem; }
  .wildcard-formula-line b:nth-of-type(3) em { right: -.5rem; bottom: -.55rem; }
  .tone-blue { color: var(--wc-blue); }
  .tone-orange { color: var(--wc-orange); }
  .tone-violet { color: var(--wc-violet); }
  .tone-blue em { background: var(--wc-blue); }
  .tone-orange em { background: var(--wc-orange); }
  .tone-violet em { background: var(--wc-violet); }
  .wildcard-formula-legend { display: grid; gap: .46rem; }
  .wildcard-formula-legend > div { display: grid; grid-template-columns: auto minmax(0,1fr); gap: .52rem; min-width: 0; padding: .58rem; border-radius: 10px; }
  .wildcard-formula-legend > div > b { display: grid; place-items: center; width: 28px; height: 28px; border-radius: 999px; color: #fff; background: #6e8ba8; font-size: .76rem; }
  .wildcard-formula-legend .tone-blue > b { background: var(--wc-blue); }
  .wildcard-formula-legend .tone-orange > b { background: var(--wc-orange); }
  .wildcard-formula-legend .tone-violet > b { background: var(--wc-violet); }
  .wildcard-formula-legend section { min-width: 0; }
  .wildcard-formula-legend small { display: block; font-size: .72rem; font-weight: 800; }
  .wildcard-formula-legend strong { display: inline; margin-right: .35rem; color: var(--wc-navy); font-size: .88rem; }
  .wildcard-formula-legend span { display: block; overflow-x: auto; font-family: ui-monospace,SFMono-Regular,Menlo,monospace; font-size: .81rem; white-space: nowrap; }
  .wildcard-result-card { display: grid; gap: .55rem; padding: .85rem; border-color: #bee1ca; border-radius: 15px; background: linear-gradient(145deg,#fff,#f0faf4); }
  .wildcard-result-card > div { display: grid; grid-template-columns: 52px 1fr; gap: .55rem; align-items: center; }
  .wildcard-result-card small { color: var(--wc-green); font-size: .75rem; font-weight: 900; }
  .wildcard-result-card strong { color: var(--wc-navy); font-size: .86rem; }
  .wildcard-result-card .is-total { padding-top: .55rem; border-top: 1px dashed #a9d7b9; }
  .wildcard-result-card .is-total strong { color: var(--wc-green); font-size: 1.55rem; }
  .wildcard-result-path { display: grid; grid-template-columns: 1fr auto 1fr auto 1.15fr; gap: .34rem; align-items: center; }
  .wildcard-result-path span,
  .wildcard-result-path strong { padding: .58rem; border: 1px solid #d4e0eb; border-radius: 8px; background: #fff; font-size: .78rem; line-height: 1.45; text-align: center; }
  .wildcard-result-path strong { color: var(--wc-green); border-color: #baddca; background: #eef9f3; }
  .wildcard-result-path i { color: var(--wc-orange); font-style: normal; font-weight: 950; }

  .wildcard-symbol-lab {
    display: grid;
    grid-template-columns: repeat(3,minmax(0,1fr));
    gap: .8rem;
    margin: 1.3rem 0 2.2rem;
  }
  .wildcard-symbol-card {
    min-width: 0;
    padding: 1rem;
    border: 1px solid var(--wc-line);
    border-radius: 18px;
    background: linear-gradient(145deg,#fff,#f8fbff);
    box-shadow: 0 12px 30px rgba(35,73,117,.06);
  }
  .wildcard-symbol-card header { display: flex; gap: .7rem; align-items: center; }
  .wildcard-symbol-card header > code {
    display: grid;
    flex: 0 0 42px;
    place-items: center;
    width: 42px;
    height: 42px;
    border-radius: 12px;
    color: #fff;
    background: var(--wc-blue);
    font-size: 1.45rem;
    font-weight: 900;
  }
  .wildcard-symbol-card.is-question header > code { background: var(--wc-violet); }
  .wildcard-symbol-card.is-escape header > code { background: var(--wc-orange); }
  .wildcard-symbol-card header strong { display: block; color: var(--wc-navy); font-size: 1rem; }
  .wildcard-symbol-card header span { display: block; margin-top: .16rem; color: var(--wc-muted); font-size: .81rem; }
  .wildcard-token-demo,
  .wildcard-escape-demo {
    display: flex;
    gap: .35rem;
    align-items: center;
    margin: .8rem 0;
    min-height: 42px;
  }
  .wildcard-token-demo span { padding: .5rem .65rem; border-radius: 9px; font-size: .84rem; font-weight: 850; }
  .token-fixed { color: var(--wc-blue); background: var(--wc-blue-soft); }
  .token-stretch { flex: 1; color: #417598; background: repeating-linear-gradient(135deg,#eef6fb,#eef6fb 8px,#e3f0f7 8px,#e3f0f7 16px); text-align: center; }
  .token-slot { display: grid; place-items: center; width: 34px; color: var(--wc-violet); background: var(--wc-violet-soft); outline: 2px dashed #b89cda; }
  .wildcard-escape-demo { justify-content: center; }
  .wildcard-escape-demo > span { min-width: 82px; padding: .45rem; border: 1px solid #e2d8cf; border-radius: 10px; background: #fffaf6; text-align: center; }
  .wildcard-escape-demo code { display: block; color: var(--wc-orange); font-size: .9rem; font-weight: 900; }
  .wildcard-escape-demo small { color: var(--wc-muted); font-size: .75rem; }
  .wildcard-test-list { display: grid; gap: .34rem; }
  .wildcard-test-list > span { display: flex; justify-content: space-between; gap: .6rem; padding: .48rem .58rem; border-radius: 8px; background: #f7f9fb; font-size: .81rem; }
  .wildcard-test-list b { flex: none; }
  .wildcard-test-list .is-pass { color: var(--wc-green); background: var(--wc-green-soft); }
  .wildcard-test-list .is-fail { color: #a4663d; background: #fff6ed; }
  .wildcard-symbol-card p { margin: .75rem 0 0; color: var(--wc-muted); font-size: .84rem; line-height: 1.58; }

  .wildcard-position-map {
    display: grid;
    grid-template-columns: repeat(3,minmax(0,1fr));
    gap: .72rem;
    margin: 1.2rem 0 1.7rem;
  }
  .wildcard-position-map article { min-width: 0; padding: .9rem; border: 1px solid var(--wc-line); border-radius: 15px; background: #fff; }
  .wildcard-position-map article > span { color: var(--wc-blue); font-size: .75rem; font-weight: 900; }
  .wildcard-position-track { display: flex; min-height: 34px; margin: .55rem 0; overflow: hidden; border: 1px solid #d8e3ed; border-radius: 9px; }
  .wildcard-position-track > * { display: grid; flex: 1; place-items: center; min-width: 0; padding: .42rem; font-size: .76rem; font-style: normal; text-align: center; }
  .wildcard-position-track b { color: var(--wc-blue); background: var(--wc-blue-soft); }
  .wildcard-position-track i { color: #6f8193; background: repeating-linear-gradient(135deg,#f7f9fb,#f7f9fb 7px,#edf2f6 7px,#edf2f6 14px); }
  .wildcard-position-track em { flex: 0 0 36px; color: var(--wc-violet); background: var(--wc-violet-soft); font-weight: 900; }
  .wildcard-position-map article > code { display: block; color: var(--wc-navy); font-size: .9rem; font-weight: 850; }
  .wildcard-position-map p { margin: .4rem 0 0; color: var(--wc-muted); font-size: .81rem; line-height: 1.5; }

  .wildcard-function-route {
    margin: 1.3rem 0 1.8rem;
    overflow: hidden;
    border: 1px solid #cbdceb;
    border-radius: 22px;
    background: linear-gradient(145deg,#f7fbff,#fff);
  }
  .wildcard-function-route > header { padding: 1rem 1.1rem .8rem; }
  .wildcard-function-route > header span { color: var(--wc-violet); font-size: .75rem; font-weight: 900; letter-spacing: .08em; }
  .wildcard-function-route > header strong { display: block; margin-top: .25rem; color: var(--wc-navy); font-size: 1.02rem; }
  .wildcard-function-route__grid { display: grid; grid-template-columns: repeat(2,minmax(0,1fr)); gap: .7rem; padding: 0 1rem 1rem; }
  .wildcard-function-route article { min-width: 0; padding: .85rem; border: 1px solid #d8e3ed; border-radius: 14px; background: rgba(255,255,255,.94); }
  .wildcard-function-route article > span { color: var(--wc-blue); font-size: .72rem; font-weight: 900; }
  .wildcard-function-route article > strong { display: block; margin: .28rem 0 .58rem; color: var(--wc-navy); font-size: .9rem; }
  .wildcard-function-route article > div { display: flex; flex-wrap: wrap; gap: .35rem; }
  .wildcard-function-route article > div code { padding: .32rem .45rem; border: 1px solid #ccdef1; border-radius: 7px; color: #155fae; background: #eff6fe; font-size: .75rem; font-weight: 800; }
  .wildcard-function-route article > small { display: block; margin-top: .58rem; color: var(--wc-muted); font-size: .81rem; line-height: 1.5; }

  .wildcard-condition-family {
    display: grid;
    grid-template-columns: repeat(2,minmax(0,1fr));
    gap: .7rem;
    margin: 1rem 0 1.3rem;
    padding: 1rem;
    border: 1px solid #d1deeb;
    border-radius: 18px;
    background: #fbfdff;
  }
  .wildcard-condition-family article { min-width: 0; padding: .85rem; border: 1px solid #d8e3ed; border-radius: 12px; background: #fff; }
  .wildcard-condition-family span { color: var(--wc-blue); font-size: .75rem; font-weight: 900; }
  .wildcard-condition-family code { display: block; overflow-x: auto; margin: .45rem 0; color: #155fae; font-size: .86rem; white-space: nowrap; }
  .wildcard-condition-family strong { color: var(--wc-green); font-size: .84rem; }

  .wildcard-lookup-link { display: flex; justify-content: space-between; gap: 1rem; align-items: center; margin: 1rem 0; padding: .9rem 1rem; border: 1px solid #cbdceb; border-radius: 14px; background: #f4f9ff; }
  .wildcard-lookup-link span { display: block; color: var(--wc-blue); font-size: .75rem; font-weight: 900; }
  .wildcard-lookup-link strong { display: block; margin-top: .2rem; color: var(--wc-navy); font-size: .88rem; }
  .wildcard-lookup-link a { flex: none; padding: .55rem .7rem; border-radius: 9px; color: #fff; background: var(--wc-blue); font-size: .81rem; font-weight: 850; text-decoration: none; }

  .wildcard-mode-compare,
  .wildcard-before-after {
    display: grid;
    grid-template-columns: minmax(0,1fr) auto minmax(0,1fr);
    gap: .8rem;
    align-items: center;
    margin: 1.15rem 0 1.5rem;
  }
  .wildcard-mode-compare section,
  .wildcard-before-after section { min-width: 0; padding: 1rem; border: 1px solid #e2d4cf; border-radius: 17px; background: linear-gradient(145deg,#fff,#fff8f5); }
  .wildcard-mode-compare section.is-correct,
  .wildcard-before-after section.is-recommended { border-color: #bfe1cb; background: linear-gradient(145deg,#fff,#f1faf4); }
  .wildcard-mode-compare > div,
  .wildcard-before-after > div { color: #7e9ab8; font-size: 1.2rem; font-weight: 900; }
  .wildcard-mode-compare span,
  .wildcard-before-after span { color: #a45c49; font-size: .75rem; font-weight: 900; }
  .wildcard-mode-compare .is-correct span,
  .wildcard-before-after .is-recommended span { color: var(--wc-green); }
  .wildcard-mode-compare code,
  .wildcard-before-after code { display: block; overflow-x: auto; margin: .5rem 0; padding: .65rem; border: 1px solid #dce4eb; border-radius: 8px; color: #155fae; background: #fff; font-size: .84rem; white-space: nowrap; }
  .wildcard-mode-compare strong,
  .wildcard-before-after strong { display: block; color: var(--wc-navy); font-size: .92rem; }
  .wildcard-mode-compare p,
  .wildcard-before-after p { margin: .48rem 0 0; color: var(--wc-muted); font-size: .84rem; line-height: 1.55; }
  .wildcard-step-chain { display: flex; flex-wrap: wrap; gap: .3rem; align-items: center; margin: .6rem 0; }
  .wildcard-step-chain i { padding: .34rem .45rem; border-radius: 7px; color: #6c4f48; background: #f8ece8; font-size: .75rem; font-style: normal; font-weight: 850; }
  .wildcard-before-after .is-recommended .wildcard-step-chain i { color: var(--wc-green); background: var(--wc-green-soft); }
  .wildcard-step-chain b { color: #9badbd; font-size: .75rem; }

  .wildcard-trouble-stack { display: grid; gap: .65rem; margin: 1.15rem 0 2rem; }
  .wildcard-trouble-stack article { display: grid; grid-template-columns: 34px minmax(0,1fr) auto; gap: .75rem; align-items: start; padding: .85rem; border: 1px solid #d8e2ec; border-radius: 14px; background: #fff; }
  .wildcard-trouble-stack article > span { display: grid; place-items: center; width: 36px; height: 36px; border-radius: 10px; color: #fff; background: linear-gradient(145deg,#f28b4e,#d85a2a); font-size: .72rem; font-weight: 900; }
  .wildcard-trouble-stack h3 { margin: 0 0 .25rem; color: var(--wc-navy); font-size: .92rem; }
  .wildcard-trouble-stack p { margin: 0; color: var(--wc-muted); font-size: .81rem; line-height: 1.58; }
  .wildcard-trouble-stack article > b { align-self: center; padding: .32rem .5rem; border-radius: 999px; color: #a85f35; background: #fff3e8; font-size: .72rem; white-space: nowrap; }

  .wildcard-final-check { display: flex; flex-wrap: wrap; gap: .45rem; align-items: center; margin: 1.2rem 0 .8rem; padding: .85rem; border: 1px solid #bedfca; border-radius: 15px; background: linear-gradient(145deg,#fff,#f1faf4); }
  .wildcard-final-check span { padding: .34rem .52rem; border-radius: 999px; color: var(--wc-green); background: #e5f5eb; font-size: .75rem; font-weight: 850; }
  .wildcard-final-check strong { flex: 1 1 260px; color: var(--wc-navy); font-size: .88rem; }

  html[data-theme='dark'] .wildcard-sheet-hero,
  html[data-theme='dark'] .wildcard-symbol-card,
  html[data-theme='dark'] .wildcard-position-map article,
  html[data-theme='dark'] .wildcard-function-route,
  html[data-theme='dark'] .wildcard-condition-family,
  html[data-theme='dark'] .wildcard-lookup-link,
  html[data-theme='dark'] .wildcard-mode-compare section,
  html[data-theme='dark'] .wildcard-before-after section,
  html[data-theme='dark'] .wildcard-trouble-stack article,
  html[data-theme='dark'] .wildcard-final-check {
    --wc-navy: #edf6ff;
    --wc-text: #dce8f5;
    --wc-muted: #a3b4c4;
    --wc-line: #29445e;
    border-color: #29445e;
    background: linear-gradient(145deg,#0e1d2d,#11263b);
    box-shadow: none;
  }
  html[data-theme='dark'] .wildcard-sheet-table-wrap,
  html[data-theme='dark'] .wildcard-formula-panel,
  html[data-theme='dark'] .wildcard-formula-line,
  html[data-theme='dark'] .wildcard-formula-legend > div,
  html[data-theme='dark'] .wildcard-result-card,
  html[data-theme='dark'] .wildcard-result-path span,
  html[data-theme='dark'] .wildcard-result-path strong,
  html[data-theme='dark'] .wildcard-sheet-legend span,
  html[data-theme='dark'] .wildcard-function-route article,
  html[data-theme='dark'] .wildcard-condition-family article,
  html[data-theme='dark'] .wildcard-mode-compare code,
  html[data-theme='dark'] .wildcard-before-after code {
    border-color: #2c4964;
    background: #0b1928;
  }
  html[data-theme='dark'] .wildcard-sheet-table { border-color: #31516b; background: #0b1927; }
  html[data-theme='dark'] .wildcard-sheet-table td { border-color: #243f59; color: #cfdeeb; background: #0d1c2b; }
  html[data-theme='dark'] .wildcard-sheet-table .sheet-column-letters th,
  html[data-theme='dark'] .wildcard-sheet-table .sheet-row-number { color: #c9d8e5; border-color: #29475f; background: #16283a; }
  html[data-theme='dark'] .wildcard-sheet-table .sheet-column-letters .sheet-corner { background: linear-gradient(135deg, transparent 49%, #617588 50%, #617588 100%), #16283a; }
  html[data-theme='dark'] .wildcard-sheet-table .is-condition-column,
  html[data-theme='dark'] .wildcard-sheet-table .is-sum-column { background: #10283d; }
  html[data-theme='dark'] .wildcard-sheet-table tr.is-hit td { color: #8fe0b6; background: #153c32; }
  html[data-theme='dark'] .wildcard-sheet-table tr.is-hit .sheet-row-number { color: #8fe0b6; background: #17382f; }
  html[data-theme='dark'] .wildcard-sheet-table td.is-match,
  html[data-theme='dark'] .wildcard-sheet-table td.is-sum { color: #8fe0b6; }

  @media (max-width: 900px) {
    .wildcard-sheet-workspace { grid-template-columns: 1fr; }
    .wildcard-symbol-lab,
    .wildcard-position-map { grid-template-columns: repeat(2,minmax(0,1fr)); }
  }
  @media (max-width: 680px) {
    .wildcard-sheet-hero { border-radius: 20px; }
    .wildcard-sheet-hero__header { align-items: flex-start; padding: 1rem; }
    .wildcard-excel-mark { flex-basis: 44px; width: 44px; height: 44px; font-size: 1.25rem; }
    .wildcard-sheet-workspace { padding: 0 .8rem .9rem; }
    .wildcard-sheet-table-wrap { overflow-x: auto; }
    .wildcard-sheet-table { min-width: 720px; table-layout: fixed; }
    .wildcard-condition-family,
    .wildcard-function-route__grid { grid-template-columns: 1fr; }
    .wildcard-symbol-lab,
    .wildcard-position-map { grid-template-columns: 1fr; }
    .wildcard-mode-compare,
    .wildcard-before-after { grid-template-columns: 1fr; }
    .wildcard-mode-compare > div,
    .wildcard-before-after > div { transform: rotate(90deg); text-align: center; }
    .wildcard-trouble-stack article { grid-template-columns: 34px minmax(0,1fr); }
    .wildcard-trouble-stack article > b { grid-column: 2; justify-self: start; }
    .wildcard-lookup-link { align-items: flex-start; flex-direction: column; }
    .wildcard-result-path { grid-template-columns: 1fr; }
    .wildcard-result-path i { transform: rotate(90deg); text-align: center; }
  }
`}</style>
