---
title: "不要用人的勤奋，掩盖数据流程的落后"
description: "从两套电商报表制度出发，讨论为什么重复下载、筛选、填写和刷新不该被当作执行力，以及如何用统一订单数据源重构流程。"
date: 2026-08-03T08:56:00+08:00
updated: 2026-08-03T09:17:00+08:00
category: "实践"
tags:
  - "数据处理"
  - "自动化"
  - "职场观察"
minutes: 11
featured: false
draft: false
private: false
---

import HighlightBox from '../../components/HighlightBox.astro';

<div class="report-story-hero">
  <div class="report-story-hero-copy">
    <span class="report-story-kicker">数据流程观察</span>
    <h2>不要用人的勤奋，<br />掩盖流程设计的落后</h2>
    <p>每增加一个业务需求，就增加一项永久人工操作。流程制造了复杂度，又开始奖励最能忍受复杂度的人。</p>
  </div>

  <div class="report-story-counter" aria-label="业务需求增加后不断累积的人工操作">
    <div class="report-story-counter-head">
      <span>业务需求</span>
      <strong>+1</strong>
    </div>
    <div class="report-story-counter-list">
      <p><span>人工下载</span><b>+1</b></p>
      <p><span>日期筛选</span><b>+1</b></p>
      <p><span>手工填写</span><b>+1</b></p>
      <p><span>固定负责人</span><b>+1</b></p>
      <p><span>日报刷新</span><b>+10</b></p>
    </div>
    <div class="report-story-counter-result">
      <small>最后得到一句评价</small>
      <strong>“这个员工执行力很强”</strong>
    </div>
  </div>
</div>

公司数据报表里经常存在一种很熟悉的工作方式：

打开表格，查看操作步骤；打开网页，选择指定日期；下载数据，核对列头；清空旧数据，再把新数据复制进去；最后刷新公式，检查结果有没有异常。

一套流程做久了，操作越快、步骤记得越牢、越不容易出错的人，通常就会被认为“执行力强”。

但如果把视角再往前推一步，就会发现一个更值得讨论的问题：

> **为什么这些固定、重复、规则明确的操作，直到今天仍然必须依赖一个人熟练完成？**

<div class="report-story-manual-line" aria-label="人工报表操作流水线">
  <div><b>01</b><span>打开网页</span></div>
  <i>→</i>
  <div><b>02</b><span>筛选日期</span></div>
  <i>→</i>
  <div><b>03</b><span>下载文件</span></div>
  <i>→</i>
  <div><b>04</b><span>核对列头</span></div>
  <i>→</i>
  <div><b>05</b><span>复制填写</span></div>
  <i>→</i>
  <div><b>06</b><span>逐表刷新</span></div>
</div>

这条流水线没有创造新的业务判断。它只是把数据从一个地方搬到另一个地方，并让人的记忆、时间和注意力承担系统本该承担的责任。

## 两张表看起来不同，底层问题完全一样

我最近看到的两套电商数据制度——「全平台商品维度退款率」和「抖音每日报表」——表面上处理的是不同指标，实际上遵循的是同一种管理逻辑。

<div class="report-story-case-system">
  <div class="report-story-case-grid">
    <section class="report-story-case-card">
      <span class="report-story-case-number">案例 01</span>
      <h3>全平台商品维度退款率</h3>
      <p class="report-story-case-mode">固定时间 · 共享表格 · 逐表刷新</p>
      <ul>
        <li>固定负责人每月 1 日、16 日更新</li>
        <li>人工筛选 1—15 日或 16 日至月底</li>
        <li>共享表更新后，10 张日报分别刷新</li>
        <li>负责人缺席会影响整条更新链路</li>
      </ul>
    </section>

    <section class="report-story-case-card">
      <span class="report-story-case-number">案例 02</span>
      <h3>抖音每日报表</h3>
      <p class="report-story-case-mode">多人填写 · 多个数据源 · 多项人工计算</p>
      <ul>
        <li>拍单金额依赖多人每天录入</li>
        <li>直播间和订单退款率人工维护</li>
        <li>上月退款率需要单独计算</li>
        <li>成交件数额外下载抖音电商·罗盘</li>
      </ul>
    </section>
  </div>

  <div class="report-story-case-arrows" aria-hidden="true"><span>↓</span><span>↓</span></div>

  <div class="report-story-common-problem">
    <span>共同底层</span>
    <strong>用人的注意力，弥补数据流程的缺陷</strong>
    <div>
      <p>人工选择日期</p>
      <p>人工搬运数据</p>
      <p>依赖固定人员</p>
      <p>出错后继续加检查</p>
    </div>
  </div>
</div>

### 第一套：全平台商品维度退款率

这张表目前每半个月更新一次，由固定负责人清空旧数据、更新自己负责的店铺，再通知其他人员更新各自店铺。更新完成后，每一张需要退款率的日报还要打开共享表并刷新。

下载订单和退款数据以后，还要人工筛选固定日期范围。操作人员必须判断当前月份是 28 天、30 天还是 31 天，月份切换后有没有修改条件，不同人员使用的日期口径是否一致。

这些都不是业务分析，而是为了让数据进入报表而产生的流程成本。

### 第二套：抖音每日报表

抖音每日报表的问题更分散：直播间退款率使用固定值，拍单金额由多人填写，订单退款率和上月退款率人工计算，订单成交件数还要额外下载「抖音电商·罗盘」。

多人拍单时，每个人都要打开同一张表填写金额。表格不能同时编辑，就会出现占用、冲突、漏填、重复填写和金额错误。

而订单成交件数本来可以直接从订单明细统计，却因为现有报表结构没有利用订单数据，又多维护了一份数据源。

> **它们不是两张表的问题，而是同一种数据管理制度：报表围绕“人应该怎么操作”设计，而不是围绕“数据应该怎么流转”设计。**

## 最容易被忽略的误区：把流程熟练度当成人才能力

一个员工能熟练打开网页、筛选日期、下载文件、核对表头、复制数据，当然说明他认真、负责，也熟悉现有流程。

但这只能证明他能够稳定维护这套制度，不能证明这套制度本身合理。

<div class="report-story-ability-board">
  <div class="report-story-ability-title">
    <small>需要重新区分的两件事</small>
    <strong>熟练度 ≠ 流程合理性</strong>
  </div>
  <div class="report-story-ability-columns">
    <section>
      <span>现有评价方式</span>
      <p>记住所有操作步骤</p>
      <p>固定时间准时更新</p>
      <p>复制粘贴很少出错</p>
      <p>熟练维护复杂报表</p>
      <p>成为流程中的固定人员</p>
    </section>
    <section>
      <span>更有价值的能力</span>
      <p>删除没有必要的步骤</p>
      <p>让更新时间不再依赖个人</p>
      <p>让流程不再需要复制</p>
      <p>重新设计数据结构</p>
      <p>让普通人也能稳定执行</p>
    </section>
  </div>
</div>

如果一项工作满足以下特征：输入和输出基本固定，日期范围可以由规则计算，字段映射可以提前定义，汇总口径可以写成公式或程序，那么它首先应该被判断为**可标准化、可自动化的流程**。

否则就会产生一个很荒诞的结果：流程设计得越差，需要记住的步骤越多；能维护这套复杂流程的人越少；这个岗位看起来就越“重要”。

最后，制度制造了复杂度，又把适应复杂度的人当成不可替代的人才。

<HighlightBox eyebrow="真正的问题" title="公司把维护低效流程的能力，当成了人才能力">
  执行力不应该只是按时完成下载、筛选、填写和刷新。发现重复劳动、统一数据口径、减少人为判断，让同样的工作不再依赖固定人员完成，同样是执行力，而且是更有价值的执行力。
</HighlightBox>

## 从“多种几块地”到“提高单位产出”

这种思路和农业时代最朴素的生产逻辑很像。

需要更多粮食，就投入更多人、耕种更多土地、工作更长时间。劳动越辛苦，越容易被理解为勤奋和光荣。

工业化改变的不是“人要不要劳动”，而是开始追问：能不能通过机器、工具和生产流程，提高单位时间的产出？今天的数据工作也处在类似变化中。

<div class="report-story-evolution" aria-label="从堆叠劳动到自动化生产方式的演进">
  <section>
    <span>01 · 农业式投入</span>
    <strong>需要更多粮食</strong>
    <div><p>增加土地</p><i>→</i><p>增加人力</p><i>→</i><p>增加劳动时间</p></div>
  </section>
  <section>
    <span>02 · 传统报表思维</span>
    <strong>需要更多指标</strong>
    <div><p>增加下载</p><i>→</i><p>增加填写</p><i>→</i><p>增加负责人</p></div>
  </section>
  <section>
    <span>03 · 自动化思维</span>
    <strong>需要更多指标</strong>
    <div><p>检查明细</p><i>→</i><p>统一数据</p><i>→</i><p>自动计算</p></div>
  </section>
</div>

“多种几块地”和“多加几个人工步骤”，本质上都是靠堆投入解决问题。

AI 和自动化并没有让所有脑力劳动失去价值，但它们正在让**规则明确、能够重复、可以被程序描述的脑力劳动迅速贬值**。

真正应该继续由人承担的，是判断指标口径是否合理、分析退款率为什么变化、识别数据异常和业务风险、决定下一步经营动作，以及设计更简单稳定的工作流程。

## 两套制度其实只需要同一个改造方向

两张表不需要分别继续增加更多操作说明。它们可以共用一套数据处理框架。

<div class="report-story-data-system" aria-label="统一订单数据源自动计算报表指标的数据管道">
  <div class="report-story-data-system-head">
    <span>统一数据管道</span>
    <strong>不是让 RPA 原样重复人工点击，而是重新整理数据源和计算逻辑</strong>
  </div>

  <div class="report-story-data-pipeline">
    <section class="report-story-pipeline-stage report-story-pipeline-source">
      <small>输入</small>
      <h3>统一数据底座</h3>
      <p>订单明细</p>
      <p>退款明细</p>
      <p><code>HS</code> 拍单标识</p>
    </section>
    <i>→</i>
    <section class="report-story-pipeline-stage">
      <small>规则</small>
      <h3>自动确定周期</h3>
      <p>更新日前 15 天</p>
      <p>上一个自然月</p>
      <p>当日 / 最近 7、15、30 天</p>
    </section>
    <i>→</i>
    <section class="report-story-pipeline-stage">
      <small>计算</small>
      <h3>自动生成指标</h3>
      <p>商品与订单退款率</p>
      <p>拍单金额、成交件数</p>
      <p>直播间及上月退款率</p>
    </section>
    <i>→</i>
    <section class="report-story-pipeline-stage report-story-pipeline-human">
      <small>人工价值</small>
      <h3>只处理异常与决策</h3>
      <p>检查数据完整性</p>
      <p>分析指标变化原因</p>
      <p>决定后续经营动作</p>
    </section>
  </div>
</div>

### 1. 用订单明细和退款明细作为统一数据底座

订单和退款明细里已经包含大部分报表需要的基础事实，例如订单时间、商品、数量、金额、退款状态和退款金额。

内部已经使用 `HS` 标记拍单订单，就可以根据该标识自动汇总拍单金额，不再要求每位拍单人员手动填写。

### 2. 下载足够长的日期范围，不要求人工精确筛选

每次下载最近 30 天或 90 天的数据即可。报表需要最近 15 天，就由公式或程序自动取更新日前一天向前推 15 天；需要上一个完整自然月，就自动计算上月第一天和最后一天。

操作人员不再需要判断今天应该选 1—15 日、16 日至月底，还是跨月的某 15 天。

<div class="report-story-date-rules">
  <div><span>最近 15 天退款率</span><b>更新日前一天 → 向前取 15 天</b></div>
  <div><span>上月退款率</span><b>上一个完整自然月</b></div>
  <div><span>当日拍单金额</span><b>当天订单 + <code>HS</code> 标识</b></div>
  <div><span>订单成交件数</span><b>订单明细直接汇总数量</b></div>
</div>

例如：

- 8 月 16 日更新，最近 15 天为 8 月 1 日至 8 月 15 日；
- 8 月 17 日更新，最近 15 天为 8 月 2 日至 8 月 16 日；
- 9 月 1 日更新，最近 15 天为 8 月 17 日至 8 月 31 日；
- 8 月报表中的上月退款率，固定取 7 月 1 日至 7 月 31 日。

日期变化不再增加人工操作，也不需要固定某一天必须完成更新。

## 减少的不是人的价值，而是对人的浪费

优化后的流程不是完全不要人，而是重新安排人的注意力。

<div class="report-story-outcome">
  <section class="report-story-outcome-remove">
    <span>可以删除的人工动作</span>
    <p>半月固定日期更新</p>
    <p>人工筛选 1—15 日</p>
    <p>多人填写拍单金额</p>
    <p>单独下载罗盘成交件数</p>
    <p>10 张日报逐一刷新</p>
    <p>固定人员通知所有人</p>
  </section>

  <div class="report-story-outcome-core">
    <small>流程改造的目的</small>
    <strong>减少的不是人的价值，<br />而是对人的浪费</strong>
  </div>

  <section class="report-story-outcome-keep">
    <span>应该保留的人类工作</span>
    <p>检查数据是否缺失</p>
    <p>判断异常是否真实</p>
    <p>分析退款率变化原因</p>
    <p>确认业务统计口径</p>
    <p>决定后续经营动作</p>
  </section>
</div>

统一订单数据源以后，可以直接计算商品维度退款率、直播间退款率、拍单金额、订单退款率、店铺上月退款率和订单成交件数。

这并不是为了追求“所有事情都必须自动化”，而是因为这些指标已经具备清晰的数据来源和计算规则。继续保留人工填写，不会让数据更可靠，反而增加了更多出错入口。

## 真正的执行力，应该包括消灭不必要的执行

我并不否认认真执行的重要性。

任何自动化流程都需要有人负责数据源、字段变化、异常检查和最终结果。问题在于，组织不能把“严格按照低效步骤操作”当作执行力的完整定义。

更高层次的执行力应该包括：看见流程中的重复劳动，分清哪些步骤必须由人判断，哪些只是机械操作；统一数据来源和统计口径；用公式、RPA 或程序减少人为判断；把节省出来的时间用于真正的业务分析。

如果一套制度必须依靠固定人员、固定时间、固定记忆才能稳定运转，那么它首先是一套脆弱的制度。

员工准时完成它值得肯定，但不应该因此停止改造它。

<HighlightBox eyebrow="最后的判断" title="不要用人的勤奋，长期掩盖流程设计的落后">
  一个好的数据流程，不是让最熟练的人永远负责更多步骤，而是让普通人在更少的操作下也能得到稳定、准确、口径一致的结果。真正有价值的人才，也不应该只是最会维护旧流程的人，而应该包括那些能让旧流程不再需要被反复维护的人。
</HighlightBox>

<style>{`
  .report-story-hero,
  .report-story-case-system,
  .report-story-ability-board,
  .report-story-evolution,
  .report-story-data-system,
  .report-story-outcome {
    --report-warm: #f97316;
    --report-warm-soft: color-mix(in srgb, var(--report-warm) 13%, transparent);
    --report-cool: #22c7a9;
    --report-cool-soft: color-mix(in srgb, var(--report-cool) 12%, transparent);
  }

  .report-story-hero {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(17rem, .75fr);
    gap: clamp(1.5rem, 4vw, 3.5rem);
    margin: 1.2rem 0 2.4rem;
    padding: clamp(1.5rem, 4vw, 3rem);
    border: 1px solid color-mix(in srgb, var(--report-warm) 32%, var(--sl-color-gray-5));
    border-radius: 1.5rem;
    background:
      linear-gradient(90deg, color-mix(in srgb, var(--report-warm) 8%, transparent) 1px, transparent 1px),
      linear-gradient(color-mix(in srgb, var(--report-warm) 8%, transparent) 1px, transparent 1px),
      radial-gradient(circle at 12% 12%, color-mix(in srgb, var(--report-warm) 22%, transparent), transparent 34%),
      linear-gradient(145deg, color-mix(in srgb, var(--sl-color-bg-nav) 90%, transparent), var(--sl-color-bg));
    background-size: 34px 34px, 34px 34px, auto, auto;
    overflow: hidden;
  }

  .report-story-hero-copy {
    align-self: center;
  }

  .report-story-kicker,
  .report-story-case-number,
  .report-story-common-problem > span,
  .report-story-data-system-head > span,
  .report-story-outcome section > span {
    display: inline-block;
    color: var(--report-warm);
    font-size: .76rem;
    font-weight: 800;
    letter-spacing: .09em;
    text-transform: uppercase;
  }

  .report-story-hero h2 {
    margin: .8rem 0 1rem;
    max-width: 12ch;
    font-size: clamp(2.35rem, 6vw, 4.7rem);
    line-height: 1.02;
    letter-spacing: -.055em;
  }

  .report-story-hero-copy > p {
    max-width: 42rem;
    margin: 0;
    color: var(--sl-color-gray-2);
    font-size: 1.08rem;
    line-height: 1.8;
  }

  .report-story-counter {
    align-self: stretch;
    padding: 1.2rem;
    border: 1px solid color-mix(in srgb, var(--report-warm) 34%, var(--sl-color-gray-5));
    border-radius: 1.1rem;
    background: color-mix(in srgb, var(--sl-color-bg) 88%, transparent);
    box-shadow: 0 1.2rem 3rem color-mix(in srgb, var(--report-warm) 13%, transparent);
  }

  .report-story-counter-head,
  .report-story-counter-list p {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .report-story-counter-head {
    padding-bottom: .9rem;
    border-bottom: 1px solid var(--sl-color-gray-5);
  }

  .report-story-counter-head span {
    font-weight: 800;
  }

  .report-story-counter-head strong {
    color: var(--report-warm);
    font-size: 2rem;
  }

  .report-story-counter-list {
    padding: .45rem 0;
  }

  .report-story-counter-list p {
    margin: 0;
    padding: .48rem 0;
    color: var(--sl-color-gray-2);
    border-bottom: 1px dashed var(--sl-color-gray-5);
  }

  .report-story-counter-list b {
    color: var(--report-warm);
  }

  .report-story-counter-result {
    padding-top: .9rem;
  }

  .report-story-counter-result small,
  .report-story-counter-result strong {
    display: block;
  }

  .report-story-counter-result small {
    color: var(--sl-color-gray-3);
  }

  .report-story-counter-result strong {
    margin-top: .35rem;
    line-height: 1.45;
  }

  .report-story-manual-line {
    display: grid;
    grid-template-columns: repeat(11, auto);
    align-items: center;
    gap: .45rem;
    margin: 2rem 0;
    padding: 1rem;
    border: 1px solid color-mix(in srgb, #f97316 28%, var(--sl-color-gray-5));
    border-radius: 1.1rem;
    background: color-mix(in srgb, #f97316 7%, transparent);
  }

  .report-story-manual-line > div {
    min-width: 0;
    padding: .75rem .6rem;
    text-align: center;
    border-radius: .75rem;
    background: var(--sl-color-bg);
  }

  .report-story-manual-line b,
  .report-story-manual-line span {
    display: block;
  }

  .report-story-manual-line b {
    margin-bottom: .25rem;
    color: #f97316;
    font-size: .7rem;
  }

  .report-story-manual-line span {
    font-size: .86rem;
    font-weight: 750;
  }

  .report-story-manual-line i,
  .report-story-case-arrows span,
  .report-story-data-pipeline > i {
    color: #f97316;
    font-style: normal;
    font-weight: 900;
  }

  .report-story-case-system {
    margin: 1.8rem 0 2.2rem;
  }

  .report-story-case-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
  }

  .report-story-case-card {
    padding: 1.35rem;
    border: 1px solid color-mix(in srgb, var(--report-warm) 25%, var(--sl-color-gray-5));
    border-radius: 1rem;
    background: linear-gradient(160deg, var(--report-warm-soft), color-mix(in srgb, var(--sl-color-bg) 95%, transparent));
  }

  .report-story-case-card h3 {
    margin: .5rem 0 .35rem;
    font-size: 1.25rem;
  }

  .report-story-case-mode {
    margin: 0 0 1rem;
    color: var(--sl-color-gray-2);
    font-size: .86rem;
  }

  .report-story-case-card ul {
    margin: 0;
    padding-left: 1.2rem;
  }

  .report-story-case-arrows {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    padding: .25rem 0;
    text-align: center;
  }

  .report-story-case-arrows span {
    font-size: 1.5rem;
  }

  .report-story-common-problem {
    padding: 1.35rem;
    text-align: center;
    border: 1px solid color-mix(in srgb, var(--report-warm) 45%, var(--sl-color-gray-5));
    border-radius: 1rem;
    background: color-mix(in srgb, var(--report-warm) 10%, transparent);
  }

  .report-story-common-problem strong {
    display: block;
    margin: .45rem 0 1rem;
    font-size: clamp(1.25rem, 3vw, 1.8rem);
  }

  .report-story-common-problem > div {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: .55rem;
  }

  .report-story-common-problem p {
    margin: 0;
    padding: .45rem .7rem;
    border: 1px solid color-mix(in srgb, var(--report-warm) 25%, var(--sl-color-gray-5));
    border-radius: 999px;
    background: var(--sl-color-bg);
    font-size: .86rem;
  }

  .report-story-ability-board {
    margin: 1.8rem 0 2rem;
    overflow: hidden;
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 1.2rem;
  }

  .report-story-ability-title {
    padding: 1.35rem;
    text-align: center;
    background: linear-gradient(110deg, var(--report-warm-soft), var(--report-cool-soft));
  }

  .report-story-ability-title small,
  .report-story-ability-title strong {
    display: block;
  }

  .report-story-ability-title small {
    color: var(--sl-color-gray-2);
  }

  .report-story-ability-title strong {
    margin-top: .3rem;
    font-size: clamp(1.55rem, 4vw, 2.5rem);
    letter-spacing: -.03em;
  }

  .report-story-ability-columns {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .report-story-ability-columns section {
    padding: 1.25rem;
  }

  .report-story-ability-columns section + section {
    border-left: 1px solid var(--sl-color-gray-5);
    background: var(--report-cool-soft);
  }

  .report-story-ability-columns span {
    display: block;
    margin-bottom: .7rem;
    color: var(--sl-color-gray-2);
    font-size: .78rem;
    font-weight: 800;
    letter-spacing: .06em;
  }

  .report-story-ability-columns p {
    margin: 0;
    padding: .62rem 0;
    border-bottom: 1px dashed var(--sl-color-gray-5);
  }

  .report-story-ability-columns section:first-child p::before {
    content: "×";
    margin-right: .55rem;
    color: var(--report-warm);
    font-weight: 900;
  }

  .report-story-ability-columns section:last-child p::before {
    content: "✓";
    margin-right: .55rem;
    color: var(--report-cool);
    font-weight: 900;
  }

  .report-story-evolution {
    display: grid;
    gap: .75rem;
    margin: 1.8rem 0;
  }

  .report-story-evolution section {
    display: grid;
    grid-template-columns: 10rem minmax(10rem, .8fr) minmax(0, 2fr);
    align-items: center;
    gap: 1rem;
    padding: 1rem 1.15rem;
    border: 1px solid var(--sl-color-gray-5);
    border-radius: .95rem;
  }

  .report-story-evolution section:nth-child(-n + 2) {
    background: var(--report-warm-soft);
  }

  .report-story-evolution section:last-child {
    border-color: color-mix(in srgb, var(--report-cool) 45%, var(--sl-color-gray-5));
    background: var(--report-cool-soft);
  }

  .report-story-evolution span {
    color: var(--sl-color-gray-2);
    font-size: .78rem;
    font-weight: 800;
  }

  .report-story-evolution strong {
    font-size: 1.05rem;
  }

  .report-story-evolution section > div {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: .5rem;
  }

  .report-story-evolution p {
    margin: 0;
    padding: .45rem .65rem;
    text-align: center;
    border-radius: .55rem;
    background: var(--sl-color-bg);
    font-size: .86rem;
  }

  .report-story-evolution i {
    color: var(--sl-color-gray-3);
    font-style: normal;
  }

  .report-story-data-system {
    margin: 2rem 0;
    padding: clamp(1.25rem, 3vw, 1.8rem);
    border: 1px solid color-mix(in srgb, var(--report-cool) 42%, var(--sl-color-gray-5));
    border-radius: 1.25rem;
    background:
      radial-gradient(circle at 95% 0%, color-mix(in srgb, var(--report-cool) 18%, transparent), transparent 30%),
      linear-gradient(145deg, var(--report-cool-soft), color-mix(in srgb, var(--sl-color-bg) 96%, transparent));
  }

  .report-story-data-system-head {
    margin-bottom: 1.25rem;
  }

  .report-story-data-system-head > span {
    color: var(--report-cool);
  }

  .report-story-data-system-head > strong {
    display: block;
    margin-top: .35rem;
    max-width: 42rem;
    font-size: clamp(1.2rem, 3vw, 1.7rem);
    line-height: 1.45;
  }

  .report-story-data-pipeline {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: stretch;
    gap: .55rem;
  }

  .report-story-pipeline-stage {
    min-width: 0;
    padding: 1rem;
    border: 1px solid color-mix(in srgb, var(--report-cool) 24%, var(--sl-color-gray-5));
    border-radius: .9rem;
    background: color-mix(in srgb, var(--sl-color-bg) 94%, transparent);
  }

  .report-story-pipeline-stage small {
    color: var(--report-cool);
    font-weight: 800;
  }

  .report-story-pipeline-stage h3 {
    margin: .3rem 0 .75rem;
    font-size: 1.02rem;
  }

  .report-story-pipeline-stage p {
    margin: .35rem 0 0;
    color: var(--sl-color-gray-2);
    font-size: .82rem;
    line-height: 1.5;
  }

  .report-story-pipeline-human {
    border-color: color-mix(in srgb, var(--report-cool) 55%, var(--sl-color-gray-5));
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--report-cool) 12%, transparent);
  }

  .report-story-data-pipeline > i {
    align-self: center;
    color: var(--report-cool);
  }

  .report-story-date-rules {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: .75rem;
    margin: 1.5rem 0;
  }

  .report-story-date-rules > div {
    padding: 1rem;
    border-left: 3px solid #22c7a9;
    border-radius: 0 .8rem .8rem 0;
    background: color-mix(in srgb, #22c7a9 9%, transparent);
  }

  .report-story-date-rules span,
  .report-story-date-rules b {
    display: block;
  }

  .report-story-date-rules span {
    margin-bottom: .35rem;
    color: var(--sl-color-gray-2);
    font-size: .82rem;
  }

  .report-story-outcome {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(13rem, .85fr) minmax(0, 1fr);
    gap: 1rem;
    align-items: stretch;
    margin: 1.8rem 0 2rem;
  }

  .report-story-outcome section,
  .report-story-outcome-core {
    padding: 1.25rem;
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 1rem;
  }

  .report-story-outcome-remove {
    background: var(--report-warm-soft);
  }

  .report-story-outcome-keep {
    background: var(--report-cool-soft);
  }

  .report-story-outcome-keep > span {
    color: var(--report-cool) !important;
  }

  .report-story-outcome section p {
    margin: 0;
    padding: .5rem 0;
    border-bottom: 1px dashed var(--sl-color-gray-5);
    font-size: .9rem;
  }

  .report-story-outcome-remove p::before,
  .report-story-outcome-keep p::before {
    margin-right: .55rem;
    font-weight: 900;
  }

  .report-story-outcome-remove p::before {
    content: "×";
    color: var(--report-warm);
  }

  .report-story-outcome-keep p::before {
    content: "✓";
    color: var(--report-cool);
  }

  .report-story-outcome-core {
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: center;
    border-color: color-mix(in srgb, var(--report-cool) 35%, var(--report-warm));
    background: linear-gradient(145deg, var(--report-warm-soft), var(--report-cool-soft));
  }

  .report-story-outcome-core small {
    color: var(--sl-color-gray-2);
  }

  .report-story-outcome-core strong {
    margin-top: .5rem;
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    line-height: 1.45;
  }

  @media (max-width: 62rem) {
    .report-story-hero {
      grid-template-columns: 1fr;
    }

    .report-story-data-pipeline {
      grid-template-columns: 1fr;
    }

    .report-story-data-pipeline > i {
      justify-self: center;
      transform: rotate(90deg);
    }

    .report-story-evolution section {
      grid-template-columns: 8rem 1fr;
    }

    .report-story-evolution section > div {
      grid-column: 1 / -1;
      justify-content: flex-start;
    }

    .report-story-outcome {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 50rem) {
    .report-story-manual-line,
    .report-story-case-grid,
    .report-story-ability-columns,
    .report-story-date-rules {
      grid-template-columns: 1fr;
    }

    .report-story-manual-line > i {
      justify-self: center;
      transform: rotate(90deg);
    }

    .report-story-case-arrows {
      grid-template-columns: 1fr;
    }

    .report-story-case-arrows span:last-child {
      display: none;
    }

    .report-story-ability-columns section + section {
      border-top: 1px solid var(--sl-color-gray-5);
      border-left: 0;
    }

    .report-story-evolution section {
      grid-template-columns: 1fr;
      gap: .35rem;
    }

    .report-story-evolution section > div {
      grid-column: auto;
      display: grid;
      grid-template-columns: 1fr;
      justify-items: stretch;
      margin-top: .5rem;
    }

    .report-story-evolution section > div i {
      justify-self: center;
      transform: rotate(90deg);
    }

    .report-story-hero h2 {
      font-size: clamp(2.2rem, 12vw, 3.45rem);
    }
  }
`}</style>
