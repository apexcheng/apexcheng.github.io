# Python 图解系列改造临时执行计划

> 状态：部分完成（Batch 1、3 已完成）
> 创建日期：2026-08-27
> 性质：临时开发控制文件；系列改造完成并验收后删除
> 目标：把 Python 零基础系列从“代码块 + 文字解释”改造成“普通人能看见代码运行原理”的图解课程

---

## 一、这份文件怎么使用

这份文件是后续批次开发的单一任务边界。后续 Agent 收到“继续 Python 系列改造”之类的指令时，必须先读取本文件，只执行用户点名的批次；用户没有点名时，只执行第一个未完成批次。

硬性规则：

1. 一次只完成一个批次。
2. 每个批次只允许结构性改造 1～3 个章节；默认控制在 1～2 个章节。
3. 完成当前批次、验证、更新本文件状态后停止，不自动开始下一批。
4. 不扩大到其他系列、全站样式或无关组件重构。
5. 保留工作区已有改动，不回滚、覆盖或清理无关文件。
6. 默认不 commit、不 push、不部署，除非用户明确要求。
7. 现有公开文章尽量保留文件名和 URL；只有计划中明确标记“新增”的章节才创建新文件。
8. 新增公开文章保持 `draft: false`、`private: false`，并更新 `docs/writing/CONTENT_INDEX.md`。
9. 修改复杂视觉结构后只做当前批次需要的构建、桌面端和移动端检查。
10. 如果本文件与临时口头推断冲突，以本文件和用户当前明确指令为准；需要改变系列结构时先停下说明，不自行改路线。

推荐的后续指令格式：

```text
执行 PYTHON_VISUAL_SERIES_REFACTOR_PLAN.tmp.md 的 Batch 1，只完成该批次并更新执行记录。
```

---

## 二、改造目标与非目标

### 目标

- 让读者看见“当前执行哪一行、数据原来是什么、发生了什么、之后变成什么”。
- 使用真实订单、SKU、库存、文件和 JSON 场景贯穿系列。
- 保留可以直接复制运行的 Python 代码。
- 建立稳定但不过度抽象的 Python 图解语言。
- 桌面端表达完整关系，移动端按阅读顺序纵向重排。
- 让完整小脚本成为主线终点，再进入类与对象选修和综合考核。

### 非目标

- 不把每一段代码都装饰成卡片。
- 不追求覆盖 Python 全部语法或计算机专业课程内容。
- 不在入门主线引入第三方库、异步、数据库、Web 框架或算法训练。
- 不机械复制 Excel 图解手册的全部版式和 CSS 体量。
- 不为了统一而提前建设大型组件库。

核心判断：

```text
优先画出人眼看不见的运行状态，不为已经清楚的文字和代码增加装饰。
```

---

## 三、系列最终结构

### 阶段一：看见数据

1. 一行 Python 代码是怎么运行的
2. 变量、基础类型与转换
3. 字符串、列表与字典

### 阶段二：看见代码流动

4. 条件分支：为什么只走一条路
5. 循环：每一轮发生了什么
6. 函数：参数、局部变量与返回值

### 阶段三：接入真实任务

7. 文件与 JSON：外部数据怎样进入 Python
8. 错误与调试：异常如何发生和传递
9. 完整订单脚本：读取、清洗、筛选、汇总、输出

### 阶段四：选修与验收

10. 类与对象（选修）
11. 综合考核

系列入口继续使用“四阶段路线”，但必须描述能力怎样形成，不能只重复文章列表。

---

## 四、固定示例数据

各篇可以按主题取其中一部分，但关键字段和计算结果保持一致，避免读者跨文章时重新建立上下文。

```python
orders = [
    {"order_no": "A001", "sku": "K161", "amount": 129.0, "status": "已支付"},
    {"order_no": "A002", "sku": "K162", "amount": 88.0, "status": "已退款"},
    {"order_no": "A003", "sku": "K161", "amount": 199.0, "status": "已支付"},
]
```

固定验证结果：

- 已支付订单：`A001`、`A003`
- 已支付订单数量：`2`
- 已支付订单总金额：`328.0`
- 文本库存示例：`raw_stock = "3"`
- 转换结果：`stock = 3`
- 补货判断：`stock < 5` 得到 `True`

如某章必须增加字段，应在该章第一次出现时明确说明，不修改上述固定结果。

---

## 五、统一图解语言

### 5.1 每个核心机制的五段式

1. **真实输入**：订单、SKU、库存、JSON 或文件内容。
2. **可复制代码**：保留真实代码，突出当前执行行。
3. **运行轨迹**：展示执行前状态、当前操作和执行后状态。
4. **输出 / 下一步**：说明结果去了哪里，后续代码会拿到什么。
5. **边界 / 错误**：使用一个反例说明失败位置和结果。

不要求每个 `H2` 都机械套用五段式。一篇文章应有 2～4 个承担主关系的完整视觉板，其余内容使用正文、代码、表格或短提示。

### 5.2 稳定语义

颜色必须配合文字、编号和形状使用，不能单独承担意义。

| 语义 | 固定标记 |
| --- | --- |
| 输入 | `输入` + 编号 |
| 当前执行 | `当前行` + 代码行高亮 |
| 状态变化 | `之前` / `之后` |
| 判断 | `条件` + True / False 文本 |
| 输出 | `输出` + 具体值 |
| 错误 | `出错行` + 异常类型 + 后续去向 |

### 5.3 各主题的主视觉

| 主题 | 主视觉结构 |
| --- | --- |
| 代码执行 | 行号轨道 + 当前行 + 状态区 + 输出区 |
| 变量 | 名字—值关系图、重新绑定前后对照 |
| 类型转换 | 原始文本 → 转换函数 → 新类型 / 错误分支 |
| 容器 | 嵌套地图、访问路径、修改前后、引用关系 |
| 条件 | 表达式求值、决策门、唯一命中分支 |
| 循环 | 逐轮状态表：轮次、当前项、条件、累计值、输出 |
| 函数 | 调用点 → 参数 → 局部作用域 → return → 调用点 |
| 文件 / JSON | 外部文本 → 读取 / 解析 → Python 对象 → 写回 |
| 异常 | 出错行 → 异常对象 → 寻找处理器 → 恢复 / 终止 |
| 类与对象 | 类定义、实例创建、独立状态、方法修改前后 |

### 5.4 移动端转换

- 横向步骤改为纵向步骤，不通过缩小教学文字维持横排。
- 每一步重复必要的当前代码行或变量名，避免上下往返寻找对应关系。
- 循环逐轮表可以改成“每轮一张状态卡”，但不能丢失轮次之间的连续性。
- 长代码允许代码块内部滚动；整页不得横向溢出。
- 不把桌面关系简单改成卡片堆叠，必须保留输入、操作和结果的顺序。

---

## 六、现有文件到新章节的映射

| 新章节 | 文件动作 | 目标文件 |
| --- | --- | --- |
| 01 代码运行 | 重写现有文章，保留 URL | `src/content/posts/python-zero-basics-roadmap.mdx` |
| 02 变量与类型 | 重写现有文章 | `src/content/posts/python-variables-basic-types.mdx` |
| 03 容器 | 重写现有文章 | `src/content/posts/python-containers-practical.mdx` |
| 04 条件分支 | 从现有“判断与循环”文章保留并重写判断部分 | `src/content/posts/python-conditions-loops.mdx` |
| 05 循环 | 新增文章 | `src/content/posts/python-loops-runtime-visual-guide.mdx` |
| 06 函数 | 重写现有文章 | `src/content/posts/python-functions-practical.mdx` |
| 07 文件与 JSON | 从现有文章保留并重写 I/O、JSON 部分 | `src/content/posts/python-files-json-errors.mdx` |
| 08 错误与调试 | 新增文章 | `src/content/posts/python-errors-debugging-visual-guide.mdx` |
| 09 完整小脚本 | 新增文章 | `src/content/posts/python-order-processing-capstone.mdx` |
| 10 类与对象 | 重写现有文章并标明选修 | `src/content/posts/python-classes-objects-basics.mdx` |
| 11 综合考核 | 重构现有考核 | `src/content/posts/python-basic-assessment-20-questions.mdx` |

新增拆分章节时，允许同步修改后续文章的 `seriesOrder` 和上下篇链接；这类纯元数据改动不算跨章节内容重构。

---

## 七、逐章改造要求

### Chapter 01：一行 Python 代码是怎么运行的

**读者结果**

- 知道代码默认从上到下执行。
- 能区分“代码行、表达式、变量状态、输出”。
- 能沿着 6～8 行代码说出每一步发生了什么。

**必须图解**

1. 单行赋值：右侧表达式先求值，再把名字绑定到结果。
2. 8 行订单脚本运行轨道：当前行、高亮数据、判断结果、输出。
3. 修改一个输入值后，运行轨迹哪里发生变化。

**现有内容处理**

- 保留当前 8 行订单脚本和“按任务学习”的原则。
- 系列路线只保留简短入口，不再占据文章主体。
- Python 安装和命令只保留最小跑通步骤，不扩展 IDE、环境和包管理。

**禁止偏移**

- 不提前系统讲循环、函数和类。
- 不变成 Python 安装教程。

### Chapter 02：变量、基础类型与转换

**读者结果**

- 看懂变量名与值不是同一件事。
- 看懂 `str`、`int`、`float`、`bool`、`None` 的业务含义。
- 看懂文本转数值和比较产生布尔值的完整过程。

**必须图解**

1. `stock = 8` 的名字—值关系。
2. `stock = 8` 再执行 `stock = 5` 的重新绑定前后。
3. `"3" → int() → 3 → stock < 5 → True` 状态流水线。
4. `int("三")` 的错误分支，明确失败发生在转换步骤。

**现有内容处理**

- 重构并保留当前 `py-types-board` 的数据类型映射价值。
- 合并重复的小代码块，避免同一概念被拆成过多片段。
- 运算符只保留本章需要的比较和基础算术。

### Chapter 03：字符串、列表与字典

**读者结果**

- 看懂“一批订单是列表，一条订单是字典”。
- 能沿着访问路径找到嵌套字段。
- 看懂修改容器后，原数据哪里改变了。

**必须图解**

1. `orders → 第 1 项 → status` 的嵌套访问路径。
2. 列表索引和字典键的差异。
3. 修改订单状态前后对照。
4. 两个变量指向同一列表与浅拷贝后的关系对照。

**现有内容处理**

- 保留当前订单嵌套地图。
- 字符串、列表、字典只讲高频读取、修改和常用方法。
- 元组、集合、列表推导式和浅拷贝细节放入“进阶补充”，不打断主线。

**禁止偏移**

- 不做容器 API 大全。
- 不把所有方法各自做一张卡片。

### Chapter 04：条件分支

**读者结果**

- 看懂条件表达式先得到 True / False。
- 看懂 `if / elif / else` 从上到下检查并只命中一条分支。
- 能判断条件顺序为什么会改变结果。

**必须图解**

1. `stock < 5` 的求值与两条去向。
2. `if / elif / else` 的顺序命中轨迹。
3. `and`、`or`、`not` 的真假组合，不只给语法定义。
4. 错误顺序案例：`stock == 0` 为什么被前一个条件截走。

**现有内容处理**

- 使用现有 `python-conditions-loops.mdx`，保留 URL。
- 移走全部循环、累计、`continue`、`break` 内容到 Chapter 05。
- Mermaid 只在能清楚表达分支时保留，不和 HTML 图解重复表达同一结论。

### Chapter 05：循环

**读者结果**

- 看懂 `for` 每次拿出一条数据。
- 能追踪每一轮的当前项、条件、累计值和输出。
- 看懂 `continue` 与 `break` 改变了哪一步。

**必须图解**

1. 三条订单的逐轮状态表。
2. 筛选已支付订单时，每轮是否进入处理分支。
3. 金额累计值：`0 → 129.0 → 129.0 → 328.0`。
4. `continue` 与 `break` 的路径差异。

**新增文件**

- `src/content/posts/python-loops-runtime-visual-guide.mdx`
- `series: "python-basics"`
- `seriesOrder: 5`

**边界**

- `while` 和 `range` 作为次要补充。
- 不引入生成器、迭代器协议和异步循环。

### Chapter 06：函数

**读者结果**

- 看懂定义函数和调用函数不是同一时刻。
- 看懂实参如何进入参数名，局部变量何时存在。
- 看懂 `return` 如何把结果送回调用位置。

**必须图解**

1. 调用位置 → 参数 → 函数内部 → return → 原调用位置。
2. `print()` 与 `return` 的结果去向对照。
3. 局部变量与函数外变量的边界。
4. 可变默认参数为何会保留上次状态。

**现有内容处理**

- 保留现有函数工作台和订单金额案例。
- 合并零散参数示例，围绕一条主调用轨迹展开。
- `*args` 只在不破坏主线时作为补充；不引入装饰器、闭包和类型系统。

### Chapter 07：文件与 JSON

**读者结果**

- 看懂文件内容最初在 Python 外部。
- 看懂读取、解析、处理、序列化和写回的边界。
- 区分 `load / loads / dump / dumps` 的输入输出形态。

**必须图解**

1. `orders.json` 文本 → `json.load()` → `list[dict]`。
2. Python 对象 → `json.dump()` → 文件。
3. 文件 JSON 与 API 返回数据进入同一处理逻辑的汇合图。
4. `load / loads / dump / dumps` 四个入口的参数与结果映射。

**现有内容处理**

- 使用现有 `python-files-json-errors.mdx`，保留 URL。
- 保留 `with open()`、编码、读写模式和 `pathlib` 的够用内容。
- 移走异常机制主体到 Chapter 08，只在本章链接错误处理篇。

### Chapter 08：错误与调试

**读者结果**

- 能找到真正出错的代码行。
- 看懂异常对象如何寻找匹配的 `except`。
- 知道何时跳过坏记录、何时应该停止脚本。

**必须图解**

1. 正常路径与异常路径分叉。
2. `ValueError`、`FileNotFoundError`、`JSONDecodeError` 的发生位置对照。
3. 异常类型匹配：命中处理器或继续向外传播。
4. 批量订单中一条坏数据如何被记录并跳过。

**新增文件**

- `src/content/posts/python-errors-debugging-visual-guide.mdx`
- `series: "python-basics"`
- `seriesOrder: 8`

**禁止偏移**

- 不推荐无边界的 `except Exception` 静默吞错。
- 不扩展日志框架、监控系统和复杂调试器。

### Chapter 09：完整订单脚本

**读者结果**

- 独立看懂一份从输入文件到结果文件的完整脚本。
- 能把变量、容器、判断、循环、函数、JSON 和异常串起来。
- 能根据状态表定位某一步的错误结果。

**主任务**

```text
读取 orders.json
→ 校验并规范字段
→ 筛选已支付订单
→ 统计数量和金额
→ 写出 paid_orders_summary.json
```

固定结果：已支付 `2` 单，总金额 `328.0`。

**必须图解**

1. 整体数据管道。
2. 完整代码分段地图，每段对应一个职责。
3. 三条订单逐条经过脚本的状态表。
4. 输入文件与输出文件对照。
5. 一条金额异常记录进入错误分支的补充案例。

**新增文件**

- `src/content/posts/python-order-processing-capstone.mdx`
- `series: "python-basics"`
- `seriesOrder: 9`

**禁止偏移**

- 不引入 pandas、数据库、网络请求或类。
- 不为了“像项目”增加配置层和目录架构。

### Chapter 10：类与对象（选修）

**读者结果**

- 看懂类、实例、属性、方法和 `self`。
- 看懂两个对象的状态为什么互不影响。
- 能判断字典 + 函数已经够用，还是值得使用类。

**必须图解**

1. 字典 + 函数与类的职责对照。
2. `Product("K161", 3)` 创建对象的初始化轨迹。
3. 两个实例的独立状态面板。
4. 方法调用前后对象属性变化。

**现有内容处理**

- 保留现有对象工作台、`__init__` 和 `self` 图解。
- 标题、系列入口和正文开头明确标记“选修”。
- 不引入继承、多态、元类、描述符和设计模式。

### Chapter 11：综合考核

**读者结果**

- 不只会补语法，还能预测结果、追踪状态和定位错误。
- 能根据薄弱点回到对应章节补漏。

**建议题目结构**

- 01–05：看图 / 看代码预测输出。
- 06–12：基础编码任务。
- 13–16：运行轨迹与错误定位。
- 17–20：围绕订单脚本的综合任务。

**必须改造**

1. 每组题明确对应章节。
2. 至少 4 题要求填写逐步状态或解释某轮变化。
3. 至少 3 题要求定位具体出错行和异常类型。
4. 综合题复用 Chapter 09 的数据，不重新发明业务场景。
5. 下载附件与网页题目保持一致。

**边界**

- 保留 20 题规模，除非实现时发现明显重复并先报告。
- 不加入算法竞赛题和第三方库题。

---

## 八、批次计划

### Batch 1：执行起点与变量状态（Chapter 01–02）

**状态：** `[x] 已完成（2026-08-27）`

范围：

- 重写 Chapter 01。
- 重写 Chapter 02。
- 建立第一版稳定视觉语义：输入、当前行、之前、之后、输出、错误。

完成后停止，先验证视觉语法是否真的降低理解成本。

### Batch 2：容器形状（Chapter 03）

**状态：** `[ ] 待执行`

范围：

- 重写 Chapter 03。
- 验证嵌套地图、访问路径和引用关系在桌面端与移动端是否成立。
- 不开始条件或循环。

### Batch 3：条件与循环（Chapter 04–05）

**状态：** `[x] 已完成（2026-08-27）`

范围：

- 将现有判断与循环文章收缩为 Chapter 04。
- 新增 Chapter 05。
- 调整受影响文章的 `seriesOrder`，只改必要 frontmatter 和系列链接。

### Batch 4：函数调用帧（Chapter 06）

**状态：** `[ ] 待执行`

范围：

- 重写 Chapter 06。
- 重点验证调用位置、局部作用域和返回路径。
- 重复视觉结构只有在本批结束后确认真实复用价值，才考虑提取组件。

### Batch 5：外部数据与错误路径（Chapter 07–08）

**状态：** `[ ] 待执行`

范围：

- 将现有文件 / JSON / 异常文章收缩为 Chapter 07。
- 新增 Chapter 08。
- 检查两篇之间的链接与职责边界，不重复解释同一机制。

### Batch 6：完整脚本（Chapter 09）

**状态：** `[ ] 待执行`

范围：

- 新增 Chapter 09。
- 复用固定订单数据和前面已经定义的视觉语义。
- 不趁机改写前面文章；发现不一致只记录在本文件“待回看项”。

### Batch 7：选修与考核（Chapter 10–11）

**状态：** `[ ] 待执行`

范围：

- 重写 Chapter 10 并调整为选修。
- 重构 Chapter 11 题目结构与附件。
- 校验考核题与前 9 章真实内容一致。

### Batch 8：系列收口

**状态：** `[ ] 待执行`

本批不重写章节，只处理集成：

- 最终确认 1～11 的 `seriesOrder`。
- 更新系列入口的四阶段能力路线。
- 检查每篇上下篇链接、标题、描述和阅读时间。
- 更新 `docs/writing/CONTENT_INDEX.md`。
- 检查是否有旧标题、旧篇数“8 篇”或旧阶段说明残留。
- 运行最终相关构建，并检查代表性桌面端和移动端页面。

---

## 九、每个批次的执行流程

### 开始前

1. 读取本文件。
2. 读取 `docs/writing/BLOG_AGENT.md`。
3. 结构性重写时读取 `docs/ARTICLE_VISUAL_SYSTEM.md` 和 `docs/MDX_WRITING_GUIDE.md`。
4. 只读取当前批次目标文章。
5. 需要对照时优先参考 `excel-functions-visual-handbook.mdx` 的信息关系；不要通读多个 Demo。
6. 检查工作区已有改动，确认不覆盖无关内容。
7. 把当前 Batch 状态改成进行中：`[-] 进行中`。

### 实施中

1. 先统一当前批次的核心数据和固定结果。
2. 先完成主视觉结构，再补正文解释。
3. 代码必须可复制、可独立运行。
4. 不为短小单次逻辑创建通用组件。
5. 发现需要跨批次修改时，记录到“待回看项”，不要顺手扩大改动。

### 完成后

1. 以第一次学习 Python 的普通读者身份通读目标页面。
2. 检查每个核心图解是否能独立回答：输入是什么、当前做什么、状态怎么变、输出是什么。
3. 检查桌面端和移动端；确认移动端语义仍成立且整页无横向溢出。
4. 运行当前环境可用的相关构建或检查。
5. 新增公开文章时更新内容索引。
6. 更新本文件的 Batch 状态、执行记录和待回看项。
7. 停止，不自动进入下一批。

---

## 十、单章完成标准

一个章节只有同时满足以下条件才能标记完成：

- [ ] 标题、描述、章节定位与最终系列结构一致。
- [ ] 使用固定示例数据，或明确说明新增字段。
- [ ] 至少有一条从输入到输出的完整运行轨迹。
- [ ] 核心机制能看见当前代码行和前后状态。
- [ ] 代码保持可复制，示例能够独立运行。
- [ ] 图解承担真实关系，没有只为了丰富页面而堆卡片。
- [ ] 正文没有重复图解已经表达清楚的内容。
- [ ] 错误或边界案例指出具体失败步骤。
- [ ] 桌面端关系清楚。
- [ ] 移动端按语义纵向化，整页无横向溢出。
- [ ] 颜色之外存在文字、编号或形状提示。
- [ ] 相关构建或最小检查通过；无法运行时已说明原因。

---

## 十一、共享组件提取条件

默认先使用文章专属 class 和局部样式。只有满足以下条件才提取共享组件：

1. 同一结构已经在至少 2～3 篇文章真实复用。
2. 输入字段和移动端规则基本稳定。
3. 提取后能明显减少重复，同时不会限制每篇的信息关系。

可能的候选方向，仅作为观察项，不代表必须创建：

- 当前行 + 状态变化轨道
- 逐轮循环状态表
- 输入 / 处理 / 输出运行板
- 错误路径分支板

禁止为了“建立系统”一次性创建大量组件、配置协议或通用 DSL。

---

## 十二、待回看项

跨批次发现的问题只记录在这里，由后续对应批次处理。

- 暂无。

---

## 十三、批次执行记录

每完成一个批次追加记录，不覆盖历史。

### 记录模板

```md
### YYYY-MM-DD · Batch N

- 状态：已完成 / 部分完成 / 阻塞
- 改造章节：
- 修改文件：
- 核心视觉决定：
- 验证：
- 未解决问题：
- 新增待回看项：
```

### 2026-08-27 · Batch 1

- 状态：已完成
- 改造章节：Chapter 01、Chapter 02
- 修改文件：`python-zero-basics-roadmap.mdx`、`python-variables-basic-types.mdx`
- 核心视觉决定：统一使用“输入 / 当前行 / 之前 / 之后 / 输出 / 错误”语义；Chapter 01 以 8 行订单脚本展示从上到下执行，Chapter 02 以库存文本 `"3"` 展示绑定、转换、判断和失败位置。
- 执行方式：2 个子 Agent 并行，各自只负责 1 篇文章；主 Agent 统一复核和集成。
- 验证：单文件 MDX 检查、`git diff --check`、整站构建、1440px 桌面端和 390px 移动端页面检查均通过；未发现横向溢出。
- 未解决问题：无。
- 新增待回看项：无。

### 2026-08-27 · Batch 3

- 状态：已完成
- 改造章节：Chapter 04、Chapter 05
- 修改文件：`python-conditions-loops.mdx`、新增 `python-loops-runtime-visual-guide.mdx`；同步调整后续现有文章 `seriesOrder`、必要系列链接和内容索引。
- 核心视觉决定：Chapter 04 只保留条件求值、顺序命中和条件顺序错误；Chapter 05 用三条固定订单展示当前项、paid 筛选、累计器和 `continue` / `break` 路径差异。
- 执行方式：复用同一组 2 个子 Agent 并行，各自只负责 1 篇文章；主 Agent 负责元数据、索引、链接和整体验收。
- 验证：固定结果 `paid_count = 2`、`total = 328.0`；单文件 MDX 检查、`git diff --check`、整站构建、1440px 桌面端和 390px 移动端页面检查均通过；未发现横向溢出或浏览器错误。
- 未解决问题：Chapter 08、09 尚未创建，因此当前系列页只连续展示现有 9 篇；后续对应批次完成后会自然补齐为 11 篇。
- 新增待回看项：无。

---

## 十四、计划完成后的清理

全部 Batch 完成并通过最终验收后：

1. 确认长期有效的规则已经存在于正式文档，不把本文件当永久规则来源。
2. 将仍需继续处理的任务移入 `TODO.md`。
3. 删除本临时文件。
